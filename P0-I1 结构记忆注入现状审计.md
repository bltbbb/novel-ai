# P0-I1 结构记忆注入现状审计

对应执行计划：[设定与结构记忆边界重构执行计划.md](./设定与结构记忆边界重构执行计划.md)

## 审计目的

本记录用于完成 `P0-I1`：

- 抽样确认前台结构记忆 7 块里，哪些实际进入正文 `bundle`
- 确认当前真实口径是不是“前台 7 块，正文主注入 6 类”
- 为 `P0-I2` 的第一版注入增强提供基线

## 审计方式

- 审计日期：`2026-04-16`
- 审计脚本：[server/src/scripts/inspect-p0-i1-structure-injection.ts](./server/src/scripts/inspect-p0-i1-structure-injection.ts)
- 执行命令：

```powershell
npx tsx src/scripts/inspect-p0-i1-structure-injection.ts
```

说明：

- 使用临时测试环境造样本，不会改动真实调试库
- 在同一个样本里同时挂上：
  - `thread_ledger`
  - `foreshadow_plan`
  - `world_state`
  - `question_pool`
  - `antagonist_agenda`
  - `pov_permission`
  - `resource_continuity`
- 然后直接调用 `buildGenerationContextBundle()`，检查最终 section

## 结构记忆 7 块注入结论

| 前台能力 | 是否进正文主 bundle | 实际 sectionKey | 结论 |
|---|---|---|---|
| `ThreadLedger` | 是 | `thread_ledger` | 已进入正文主 bundle |
| `ForeshadowPlan` | 是 | `foreshadow_plan` | 已进入正文主 bundle |
| `WorldStateEntry` | 是 | `world_state_delta` | 已进入正文主 bundle |
| `QuestionPool` | 否 | 无 | 当前不进入 `buildGenerationContextBundle` |
| `AntagonistAgenda` | 是 | `antagonist_agenda` | 已进入正文主 bundle |
| `POVPermission` | 是 | `pov_permission` | 已进入正文主 bundle |
| `ResourceContinuity` | 是 | `resource_continuity` | 已进入正文主 bundle |

当前结论可以明确写成一句话：

**前台结构记忆共有 7 块，但正文主 bundle 当前只主注入 6 类，`QuestionPool` 不在正文主 bundle 链路里。**

## 样本输出摘录

### `thread_ledger`

```text
- 封门主线（主线 / 活跃 / 热度 5）
核心问题：沈砚能否在子时前压住封门局势
当前阶段：封门阵已经启动，第二枚印记开始回应
最近推进：第8章《封门前夜》
下一触发：沈砚必须在子时前靠近井口
当前卡点：顾沉舟利用封门阵逼灰契认主
预计收束卷：第2卷
```

### `foreshadow_plan`

```text
- 灰契第二枚印记（回收伏笔 / 核心伏笔）
当前状态：已激活
计划激活：第2卷
计划回收：第2卷
回收条件：确认第二枚印记真正归属
回收效果：反揭顾沉舟的布局
激活条件：顾沉舟启动封门阵，灰契在井口开始发热
```

### `antagonist_agenda`

```text
- 顾沉舟（井坊监印人）
当前目标：在沈砚靠近井口前完成封门并逼出灰契回应
当前动作：利用封门阵诱出沈砚血印反噬
出手触发：沈砚在子时前夜主动触印
主角不动时：顾沉舟会在子时后独吞第二枚印记
```

### `pov_permission`

```text
- 沈砚 / 章节 第9章 子时封门
禁止透露：顾沉舟其实在等第二枚印记主动认主
允许暗示：顾沉舟故意放慢封门节奏
本单元禁止揭晓：第二枚印记的真正持有人
```

### `world_state_delta`

```text
- 世界状态-本卷变化《第二卷》
公开事件：封门阵启动前夜，井口开始不稳定回应
舆论状态：顾沉舟正在有意拖慢封门节奏
当前风险：若封门失控，第二枚印记可能提前认主
```

### `resource_continuity`

```text
- 沈砚 / 灰契反噬
风险级别：致命
当前状态：血印反噬已经逼近临界点
行动限制：若强行触印，沈砚会先被反噬击穿
连续性风险：封门阵一旦回卷，沈砚会失去主动权
```

## `QuestionPool` 的真实位置

`QuestionPool` 当前不在正文主 `bundle` section 里。

它现在走的是另一条链路：

1. 前端卷纲页先把问题池整理成 `questionPoolBundle`
2. 卷纲 / 里程碑生成时把这个 bundle 塞进请求
3. 服务端在 `generation.ts` 里把 `questionPoolBundle` 拼到卷纲相关 prompt

也就是说，当前 `QuestionPool` 更像：

- 卷纲规划提醒
- 未解问题推进提醒
- 生成前的辅助候选层

而不是正文主 `bundle` 的结构记忆 section。

这也解释了为什么用户体感上会觉得“结构记忆有 7 块，但正文里像只看到 6 块”。

## 当前注入强度观察

虽然这 6 类已经进入正文主 bundle，但当前参与感仍偏弱，主要不是“没进”，而是“筛选偏紧 + 预算偏小”：

### `thread_ledger`

- 需要 `status != resolved`
- 需要 `audienceHeat >= 3`
- 当前最多只保留 `4` 条
- 其中默认只有前 `2` 条保留完整块，其余已压成摘要

### `foreshadow_plan`

- 只保留命中活跃伏笔或命中 `requiredForeshadowTitles` 的计划
- 当前最多 `4` 条
- 默认前 `2` 条完整，其余压成摘要

### `antagonist_agenda`

- 仅 `active`
- 需要达到当前打分阈值
- 当前最多 `2` 条

### `pov_permission`

- 当前最多 `2` 条
- 已按“章节 > 里程碑 > 卷级”收口
- 更强调精度，不强调铺量

### `world_state_delta`

- 当前卷强保留
- 前一卷残留保留
- 不再往更早卷回溯

### `resource_continuity`

- 默认仍要过打分阈值
- 非高压章节下有效命中偏保守
- 低优先级条目已开始压成摘要

## 审计结论

### 1. 结构记忆“参与正文生成”这件事是成立的

当前正文主 `bundle` 已明确吃进：

- `thread_ledger`
- `foreshadow_plan`
- `antagonist_agenda`
- `pov_permission`
- `world_state_delta`
- `resource_continuity`

所以问题不是“没接入”，而是“接入后体感偏弱”。

### 2. `QuestionPool` 当前确实不在正文主注入链路里

它当前定位仍然更偏：

- 卷纲生成
- 规划提醒
- 问题推进提示

而不是正文主 section。

### 3. 下一步增强应优先放在 4 条最直接影响正文的线

结合当前口径，`P0-I2` 第一版最适合优先放宽：

- `thread_ledger`
- `foreshadow_plan`
- `world_state_delta`
- `resource_continuity`

这也和执行计划原本的第一版覆盖范围一致。

## 对 `P0-I2` 的直接输入

`P0-I1` 完成后，`P0-I2` 可以直接承接两类动作：

1. 放宽筛选
   - 降低 `thread_ledger / resource_continuity` 的保守门槛
   - 放宽 `foreshadow_plan` 的保留上限
   - 提高 `world_state_delta` 的当前卷参与感

2. 保留压缩机制
   - 继续维持“高优先级完整块、低优先级摘要”
   - 避免放宽后 bundle 噪音回潮

# 完整接口手册

## 1. 文档定位

这份手册只覆盖当前仓库已经落地、且适合按 HTTP / client / route / service 追链联调的两大接口族：

- A. 结构记忆接口：`/api/structure-memory/*` 与维护接口
- B. 章节生成主链接口：`/api/ai/plan`、`/api/ai/write`、`/api/ai/review`、`/api/ai/language-qa`、`/api/ai/style`、`/api/ai/polish`、`/api/ai/extract`、`/api/generation/artifacts/sync`

配套文档分工如下：

- 设计边界与现状说明：[`longform-structure-systems.md`](./longform-structure-systems.md)
- 人物关系层与 `relationSnapshot` 设计：[`hashed-discovering-kay.md`](./hashed-discovering-kay.md)
- 推进计划与测试待办：[`longform-structure-systems-plan.md`](./longform-structure-systems-plan.md)
- 使用者视角入口：[`全系统使用手册.md`](./全系统使用手册.md)

本文不覆盖 `book-outline / volume-outline / volume-milestones / volume-beats / volume-plan-reconcile`、拆书分析任务和运行态 debug GET 接口。

- 这份手册是“主链接调手册”，不是“全仓库 HTTP 百科”
- 反例：
  - 调 `POST /api/ai/plan` 成功后，即时响应仍只有 `{ outline, rawText }`
  - 它不会直接带回上下文章节拆分、焦点人物列表、检索统计、关系调试预览
  - 这些属于独立 runtime debug GET 的返回，不属于本文覆盖范围，也不应写成主链接口的即时响应

## 2. 通用约定

### 2.1 服务端与 client 落点

- 结构记忆前端 client：`app/src/lib/structure-memory-client.ts`
- 章节生成前端 client：`app/src/lib/generation-client.ts`
- 结构记忆 route：`server/src/routes/structure-memory.ts`
- 章节生成 route：`server/src/routes/generation.ts`

### 2.2 通用字段

- `projectId` 是本文两大接口族的硬前提；结构记忆所有列表与删除都要求 query 里有它，所有新建 / 更新都要求 body 里有它
- `id / createdAt / updatedAt` 是结构记忆记录返回的统一骨架
- 结构记忆大量采用 `Id + name/title` 双存，例如 `relatedCharacterIds + relatedCharacterNames`、`belongsToThreadId + belongsToThreadName`
- 章节生成主链的公共上下文字段主要包括：
  - 章节定位：`projectId / chapterId / chapterTitle / chapterOrder / volumeTitle / previousChapterId / previousChapterTitle`
  - 项目与大纲：`projectTitle / projectDescription / bookOutline / volumeOutline / volumeGoal / chapterBeat / nextChapterPreview / forbiddenZone`
  - 运行上下文：`previousSummary / worldState / contextBundle / entitySnapshot / relationSnapshot / requiredEntityNames / availableCharacterNames / requiredForeshadowTitles / foreshadowSnapshot / gateConfigOverride`
  - 模型参数：`model / temperature / reasoningEffort`

### 2.3 通用错误壳子

- 结构记忆接口：
  - 缺失 `projectId` 时通常返回 `400`：`{ "message": "参数缺少 projectId" }` 或 `{ "message": "请求体缺少有效的 projectId" }`
  - 更新 / 删除目标不存在时通常返回 `404`：`{ "message": "目标XXX不存在" }`
- 章节生成主链：
  - 请求体验证失败返回 `400`：`{ "message": "请求体不符合 XXX 结构" }`
  - 服务异常返回 `500`：`{ "message": "..." }`
- 最小失败样例：

```text
POST /api/structure-memory/thread-ledgers
Body: { "name": "身世暗线" }
-> 400 { "message": "请求体缺少有效的 projectId" }
```

```text
POST /api/structure-memory/maintenance/backfill-preview
Body: { "projectId": "project_demo", "systems": "thread_ledger" }
-> 400 { "message": "请求体缺少有效的 projectId，或 systems / candidateIds 格式不正确" }
```

```text
DELETE /api/structure-memory/thread-ledgers/thread_missing?projectId=project_demo
-> 404 { "message": "目标剧情线不存在" }
```

```text
POST /api/ai/plan
Body: { "projectId": "project_demo", "model": "gpt-5.4", "temperature": 0.8 }
-> 400 { "message": "请求体不符合 AIPlanRequest 结构" }
```

- 这些样例只代表当前 route 层稳定可见的最小错误壳子，不等于所有可选字段都做了深层 schema 承诺

### 2.4 样例阅读说明

- 下文“最小样例”只保留最小必填和联调最关键字段，不等于完整字段清单
- `PUT` 默认与对应 `POST` 共用 body 骨架，只是路径多一个资源 id
- `DELETE` 默认只展示必要 query / path；如无特别说明，都返回 `{ "success": true }`

---

## 3. A. 结构记忆接口

### 3.1 总览索引

| 资源 | 方法与路径 | 主要 client | route | service |
| --- | --- | --- | --- | --- |
| `ThreadLedger` | `GET/POST /api/structure-memory/thread-ledgers`<br>`PUT/DELETE /api/structure-memory/thread-ledgers/:threadLedgerId` | `fetchThreadLedgers / createThreadLedger / updateThreadLedger / deleteThreadLedger` | `server/src/routes/structure-memory.ts` | `server/src/services/structure-memory-store.ts` |
| `ForeshadowPlan` | `GET/POST /api/structure-memory/foreshadow-plans`<br>`PUT/DELETE /api/structure-memory/foreshadow-plans/:foreshadowPlanId` | `fetchForeshadowPlans / createForeshadowPlan / updateForeshadowPlan / deleteForeshadowPlan` | 同上 | `server/src/services/structure-memory-store.ts` |
| `WorldStateEntry` | `GET/POST /api/structure-memory/world-state-entries`<br>`PUT/DELETE /api/structure-memory/world-state-entries/:worldStateEntryId` | `fetchWorldStateEntries / createWorldStateEntry / updateWorldStateEntry / deleteWorldStateEntry` | 同上 | `server/src/services/structure-memory-store.ts` |
| `QuestionPool` | `GET/POST /api/structure-memory/question-pools`<br>`PUT/DELETE /api/structure-memory/question-pools/:questionPoolId` | `fetchQuestionPools / createQuestionPool / updateQuestionPool / deleteQuestionPool` | 同上 | `server/src/services/question-pool-store.ts` |
| `AntagonistAgenda` | `GET/POST /api/structure-memory/antagonist-agendas`<br>`PUT/DELETE /api/structure-memory/antagonist-agendas/:antagonistAgendaId` | `fetchAntagonistAgendas / createAntagonistAgenda / updateAntagonistAgenda / deleteAntagonistAgenda` | 同上 | `server/src/services/antagonist-agenda-store.ts` |
| `POVPermission` | `GET/POST /api/structure-memory/pov-permissions`<br>`PUT/DELETE /api/structure-memory/pov-permissions/:povPermissionId` | `fetchPovPermissions / createPovPermission / updatePovPermission / deletePovPermission` | 同上 | `server/src/services/pov-permission-store.ts` |
| `ResourceContinuity` | `GET/POST /api/structure-memory/resource-continuities`<br>`PUT/DELETE /api/structure-memory/resource-continuities/:resourceContinuityId` | `fetchResourceContinuities / createResourceContinuity / updateResourceContinuity / deleteResourceContinuity` | 同上 | `server/src/services/structured-resource-continuity-store.ts` |
| `backfill-preview` | `POST /api/structure-memory/maintenance/backfill-preview` | `previewStructureMemoryBackfill` | 同上 | `server/src/services/structure-memory-maintenance.ts` |
| `backfill-apply` | `POST /api/structure-memory/maintenance/backfill-apply` | `applyStructureMemoryBackfill` | 同上 | `server/src/services/structure-memory-maintenance.ts` |
| `guard-alerts` | `GET /api/structure-memory/maintenance/guard-alerts` | `fetchStructureMemoryGuardAlerts` | 同上 | `server/src/services/structure-memory-maintenance.ts` |

### 3.2 `ThreadLedger`

- 用途：维护剧情线账本，并按当前章序返回“高热且长时间未推进”的提醒
- 方法与路径：
  - `GET /api/structure-memory/thread-ledgers?projectId=...&status=...&currentChapterOrder=...&staleChapterGap=...`
  - `POST /api/structure-memory/thread-ledgers`
  - `PUT /api/structure-memory/thread-ledgers/:threadLedgerId`
  - `DELETE /api/structure-memory/thread-ledgers/:threadLedgerId?projectId=...`
- 关键请求字段：
  - 查询：`projectId` 必填；`status` 可选值为 `active / dormant / resolved`；`currentChapterOrder` 传入后才会计算 `alerts`；`staleChapterGap` 默认 `15`
  - 新建 / 更新：`name / coreQuestion / currentPhase / status / audienceHeat` 最常用；`lastProgress*`、`nextTrigger`、`plannedResolveVolume`、`relatedCharacter*`、`relatedForeshadow*` 用于补充上下文
- 响应骨架：

```json
{
  "items": [
    {
      "id": "thread_1",
      "projectId": "project_demo",
      "name": "身世暗线",
      "coreQuestion": "主角真实来历是什么",
      "currentPhase": "线索分散",
      "status": "dormant",
      "audienceHeat": 4,
      "createdAt": "2026-04-16T10:00:00.000Z",
      "updatedAt": "2026-04-16T10:00:00.000Z"
    }
  ],
  "alerts": [
    {
      "threadLedgerId": "thread_1",
      "name": "身世暗线",
      "currentChapterOrder": 18,
      "overdueChapterCount": 6,
      "message": "剧情线已长时间未推进"
    }
  ]
}
```

- 错误壳子：
  - `400`：缺失 `projectId`
  - `404`：`PUT/DELETE` 目标不存在时返回 `{ "message": "目标剧情线不存在" }`
- 最小样例：

```http
GET /api/structure-memory/thread-ledgers?projectId=project_demo&currentChapterOrder=18&staleChapterGap=12
```

```json
POST /api/structure-memory/thread-ledgers
{
  "projectId": "project_demo",
  "name": "身世暗线",
  "coreQuestion": "主角真实来历是什么",
  "currentPhase": "线索分散",
  "status": "dormant",
  "audienceHeat": 4,
  "plannedResolveVolume": 3
}
```

- 调用入口 / 落点：
  - UI：`app/src/components/StructureWorkspace.tsx`、`app/src/components/ThreadLedgerPanel.tsx`
  - client：`app/src/lib/structure-memory-client.ts`
  - route：`server/src/routes/structure-memory.ts`
  - service：`server/src/services/structure-memory-store.ts`
- 不要误解：
  - `alerts` 不是无条件返回；只有传了有效 `currentChapterOrder` 才会根据 `staleChapterGap` 计算
  - 生成消费不是“只看 active”；当前真实口径是“未 `resolved` 且高热，`active` 优先但 `dormant` 也可见”

### 3.3 `ForeshadowPlan`

- 用途：为已有伏笔建立激活 / 回收计划
- 方法与路径：
  - `GET /api/structure-memory/foreshadow-plans?projectId=...&foreshadowId=...&currentVolumeOrder=...&overdueVolumeGap=...`
  - `POST /api/structure-memory/foreshadow-plans`
  - `PUT /api/structure-memory/foreshadow-plans/:foreshadowPlanId`
  - `DELETE /api/structure-memory/foreshadow-plans/:foreshadowPlanId?projectId=...`
- 关键请求字段：
  - 查询：`projectId` 必填；`foreshadowId` 可按单个伏笔过滤；`currentVolumeOrder` 传入后会产出逾期提醒；`overdueVolumeGap` 默认 `2`
  - 新建 / 更新：`foreshadowId / foreshadowTitle / importance / plannedActivateVolume / plannedResolveVolume / activationCondition / resolveCondition / payoffEffect`
- 响应骨架：

```json
{
  "items": [
    {
      "id": "plan_1",
      "projectId": "project_demo",
      "foreshadowId": "foreshadow_1",
      "foreshadowTitle": "黑玉佩",
      "importance": "major",
      "plannedActivateVolume": 2,
      "plannedResolveVolume": 4,
      "activationCondition": "主角首次进入皇城",
      "resolveCondition": "玉佩来源被揭开",
      "relatedQuestionIds": [],
      "payoffEffect": "引出皇族血脉真相"
    }
  ],
  "alerts": [
    {
      "foreshadowPlanId": "plan_1",
      "foreshadowTitle": "黑玉佩",
      "currentVolumeOrder": 5,
      "overdueVolumeCount": 1,
      "message": "伏笔规划已逾期"
    }
  ]
}
```

- 错误壳子：
  - `400`：缺失 `projectId`
  - `404`：`PUT/DELETE` 目标不存在时返回 `{ "message": "目标伏笔规划不存在" }`
- 最小样例：

```json
POST /api/structure-memory/foreshadow-plans
{
  "projectId": "project_demo",
  "foreshadowId": "foreshadow_1",
  "foreshadowTitle": "黑玉佩",
  "importance": "major",
  "plannedActivateVolume": 2,
  "plannedResolveVolume": 4,
  "activationCondition": "主角首次进入皇城",
  "resolveCondition": "玉佩来源被揭开",
  "payoffEffect": "引出皇族血脉真相"
}
```

- 调用入口 / 落点：
  - UI：`app/src/components/StructureWorkspace.tsx`、`app/src/components/ForeshadowPlanPanel.tsx`
  - client：`app/src/lib/structure-memory-client.ts`
  - route：`server/src/routes/structure-memory.ts`
  - service：`server/src/services/structure-memory-store.ts`
- 不要误解：
  - `relatedQuestionIds` 当前仍是预留字段；第一版 UI 保存时默认空数组，不能按“已和 QuestionPool 闭环”理解
  - “同一伏笔只能有一条计划”当前主要是面板侧约束，不应写成服务端硬承诺

### 3.4 `WorldStateEntry`

- 用途：记录卷级世界状态变化，支持可选里程碑粒度过滤
- 方法与路径：
  - `GET /api/structure-memory/world-state-entries?projectId=...&volumeId=...&milestoneIndex=...`
  - `POST /api/structure-memory/world-state-entries`
  - `PUT /api/structure-memory/world-state-entries/:worldStateEntryId`
  - `DELETE /api/structure-memory/world-state-entries/:worldStateEntryId?projectId=...`
- 关键请求字段：
  - 查询：`projectId` 必填；`volumeId` 可选；`milestoneIndex` 可选，允许 `0`
  - 新建 / 更新：`volumeId / volumeTitle / volumeOrder / milestoneIndex / publicEvents / secretEvents / powerBalanceChange / institutionChange / ruleChange / rumorState / knownByCharacter* / currentRisks`
- 响应骨架：

```json
{
  "items": [
    {
      "id": "world_1",
      "projectId": "project_demo",
      "volumeId": "volume_2",
      "volumeTitle": "皇城风起",
      "volumeOrder": 2,
      "milestoneIndex": 1,
      "publicEvents": ["皇城戒严"],
      "secretEvents": ["太傅暗中换防"],
      "currentRisks": ["城门盘查升级"]
    }
  ]
}
```

- 错误壳子：
  - `400`：缺失 `projectId`
  - `404`：`PUT/DELETE` 目标不存在时返回 `{ "message": "目标世界状态记录不存在" }`
- 最小样例：

```json
POST /api/structure-memory/world-state-entries
{
  "projectId": "project_demo",
  "volumeId": "volume_2",
  "volumeTitle": "皇城风起",
  "volumeOrder": 2,
  "milestoneIndex": 1,
  "publicEvents": ["皇城戒严"],
  "secretEvents": ["太傅暗中换防"],
  "currentRisks": ["城门盘查升级"]
}
```

- 调用入口 / 落点：
  - UI：`app/src/components/StructureWorkspace.tsx`、`app/src/components/WorldStatePanel.tsx`
  - client：`app/src/lib/structure-memory-client.ts`
  - route：`server/src/routes/structure-memory.ts`
  - service：`server/src/services/structure-memory-store.ts`
- 不要误解：
  - 当前正式接口只承诺“卷级记录 + 可选里程碑记录”，不承诺 chapter 级 `WorldStateEntry`
  - “同一卷 / 里程碑范围只能有一条记录”当前不是 route 层公共契约，主要由面板侧拦截

### 3.5 `QuestionPool`

- 用途：维护未解问题池，并按卷号窗口给出提醒
- 方法与路径：
  - `GET /api/structure-memory/question-pools?projectId=...&status=...&currentVolumeOrder=...`
  - `POST /api/structure-memory/question-pools`
  - `PUT /api/structure-memory/question-pools/:questionPoolId`
  - `DELETE /api/structure-memory/question-pools/:questionPoolId?projectId=...`
- 关键请求字段：
  - 查询：`projectId` 必填；`status` 可选值为 `open / partial / answered`；`currentVolumeOrder` 用于辅助提醒判断
  - 新建 / 更新：`question / belongsToThreadId / belongsToThreadName / currentClue / falseAnswers / expectedRevealWindow / finalAnswerSummary / status`
- 响应骨架：

```json
{
  "items": [
    {
      "id": "question_1",
      "projectId": "project_demo",
      "question": "黑玉佩为什么会认主",
      "belongsToThreadId": "thread_1",
      "belongsToThreadName": "身世暗线",
      "currentClue": "玉佩在血脉共鸣时发热",
      "falseAnswers": ["只是护身符"],
      "expectedRevealWindow": "第4卷",
      "status": "open"
    }
  ],
  "alerts": [
    {
      "questionPoolId": "question_1",
      "question": "黑玉佩为什么会认主",
      "expectedRevealWindow": "第4卷",
      "currentVolumeOrder": 5,
      "overdueVolumeCount": 1,
      "message": "揭晓窗口可能已过"
    }
  ]
}
```

- 错误壳子：
  - `400`：缺失 `projectId`
  - `404`：`PUT/DELETE` 目标不存在时返回 `{ "message": "目标未解问题不存在" }`
- 最小样例：

```json
POST /api/structure-memory/question-pools
{
  "projectId": "project_demo",
  "question": "黑玉佩为什么会认主",
  "belongsToThreadId": "thread_1",
  "belongsToThreadName": "身世暗线",
  "currentClue": "玉佩在血脉共鸣时发热",
  "falseAnswers": ["只是护身符"],
  "expectedRevealWindow": "第4卷",
  "status": "open"
}
```

- 调用入口 / 落点：
  - UI：`app/src/components/StructureWorkspace.tsx`、`app/src/components/QuestionPoolPanel.tsx`
  - client：`app/src/lib/structure-memory-client.ts`
  - route：`server/src/routes/structure-memory.ts`
  - service：`server/src/services/question-pool-store.ts`
- 不要误解：
  - `expectedRevealWindow` 当前是自由文本备注；自动化只稳定识别卷号，不支持“中段 / 末段 / 地点”级判断

### 3.6 `AntagonistAgenda`

- 用途：记录反派的公开身份、真实议程和当前动作
- 方法与路径：
  - `GET /api/structure-memory/antagonist-agendas?projectId=...&status=...`
  - `POST /api/structure-memory/antagonist-agendas`
  - `PUT /api/structure-memory/antagonist-agendas/:antagonistAgendaId`
  - `DELETE /api/structure-memory/antagonist-agendas/:antagonistAgendaId?projectId=...`
- 关键请求字段：
  - 查询：`projectId` 必填；`status` 可选值为 `active / dormant / defeated`
  - 新建 / 更新：`characterEntityId / characterName / publicRole / hiddenAgenda / currentObjective / currentAction / triggerToStrike / nextMoveWindow / status`
- 响应骨架：

```json
{
  "items": [
    {
      "id": "agenda_1",
      "projectId": "project_demo",
      "characterEntityId": "char_1",
      "characterName": "韩相",
      "publicRole": "当朝宰相",
      "hiddenAgenda": "逼宫改制",
      "currentObjective": "稳住城防",
      "currentAction": "调换禁军统领",
      "triggerToStrike": "皇帝病重",
      "nextMoveWindow": "第3卷末",
      "status": "active"
    }
  ]
}
```

- 错误壳子：
  - `400`：缺失 `projectId`
  - `404`：`PUT/DELETE` 目标不存在时返回 `{ "message": "目标反派议程不存在" }`
- 最小样例：

```json
POST /api/structure-memory/antagonist-agendas
{
  "projectId": "project_demo",
  "characterEntityId": "char_1",
  "characterName": "韩相",
  "publicRole": "当朝宰相",
  "hiddenAgenda": "逼宫改制",
  "currentObjective": "稳住城防",
  "currentAction": "调换禁军统领",
  "triggerToStrike": "皇帝病重",
  "nextMoveWindow": "第3卷末",
  "status": "active"
}
```

- 调用入口 / 落点：
  - UI：`app/src/components/StructureWorkspace.tsx`、`app/src/components/AntagonistAgendaPanel.tsx`
  - client：`app/src/lib/structure-memory-client.ts`
  - route：`server/src/routes/structure-memory.ts`
  - service：`server/src/services/antagonist-agenda-store.ts`
- 不要误解：
  - `WorldState` 与反派议程当前联动口径是“提醒 / 建议复核”，不是“接口会自动改写 `currentAction`”

### 3.7 `POVPermission`

- 用途：维护不同范围下的视角信息权限
- 方法与路径：
  - `GET /api/structure-memory/pov-permissions?projectId=...&volumeId=...&chapterId=...`
  - `POST /api/structure-memory/pov-permissions`
  - `PUT /api/structure-memory/pov-permissions/:povPermissionId`
  - `DELETE /api/structure-memory/pov-permissions/:povPermissionId?projectId=...`
- 关键请求字段：
  - 查询：`projectId` 必填；`volumeId` 与 `chapterId` 可选
  - 新建 / 更新：`volumeId / volumeTitle / milestoneIndex / chapterId / chapterTitle / povCharacterId / povCharacterName / readerKnows / protagonistKnows / antagonistKnows / mustHide / canHint / forbiddenReveal`
- 响应骨架：

```json
{
  "items": [
    {
      "id": "pov_1",
      "projectId": "project_demo",
      "volumeId": "volume_2",
      "volumeTitle": "皇城风起",
      "chapterId": "chapter_12",
      "chapterTitle": "夜入禁苑",
      "povCharacterId": "char_hero",
      "povCharacterName": "林烬",
      "readerKnows": ["韩相调换禁军统领"],
      "mustHide": ["韩相与太傅合谋"],
      "forbiddenReveal": ["逼宫时间表"]
    }
  ]
}
```

- 错误壳子：
  - `400`：缺失 `projectId`
  - `404`：`PUT/DELETE` 目标不存在时返回 `{ "message": "目标视角权限不存在" }`
- 最小样例：

```json
POST /api/structure-memory/pov-permissions
{
  "projectId": "project_demo",
  "volumeId": "volume_2",
  "volumeTitle": "皇城风起",
  "chapterId": "chapter_12",
  "chapterTitle": "夜入禁苑",
  "povCharacterId": "char_hero",
  "povCharacterName": "林烬",
  "readerKnows": ["韩相调换禁军统领"],
  "mustHide": ["韩相与太傅合谋"],
  "canHint": ["禁军换防节奏异常"],
  "forbiddenReveal": ["逼宫时间表"]
}
```

- 调用入口 / 落点：
  - UI：`app/src/components/StructureWorkspace.tsx`、`app/src/components/PovPermissionPanel.tsx`
  - client：`app/src/lib/structure-memory-client.ts`
  - route：`server/src/routes/structure-memory.ts`
  - service：`server/src/services/pov-permission-store.ts`

### 3.8 `ResourceContinuity`

- 用途：维护法器 / 资源 / 代价 / 恢复条件的连续性
- 方法与路径：
  - `GET /api/structure-memory/resource-continuities?projectId=...&status=...&ownerCharacterId=...&resourceType=...&riskLevel=...`
  - `POST /api/structure-memory/resource-continuities`
  - `PUT /api/structure-memory/resource-continuities/:resourceContinuityId`
  - `DELETE /api/structure-memory/resource-continuities/:resourceContinuityId?projectId=...`
- 关键请求字段：
  - 查询：`projectId` 必填；`status` 可选值为 `active / recovered / permanent`；`ownerCharacterId / resourceType / riskLevel` 可选
  - 新建 / 更新：`resourceType / ownerCharacterId / ownerCharacterName / currentState / performanceImpact / lastConsumedAt / recoveryCondition / hiddenCost / continuityRisk / status`
- 响应骨架：

```json
{
  "items": [
    {
      "id": "resource_1",
      "projectId": "project_demo",
      "resourceType": "法器",
      "ownerCharacterId": "char_hero",
      "ownerCharacterName": "林烬",
      "currentState": "黑玉佩裂纹扩大",
      "performanceImpact": "共鸣频率降低",
      "continuityRisk": "关键时刻可能失效",
      "status": "active",
      "riskLevel": "high"
    }
  ]
}
```

- 错误壳子：
  - `400`：缺失 `projectId`
  - `404`：`PUT/DELETE` 目标不存在时返回 `{ "message": "目标资源连续性记录不存在" }`
- 最小样例：

```json
POST /api/structure-memory/resource-continuities
{
  "projectId": "project_demo",
  "resourceType": "法器",
  "ownerCharacterId": "char_hero",
  "ownerCharacterName": "林烬",
  "currentState": "黑玉佩裂纹扩大",
  "performanceImpact": "共鸣频率降低",
  "recoveryCondition": "找到镇魂石补缝",
  "hiddenCost": "每次强开都消耗寿元",
  "continuityRisk": "关键时刻可能失效",
  "status": "active"
}
```

- 调用入口 / 落点：
  - UI：`app/src/components/StructureWorkspace.tsx`、`app/src/components/ResourceContinuityPanel.tsx`
  - client：`app/src/lib/structure-memory-client.ts`
  - route：`server/src/routes/structure-memory.ts`
  - service：`server/src/services/structured-resource-continuity-store.ts`
- 不要误解：
  - `riskLevel` 出现在返回与过滤条件里，但不是 mutation body 的可写字段

### 3.9 `maintenance/backfill-preview`

- 用途：预览可从运行态资产回填到结构记忆的候选
- 方法与路径：`POST /api/structure-memory/maintenance/backfill-preview`
- 关键请求字段：
  - `projectId` 必填
  - `systems` 可选，当前只支持：`thread_ledger / question_pool / world_state_entry / resource_continuity`
- 响应骨架：

```json
{
  "projectId": "project_demo",
  "generatedAt": "2026-04-16T10:00:00.000Z",
  "totalCandidates": 4,
  "newCandidates": 3,
  "existingCandidates": 1,
  "counts": [
    {
      "system": "thread_ledger",
      "total": 2,
      "newCount": 1,
      "existingCount": 1
    }
  ],
  "candidates": [
    {
      "candidateId": "candidate_1",
      "system": "thread_ledger",
      "status": "new",
      "title": "身世暗线",
      "scopeLabel": "第2卷",
      "summary": "从章节摘要中识别出的长期问题",
      "evidence": [
        {
          "sourceType": "chapter_summary",
          "sourceLabel": "第12章",
          "excerpt": "林烬再次触碰到身世谜团"
        }
      ]
    }
  ]
}
```

- 错误壳子：
  - `400`：`projectId` 缺失，或 `systems / candidateIds` 不是字符串数组
- 最小样例：

```json
POST /api/structure-memory/maintenance/backfill-preview
{
  "projectId": "project_demo",
  "systems": ["thread_ledger", "question_pool"]
}
```

- 边界样例（当前实现）：

```json
POST /api/structure-memory/maintenance/backfill-preview
{
  "projectId": "project_demo",
  "systems": ["thread_ledger"],
  "candidateIds": ["candidate_1"]
}
```

- 当前 route 会接受这类 body，但传给 service 的仍只有 `projectId + systems`
- 也就是说，这个请求现在是“兼容但不按 `candidateIds` 缩小范围”；返回的仍是当前 `thread_ledger` 范围下的全部预览候选

- 调用入口 / 落点：
  - UI：`app/src/components/StructureWorkspace.tsx`
  - client：`app/src/lib/structure-memory-client.ts`
  - route：`server/src/routes/structure-memory.ts`
  - service：`server/src/services/structure-memory-maintenance.ts`
- 不要误解：
  - preview 只真正消费 `projectId` 与 `systems`；即便 body 里带了 `candidateIds`，当前 route 也不会把它传给 service
  - 回填当前只覆盖 4 套系统，不是 7 套全覆盖

### 3.10 `maintenance/backfill-apply`

- 用途：把预览阶段挑中的候选真正写回结构记忆表
- 方法与路径：`POST /api/structure-memory/maintenance/backfill-apply`
- 关键请求字段：
  - `projectId` 必填
  - `systems` 可选
  - `candidateIds` 可选；不传时按当前筛选批量应用
- 响应骨架：

```json
{
  "projectId": "project_demo",
  "generatedAt": "2026-04-16T10:05:00.000Z",
  "requestedCandidateCount": 2,
  "createdCount": 1,
  "skippedExistingCount": 1,
  "counts": [
    {
      "system": "thread_ledger",
      "requestedCount": 2,
      "createdCount": 1,
      "skippedExistingCount": 1
    }
  ],
  "createdRecordIds": ["thread_12"]
}
```

- 错误壳子：
  - `400`：`projectId` 缺失，或 `systems / candidateIds` 不是字符串数组
- 最小样例：

```json
POST /api/structure-memory/maintenance/backfill-apply
{
  "projectId": "project_demo",
  "systems": ["thread_ledger"],
  "candidateIds": ["candidate_1", "candidate_2"]
}
```

- 当前实现分支说明：
  - 未传 `candidateIds`：

```json
POST /api/structure-memory/maintenance/backfill-apply
{
  "projectId": "project_demo",
  "systems": ["thread_ledger"]
}
```

  - 当前 service 会只应用这批 drafts 里 `status === "new"` 的候选；`existing` 不会被创建
  - 传部分 `candidateIds`：

```json
POST /api/structure-memory/maintenance/backfill-apply
{
  "projectId": "project_demo",
  "candidateIds": ["candidate_1", "candidate_2"]
}
```

  - 当前 service 只处理能在本次 drafts 中命中的 id；若命中的候选本身是 `existing`，它会计入 `skippedExistingCount`
  - 只传未知 id：

```json
POST /api/structure-memory/maintenance/backfill-apply
{
  "projectId": "project_demo",
  "candidateIds": ["candidate_missing"]
}
```

  - 当前实现不会因此返回 `404`
  - 因为未知 id 不会进入 `selectedDrafts`，所以这类请求当前通常表现为 `requestedCandidateCount: 0`、`createdRecordIds: []`
  - 以上三条都是当前 route / service 的实现分支，不应上升成长期接口保证

- 调用入口 / 落点：
  - UI：`app/src/components/StructureWorkspace.tsx`
  - client：`app/src/lib/structure-memory-client.ts`
  - route：`server/src/routes/structure-memory.ts`
  - service：`server/src/services/structure-memory-maintenance.ts`

### 3.11 `maintenance/guard-alerts`

- 用途：统一扫描结构记忆守护告警
- 方法与路径：`GET /api/structure-memory/maintenance/guard-alerts?projectId=...&trigger=...&chapterId=...&volumeTitle=...`
- 关键请求字段：
  - `projectId` 必填
  - `trigger` 可选，常见值为 `project_scan / chapter_completed / volume_completed / structure_memory_updated`
  - `chapterId / volumeTitle` 可选，用于更细的扫描上下文
- 响应骨架：

```json
{
  "projectId": "project_demo",
  "scannedAt": "2026-04-16T10:08:00.000Z",
  "trigger": "project_scan",
  "items": [
    {
      "id": "alert_1",
      "ruleKey": "thread_ledger_stale",
      "trigger": "project_scan",
      "severity": "high",
      "title": "高热剧情线长期未推进",
      "message": "请检查“身世暗线”是否需要重新推进",
      "evidence": "距离上次推进已 6 章",
      "targetSystem": "thread_ledger",
      "targetRecordId": "thread_1"
    }
  ]
}
```

- 错误壳子：
  - `400`：缺失 `projectId`
- 最小样例：

```http
GET /api/structure-memory/maintenance/guard-alerts?projectId=project_demo&trigger=project_scan
```

- 调用入口 / 落点：
  - UI：`app/src/components/StructureWorkspace.tsx`
  - client：`app/src/lib/structure-memory-client.ts`
  - route：`server/src/routes/structure-memory.ts`
  - service：`server/src/services/structure-memory-maintenance.ts`

### 3.12 结构记忆接口不要误解

- `ForeshadowPlan.relatedQuestionIds` 仍是预留字段，不代表现在已经和 `QuestionPool` 闭环联动
- `WorldStateEntry` 不承诺 chapter 级接口能力；当前正式边界仍是卷级记录 + 可选里程碑记录
- `QuestionPool.expectedRevealWindow` 只稳定识别卷号；不要把“中段 / 地点”写成接口保证
- `ResourceContinuity.riskLevel` 是返回态与过滤态字段，不是可写入 body 的业务字段
- `backfill-preview / backfill-apply` 当前只覆盖 `thread_ledger / question_pool / world_state_entry / resource_continuity`
- Dexie 镜像、`pending_push / sync_error`、本地兜底同步是前端 store 行为，不是这组 HTTP 接口的契约

---

## 4. B. 章节生成主链接口

### 4.1 总览索引

| 接口 | 用途 | client | route | service |
| --- | --- | --- | --- | --- |
| `POST /api/ai/plan` | 生成章节规划 | `createChapterPlan` | `server/src/routes/generation.ts` | `server/src/services/generation.ts` |
| `POST /api/ai/write` | 生成当前 beat 草稿 | `writeChapterBeat` | 同上 | 同上 |
| `POST /api/ai/review` | 生成审稿意见 | `reviewChapterDraft` | 同上 | 同上 |
| `POST /api/ai/language-qa` | 生成语言校对意见 | `checkChapterLanguageQa` | 同上 | 同上 |
| `POST /api/ai/style` | 按风格提示改写正文 | `styleChapterDraft` | 同上 | 同上 |
| `POST /api/ai/polish` | 根据 review / languageQa 输出润色稿 | `polishChapterDraft` | 同上 | 同上 |
| `POST /api/ai/extract` | 从成稿提取摘要、状态变化、主线类型 | `extractChapterState` | 同上 | `server/src/services/generation.ts` |
| `POST /api/generation/artifacts/sync` | 将生成产物写回项目 | `syncGenerationArtifacts` | 同上 | `server/src/services/generation-artifact-sync.ts` |

### 4.2 公共请求字段

| 字段组 | 代表字段 | 说明 |
| --- | --- | --- |
| 章节定位 | `projectId / chapterId / chapterTitle / chapterOrder / volumeTitle / previousChapterId / previousChapterTitle` | 定位当前请求对应的项目、卷、章节与上一篇 |
| 项目与大纲 | `projectTitle / projectDescription / bookOutline / volumeOutline / volumeGoal / chapterBeat / nextChapterPreview / forbiddenZone` | 来自主项目与大纲层，主要用于 plan / write |
| 运行上下文 | `previousSummary / worldState / contextBundle` | 调用方可补充，但服务端会按当前实现重建关键上下文 |
| 结构化快照 | `entitySnapshot / relationSnapshot / foreshadowSnapshot` | 由前端整理后随请求上传 |
| 约束列表 | `requiredEntityNames / availableCharacterNames / requiredForeshadowTitles` | 约束章节必须出现、可出现、必须照顾的对象 |
| 模型参数 | `model / temperature / reasoningEffort` | 所有主链接口都会消费 |
| 专属字段 | `outline / beatIndex / currentBeat / previousText / rewriteGuidance / stylePrompt / content / currentStateTable / review / languageQa` | 仅在对应子接口使用 |

### 4.3 `relationSnapshot` 专项边界

- 字段结构固定为：
  - `id / sourceEntityId / targetEntityId / sourceEntityName / targetEntityName / relationType / origin / description / currentStance / currentIntensity / stanceReason / draft`
- 前端不会原样透传 `EntityRelation`；而是先经 `app/src/lib/generation-relation-snapshot.ts` 规整：
  - `trim` 文本
  - 将 `currentIntensity` 夹到 `0..5` 并取整
  - 过滤缺少 `sourceEntityName / targetEntityName / relationType` 的关系
- 真实调用入口主要在：
  - `app/src/components/GenerationView.tsx`
  - `app/src/components/GenerationWorkspace.tsx`
  - `app/src/components/GenerationLabDialog.tsx`
- 服务端不会因为调用 `/api/ai/plan` 或 `/api/ai/write` 就把 `relationSnapshot` 持久化到运行态关系表；它只是本次请求的瞬时输入
- `plan` 会显式开启 `allowDraftContext: true`，所以规划阶段可以吃草案关系；`write / review / language-qa / style / polish` 默认不让草案关系进正式上下文
- 并不是所有传入关系都会进 prompt；当前服务端只会保留与焦点人物集合相关的显式关系块，且有显式关系块数量上限
- `/api/ai/*` 即时响应不会回显 `relationSnapshot`、上下文章节拆分、检索统计或关系 debug 结果；这些属于独立 debug 接口
- 最小边界样例：
  - 合法空输入：

```json
POST /api/ai/plan
{
  "projectId": "project_demo",
  "chapterTitle": "夜入禁苑",
  "model": "gpt-5.4",
  "temperature": 0.8
}
```

  - 也可以显式传 `relationSnapshot: []`
  - 这两种写法在当前实现里都合法；服务端只会按“空关系输入”处理，不会因此报错，也不会自动补出显式关系块
  - `draft` 关系对比：

```json
POST /api/ai/plan
{
  "projectId": "project_demo",
  "chapterTitle": "夜入禁苑",
  "model": "gpt-5.4",
  "temperature": 0.8,
  "relationSnapshot": [
    {
      "id": "rel_draft_1",
      "sourceEntityName": "林烬",
      "targetEntityName": "韩相",
      "relationType": "敌对试探",
      "origin": "显式关系",
      "description": "双方互相试探身份",
      "currentStance": "敌对",
      "currentIntensity": 4,
      "stanceReason": "韩相怀疑林烬身份",
      "draft": true
    }
  ]
}
```

```json
POST /api/ai/write
{
  "projectId": "project_demo",
  "chapterTitle": "夜入禁苑",
  "outline": {
    "goal": "潜入禁苑拿到密档",
    "beats": ["夜入禁苑"]
  },
  "beatIndex": 0,
  "currentBeat": "夜入禁苑",
  "model": "gpt-5.4",
  "temperature": 0.8,
  "relationSnapshot": [
    {
      "id": "rel_draft_1",
      "sourceEntityName": "林烬",
      "targetEntityName": "韩相",
      "relationType": "敌对试探",
      "origin": "显式关系",
      "description": "双方互相试探身份",
      "currentStance": "敌对",
      "currentIntensity": 4,
      "stanceReason": "韩相怀疑林烬身份",
      "draft": true
    }
  ]
}
```

  - 这两条请求在当前 route 层都合法
  - 当前实现里，`plan` 会放行 `draft` 关系进入上下文组装；`write` 默认会在正式上下文前把 `draft` 关系过滤掉
  - 这不等于模型输出一定出现可见差异；是否真正进入 prompt、以及是否产生明显结果，还会继续受焦点人物命中和块上限影响

### 4.4 `POST /api/ai/plan`

- 用途：基于章节定位、大纲与上下文生成 `ChapterOutlineDraft`
- 关键请求字段：
  - 必填：`projectId / chapterTitle / model / temperature`
  - 常用：`chapterId / chapterOrder / volumeTitle / bookOutline / volumeOutline / volumeGoal / chapterBeat / nextChapterPreview / forbiddenZone / previousSummary / worldState / entitySnapshot / relationSnapshot / requiredEntityNames / availableCharacterNames / requiredForeshadowTitles`
- 响应骨架：

```json
{
  "outline": {
    "goal": "潜入禁苑拿到密档",
    "obstacle": "禁军换防后盘查更严",
    "cost": "暴露血脉风险上升",
    "beats": ["入城试探", "夜入禁苑", "玉佩异动"],
    "timeAnchor": "皇城戒严当夜",
    "chapterTimeSpan": "半夜至黎明前",
    "gapFromPrevious": "紧接上一章",
    "strand": "fire",
    "hookType": "身份暴露",
    "hookStrength": "strong",
    "immutableFacts": ["黑玉佩已出现裂纹"]
  },
  "rawText": "..."
}
```

- 错误壳子：
  - `400`：`{ "message": "请求体不符合 AIPlanRequest 结构" }`
  - `500`：`{ "message": "..." }`
- 最小样例：

```json
POST /api/ai/plan
{
  "projectId": "project_demo",
  "chapterId": "chapter_12",
  "chapterTitle": "夜入禁苑",
  "chapterOrder": 12,
  "volumeTitle": "皇城风起",
  "bookOutline": "主角在皇城篇接近身世真相。",
  "volumeOutline": "第2卷核心是潜入皇城并查明玉佩来源。",
  "chapterBeat": "潜入禁苑拿密档，但不能暴露。",
  "entitySnapshot": [
    {
      "name": "林烬",
      "type": "character",
      "description": "主角",
      "fields": {},
      "tags": ["主角"],
      "pinned": true
    }
  ],
  "relationSnapshot": [
    {
      "id": "rel_1",
      "sourceEntityId": "char_hero",
      "targetEntityId": "char_prime",
      "sourceEntityName": "林烬",
      "targetEntityName": "韩相",
      "relationType": "敌对试探",
      "origin": "显式关系",
      "description": "双方互相试探身份",
      "currentStance": "敌对",
      "currentIntensity": 4,
      "stanceReason": "韩相怀疑林烬身份",
      "draft": false
    }
  ],
  "requiredEntityNames": ["林烬", "韩相"],
  "availableCharacterNames": ["沈青梧"],
  "model": "gpt-5.4",
  "temperature": 0.8,
  "reasoningEffort": "xhigh"
}
```

- 调用入口 / 落点：
  - UI：`app/src/components/GenerationView.tsx`
  - client：`app/src/lib/generation-client.ts`
  - route：`server/src/routes/generation.ts`
  - service：`server/src/services/generation.ts`
  - 上下文组装：`server/src/services/generation-context.ts`
- 不要误解：
  - `plan` 的 `relationSnapshot` 只是本次请求输入，不会被服务端持久化
  - `relationSnapshot` 可以省略或传空数组；当前差异见 4.3
  - 即时响应只有 `{ outline, rawText }`，不会直接回显 debug 结构

### 4.5 `POST /api/ai/write`

- 用途：针对某个 `beat` 生成章节正文草稿
- 关键请求字段：
  - 必填：`projectId / chapterTitle / outline / beatIndex / currentBeat / model / temperature`
  - 常用：`previousText / rewriteGuidance / stylePrompt / previousSummary / worldState / entitySnapshot / relationSnapshot`
- 响应骨架：

```json
{
  "content": "夜风掠过宫墙，林烬按住袖中的黑玉佩……",
  "rawText": "..."
}
```

- 错误壳子：
  - `400`：`{ "message": "请求体不符合 AIWriteRequest 结构" }`
  - `500`：`{ "message": "..." }`
- 最小样例：

```json
POST /api/ai/write
{
  "projectId": "project_demo",
  "chapterId": "chapter_12",
  "chapterTitle": "夜入禁苑",
  "outline": {
    "goal": "潜入禁苑拿到密档",
    "obstacle": "禁军换防后盘查更严",
    "cost": "暴露血脉风险上升",
    "beats": ["入城试探", "夜入禁苑", "玉佩异动"],
    "timeAnchor": "皇城戒严当夜",
    "chapterTimeSpan": "半夜至黎明前",
    "gapFromPrevious": "紧接上一章",
    "strand": "fire",
    "hookType": "身份暴露",
    "hookStrength": "strong",
    "immutableFacts": ["黑玉佩已出现裂纹"]
  },
  "beatIndex": 1,
  "currentBeat": "夜入禁苑",
  "previousText": "上一拍已进入内城。",
  "model": "gpt-5.4",
  "temperature": 0.8
}
```

- 调用入口 / 落点：
  - UI：`app/src/components/GenerationView.tsx`
  - client：`app/src/lib/generation-client.ts`
  - route：`server/src/routes/generation.ts`
  - service：`server/src/services/generation.ts`
- 不要误解：
  - 正式写作阶段默认不放行草案关系进入上下文
  - `currentStateTable` 即使出现在服务端类型里，也不是当前调用方必须手工给出的最终真相；服务端会按现有链路补入运行态版本

### 4.6 `POST /api/ai/review`

- 用途：对正文草稿做一致性、连续性、读者拉力审稿
- 关键请求字段：
  - 必填：`projectId / chapterId / chapterTitle / content / model / temperature`
  - 常用：`chapterOrder / volumeTitle / chapterBeat / outline / previousSummary / worldState / contextBundle / entitySnapshot / relationSnapshot`
- 响应骨架：

```json
{
  "review": {
    "summary": "整体可用，但关系体现仍偏弱。",
    "overallSeverity": "medium",
    "needsRewrite": false,
    "antiAiForceCheck": "pass",
    "checkerResults": [
      {
        "checker": "continuity",
        "score": 78,
        "summary": "设定延续基本稳定",
        "issues": []
      }
    ]
  },
  "rawText": "..."
}
```

- 错误壳子：
  - `400`：`{ "message": "请求体不符合 AIReviewRequest 结构" }`
  - `500`：`{ "message": "..." }`
- 最小样例：

```json
POST /api/ai/review
{
  "projectId": "project_demo",
  "chapterId": "chapter_12",
  "chapterTitle": "夜入禁苑",
  "content": "夜风掠过宫墙，林烬按住袖中的黑玉佩……",
  "model": "gpt-5.4",
  "temperature": 0.4
}
```

- 调用入口 / 落点：
  - UI：`app/src/components/GenerationView.tsx`
  - client：`app/src/lib/generation-client.ts`
  - route：`server/src/routes/generation.ts`
  - service：`server/src/services/generation.ts`
- 不要误解：
  - `review` 是主链接口里关系消费最重的一步，但即时响应仍只有 `{ review, rawText }`
  - 它不会把 `relationSnapshot` 原样回显给前端

### 4.7 `POST /api/ai/language-qa`

- 用途：做语言与表述层面 QA
- 关键请求字段：
  - 必填：`projectId / chapterId / chapterTitle / content / model / temperature`
  - 常用：`outline / previousSummary / worldState / entitySnapshot / relationSnapshot`
- 响应骨架：

```json
{
  "languageQa": {
    "severity": "low",
    "summary": "存在少量重复表达",
    "issues": [
      {
        "severity": "low",
        "title": "重复词",
        "description": "“低声”重复出现",
        "suggestion": "替换其中一处近义表达",
        "evidence": "第3段与第5段"
      }
    ]
  },
  "rawText": "..."
}
```

- 错误壳子：
  - `400`：`{ "message": "请求体不符合 AILanguageQaRequest 结构" }`
  - `500`：`{ "message": "..." }`
- 最小样例：

```json
POST /api/ai/language-qa
{
  "projectId": "project_demo",
  "chapterId": "chapter_12",
  "chapterTitle": "夜入禁苑",
  "content": "夜风掠过宫墙，林烬按住袖中的黑玉佩……",
  "model": "gpt-5.4",
  "temperature": 0.3
}
```

- 调用入口 / 落点：
  - UI：`app/src/components/GenerationView.tsx`
  - client：`app/src/lib/generation-client.ts`
  - route：`server/src/routes/generation.ts`
  - service：`server/src/services/generation.ts`

### 4.8 `POST /api/ai/style`

- 用途：按风格提示改写现有正文
- 关键请求字段：
  - 必填：`projectId / chapterId / chapterTitle / stylePrompt / content / model / temperature`
  - 常用：`outline / previousSummary / worldState / entitySnapshot / relationSnapshot`
- 响应骨架：

```json
{
  "content": "宫墙如墨，夜色把林烬的影子拉得更长……",
  "style": {
    "summary": "已增强悬疑感与画面密度",
    "appliedChanges": ["压缩直白说明", "加强夜色意象"]
  },
  "rawText": "..."
}
```

- 错误壳子：
  - `400`：`{ "message": "请求体不符合 AIStyleRequest 结构" }`
  - `500`：`{ "message": "..." }`
- 最小样例：

```json
POST /api/ai/style
{
  "projectId": "project_demo",
  "chapterId": "chapter_12",
  "chapterTitle": "夜入禁苑",
  "stylePrompt": "更冷峻、更有压迫感，减少解释句。",
  "content": "夜风掠过宫墙，林烬按住袖中的黑玉佩……",
  "model": "gpt-5.4",
  "temperature": 0.6
}
```

- 调用入口 / 落点：
  - UI：`app/src/components/GenerationView.tsx`
  - client：`app/src/lib/generation-client.ts`
  - route：`server/src/routes/generation.ts`
  - service：`server/src/services/generation.ts`

### 4.9 `POST /api/ai/polish`

- 用途：基于正文、审稿结果、语言 QA 结果输出润色稿
- 关键请求字段：
  - 必填：`projectId / chapterId / chapterTitle / content / model / temperature`
  - 常用：`review / languageQa / outline / previousSummary / worldState / entitySnapshot / relationSnapshot`
- 响应骨架：

```json
{
  "content": "林烬贴着宫墙潜行，袖中黑玉佩像将醒未醒的兽心……",
  "polish": {
    "summary": "已按 review 与 language QA 完成润色",
    "antiAiForceCheck": "pass",
    "appliedChanges": ["收紧叙述节奏", "替换重复表达"]
  },
  "rawText": "..."
}
```

- 错误壳子：
  - `400`：`{ "message": "请求体不符合 AIPolishRequest 结构" }`
  - `500`：`{ "message": "..." }`
- 最小样例：

```json
POST /api/ai/polish
{
  "projectId": "project_demo",
  "chapterId": "chapter_12",
  "chapterTitle": "夜入禁苑",
  "content": "夜风掠过宫墙，林烬按住袖中的黑玉佩……",
  "review": {
    "summary": "整体可用，但关系体现仍偏弱。",
    "overallSeverity": "medium",
    "needsRewrite": false,
    "antiAiForceCheck": "pass",
    "checkerResults": []
  },
  "languageQa": {
    "severity": "low",
    "summary": "存在少量重复表达",
    "issues": []
  },
  "model": "gpt-5.4",
  "temperature": 0.5
}
```

- 调用入口 / 落点：
  - UI：`app/src/components/GenerationView.tsx`
  - client：`app/src/lib/generation-client.ts`
  - route：`server/src/routes/generation.ts`
  - service：`server/src/services/generation.ts`

### 4.10 `POST /api/ai/extract`

- 用途：从成稿中提取摘要、状态变化与主线类型
- 关键请求字段：
  - 必填：`projectId / chapterId / chapterTitle / content / model / temperature`
  - 常用：`chapterOrder / chapterBeat / currentStateTable / loreSummary`
- 响应骨架：

```json
{
  "summary": {
    "summary": "林烬潜入禁苑并拿到密档，但玉佩异动暴露了风险。",
    "hook": "韩相已开始怀疑林烬身份",
    "foreshadowings": ["黑玉佩与皇族血脉有关"]
  },
  "stateChanges": [
    {
      "entityName": "黑玉佩",
      "field": "当前状态",
      "oldValue": "轻微裂纹",
      "newValue": "裂纹扩大"
    }
  ],
  "strand": "fire",
  "rawText": "..."
}
```

- 错误壳子：
  - `400`：`{ "message": "请求体不符合 AIExtractRequest 结构" }`
  - `500`：`{ "message": "..." }`
- 最小样例：

```json
POST /api/ai/extract
{
  "projectId": "project_demo",
  "chapterId": "chapter_12",
  "chapterTitle": "夜入禁苑",
  "chapterOrder": 12,
  "chapterBeat": "潜入禁苑拿密档，但不能暴露。",
  "content": "夜风掠过宫墙，林烬按住袖中的黑玉佩……",
  "model": "gpt-5.4",
  "temperature": 0.3
}
```

- 调用入口 / 落点：
  - UI：`app/src/components/GenerationView.tsx`
  - client：`app/src/lib/generation-client.ts`
  - route：`server/src/routes/generation.ts`
  - service：`server/src/services/generation.ts`
- 不要误解：
  - 这一步不吃 `relationSnapshot`

### 4.11 `POST /api/generation/artifacts/sync`

- 用途：将生成链路的最终产物写回项目
- 关键请求字段：
  - 必填：`projectId / chapterId / chapterTitle / summary / stateChanges / strand / content`
  - 常用：`chapterOrder / volumeTitle / previousChapterId / previousChapterTitle / outline / review / languageQa`
- 响应骨架：

```json
{
  "ok": true
}
```

- 错误壳子：
  - `400`：`{ "message": "请求体不符合 GenerationArtifactSyncRequest 结构" }`
  - `500`：`{ "message": "..." }`
- 最小样例：

```json
POST /api/generation/artifacts/sync
{
  "projectId": "project_demo",
  "chapterId": "chapter_12",
  "chapterTitle": "夜入禁苑",
  "chapterOrder": 12,
  "summary": {
    "summary": "林烬潜入禁苑并拿到密档，但玉佩异动暴露了风险。",
    "hook": "韩相已开始怀疑林烬身份",
    "foreshadowings": ["黑玉佩与皇族血脉有关"]
  },
  "stateChanges": [
    {
      "entityName": "黑玉佩",
      "field": "当前状态",
      "oldValue": "轻微裂纹",
      "newValue": "裂纹扩大"
    }
  ],
  "strand": "fire",
  "content": "夜风掠过宫墙，林烬按住袖中的黑玉佩……"
}
```

- 调用入口 / 落点：
  - UI：章节生成收口流程
  - client：`app/src/lib/generation-client.ts`
  - route：`server/src/routes/generation.ts`
  - service：`server/src/services/generation-artifact-sync.ts`
- 不要误解：
  - 返回体只有 `{ ok: true }`，不是全量写回详情

### 4.12 章节生成接口不要误解

- `relationSnapshot` 只在本次请求内消费，不会被 `/api/ai/plan` 或 `/api/ai/write` 自动持久化
- `plan` 允许草案关系参与上下文；`write / review / language-qa / style / polish` 默认不允许草案关系进入正式上下文
- “请求被接受”不等于“一定进入 prompt”，也不等于“一定能从即时响应直接观察到”
- `/api/ai/plan / write / review / language-qa / style / polish / extract` 的即时响应都很瘦，不会直接返回：
  - 上下文章节拆分
  - 焦点人物列表
  - 检索统计
  - 关系调试预览
- 这些调试能力属于独立 runtime debug GET 接口，不应写成主链接口响应的一部分
- 反例：
  - 即使 `POST /api/ai/plan` 里传了 `relationSnapshot`，成功响应里也不会直接看到 `sections / focusEntityNames / queryPhrases / relationshipCount`
  - 即使 `POST /api/ai/write` 接受了 `draft: true` 的关系，这也不表示它会进入正式上下文，更不表示响应会回显“草案关系已被消费”
- `contextBundle` 虽然对外是请求字段，但当前真实链路会在服务端重建；它更像“前端补充块”，不是调用方的最终权威版本
- `currentStateTable` 在 `write / review / language-qa / extract` 链路中也可能由服务端补入，不要把它写成调用方必须提前算好的最终真相
- `availableCharacterNames` 当前是前端手工维护的章节拍字段下游消费，不是系统自动候选池接口

## 5. 最小联调顺序建议

### 5.1 结构记忆

1. 先用 `ThreadLedger` 跑通 `GET -> POST -> PUT -> DELETE`
2. 再补 `guard-alerts`
3. 最后再验 `backfill-preview -> backfill-apply`

### 5.2 章节生成主链

1. 先抓 `POST /api/ai/plan`，确认 `relationSnapshot`、`availableCharacterNames`、`requiredEntityNames` 已进入请求
2. 再跑 `write -> review -> language-qa -> style -> polish`
3. 最后补 `extract -> /api/generation/artifacts/sync`

### 5.3 最容易写高的边界

- 不要把 `ForeshadowPlan.relatedQuestionIds` 当成已闭环能力
- 不要把 `WorldStateEntry` 写成 chapter 级接口
- 不要把 `QuestionPool.expectedRevealWindow` 写成支持“中段 / 地点”自动判断
- 不要把 `relationSnapshot` 写成服务端持久化关系表
- 不要把 debug GET 的返回误写成 `/api/ai/plan` 的即时响应

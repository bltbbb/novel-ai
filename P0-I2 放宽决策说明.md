# P0-I2 放宽决策说明

对应执行计划：[设定与结构记忆边界重构执行计划.md](./设定与结构记忆边界重构执行计划.md)  
上游审计依据：[P0-I1 结构记忆注入现状审计.md](./P0-I1%20结构记忆注入现状审计.md)  
边界总方案：[设定与结构记忆边界重构方案.md](./设定与结构记忆边界重构方案.md)

## 文档目的

本文用于补齐 `P0-I2` 的决策可追溯性：

- 说明这轮“放宽限制”是依据什么规划出来的
- 说明为什么只放宽到当前程度
- 说明每一类的阈值、上限、排序改动分别是什么
- 说明哪些项本轮刻意不动，留给后续阶段处理

本文不是执行计划，也不是回归记录。

## 决策输入

这轮 `P0-I2` 的规划，来自三层输入：

### 1. 方案文档的硬约束

先遵守方案文档的三条总原则：

1. `先去重，再放宽`
2. `强制项 / 候选项分层`
3. `低优先级必须压缩`

这意味着：

- 不能在 `P0` 去重未完成前直接全量放开
- 不能把所有新增条目都升成完整主块
- 不能只加条数、不做表达压缩

### 2. `P0-I1` 的现状审计结果

`P0-I1` 已确认：

- 前台结构记忆共有 `7` 块
- 正文主 `bundle` 当前只主注入 `6` 类
- `QuestionPool` 当前不在正文主 `bundle` 链路里
- 已进入正文的 6 类，问题主要不是“没接入”，而是“筛选偏紧、预算偏小、参与感偏弱”

因此 `P0-I2` 的目标不是“证明结构记忆能进正文”，而是：

- 提高有效注入量
- 保持已有去重成果
- 不让 prompt 噪音回潮

### 3. 当前实现的实际门槛

`P0-I1` 审计前，四条主线的保守点主要是：

- `thread_ledger`
  - `audienceHeat >= 3`
  - 最多 `4` 条
  - 默认仅前 `2` 条完整
- `foreshadow_plan`
  - 必须命中活跃伏笔或 `requiredForeshadowTitles`
  - 最多 `4` 条
  - 默认仅前 `2` 条完整
- `resource_continuity`
  - 非高压章节阈值偏高
  - 预算偏小
  - 默认完整块偏少
- `world_state_delta`
  - 当前卷 + 前一卷的主链已比较稳定
  - 这条线的核心问题已经不是“缺失”，而是后续是否要再扩张表达

## 总体策略

这轮采用的是：

**小步放宽，不做全放开。**

具体表现为：

1. 只动最直接影响正文参与感的 3 条线
   - `thread_ledger`
   - `foreshadow_plan`
   - `resource_continuity`
2. `world_state_delta` 本轮不继续扩张
3. 总量上调一档，但仍保留摘要层
4. 低热条目只有在“直接命中当前章”时才放宽
5. 核心规划允许补位，但不改真源边界

## 分项决策

## 1. `thread_ledger`

### 原始问题

- 低热但和当前章强相关的 active 线会被直接挡掉
- 条数和完整块预算偏小
- 用户体感容易变成“只看见最热的两条”

### 这轮改动

1. 总上限从 `4` 提到 `5`
2. 完整块上限从 `2` 提到 `3`
3. 保留门槛从固定 `audienceHeat >= 3`，改成：
   - `active`
     - 默认仍要求 `heat >= 3`
     - 但如果 `heat = 2` 且存在直接命中信号，也允许进入
   - `dormant`
     - 默认要求 `heat >= 4`
     - 但如果 `heat = 3` 且存在直接命中信号，也允许进入

### 什么叫“直接命中信号”

当前实现里，满足任一项即可视为直接命中：

- 章节标题 / outline 明确命中该线名称或核心问题
- 命中相关伏笔
- 命中两个及以上相关角色

### 为什么这样放

这是为了只放宽“当前章真相关”的低热线，而不是让所有低热线回流。

如果简单把门槛改成 `heat >= 2`：

- 会把很多背景支线一起拉进来
- 容易迅速吃掉正文上下文预算
- 体感会变成“结构记忆很多，但没有重点”

### 为什么不再放大

本轮没有把总上限直接放到 `6+`，也没有把完整块直接放到 `4+`，原因是：

- `thread_ledger` 本身信息密度很高
- 它和 `short_term_memory / retrieval_memory` 的语义重复风险也最高
- 在 `P0-I4` 的“强制项 / 候选项分层”没落之前，再继续放大会更容易引起噪音回潮

## 2. `foreshadow_plan`

### 原始问题

- 核心规划过度依赖“当前已挂 active 事实”
- 有些确实很重要的规划项，即使当前未挂 active，也会完全进不来
- 默认完整块预算偏小

### 这轮改动

1. 总上限维持 `4` 不变
2. 完整块上限从 `2` 提到 `3`
3. 过滤条件从：
   - 命中活跃伏笔
   - 命中 `requiredForeshadowTitles`
   扩到：
   - 命中活跃伏笔
   - 命中 `requiredForeshadowTitles`
   - 或 `importance === major`
4. 排序上继续保证：
   - 当前命中的活跃伏笔优先
   - 必需伏笔优先
   - 再按 `major > minor`

### 为什么这样放

`foreshadow_plan` 的真源职责本来就是“规划安排”，不是“事实状态镜像”。

所以这条线如果完全绑定在“当前是否 active”上，会出现一个问题：

- 明明是核心规划
- 但只因为事实还没激活
- 在正文上下文里彻底消失

这会削弱长线铺排感。

### 为什么总上限不继续提高

本轮没有把总上限从 `4` 再抬高，是因为：

- 规划块比剧情线块更容易和 `working_memory` 产生功能重叠
- 当前正文真正需要的是“把核心计划稳定带进来”
- 不是把更多次级计划塞进去

因此先做的是“放宽准入”和“增加一个完整块位”，而不是无差别加总量

## 3. `resource_continuity`

### 原始问题

- 非高压章节下阈值偏保守
- 中高风险但与当前章相关的资源约束容易被挡掉
- 完整块与总预算都偏小

### 这轮改动

1. 非高压章节阈值从更保守口径下调到 `45`
2. 高压章节阈值从更保守口径下调到 `30`
3. 总上限从：
   - 非高压 `4`
   - 高压 `6`
   调整为：
   - 非高压 `5`
   - 高压 `7`
4. 完整块上限从：
   - 非高压 `2`
   - 高压 `3`
   调整为：
   - 非高压 `3`
   - 高压 `4`
5. 仍保留“低优先级资源条目压成一行摘要”的机制

### 为什么这样放

资源线的问题不是“完全没进”，而是：

- 一些中高风险条目只有在特别高压的章节才容易被看见
- 日常章节里反而经常掉出 prompt

这会让“代价连续性”在正文里参与感偏弱。

所以这轮做的是：

- 适度降阈值
- 适度加预算
- 继续保留摘要压缩

### 为什么不直接全量放开

资源线如果全量放开，会很快变成：

- 角色代价
- 物资状态
- 关系消耗
- 证据链断裂

同时大量堆进正文。

这种信息虽然都重要，但并不都需要完整块表达。

所以本轮仍坚持：

- 高优先级完整块
- 后排条目压摘要

## 4. `world_state_delta`

### 为什么这轮没有继续改

这条线不是忘了，而是刻意没继续放。

原因有三点：

1. 它当前已经是“当前卷强保留”
2. 里程碑优先覆盖当前卷默认值已经成立
3. 前一卷残留也已经稳定保留

也就是说，`world_state_delta` 当前的核心问题不是“注入太弱”，而更像是：

- 后面是否需要做更细的风险分层
- 是否需要进一步压缩前一卷残留的表达

这已经更接近 `P0-I4` 的分层问题，而不是 `P0-I2` 的第一版放宽问题。

### 当前结论

`world_state_delta` 在第一版里按“现状已满足目标”处理：

- 口径确认
- 不额外加码
- 等后续分层时再看是否要进一步微调

## 为什么没有直接动 `antagonist_agenda / pov_permission / QuestionPool`

因为这三条线被明确留给了后续阶段：

- `antagonist_agenda`
- `pov_permission`
  - 进入 `P0-I3`
- `QuestionPool`
  - 先维持在卷纲 / 里程碑生成链路
  - 后续只评估轻量摘要式参与

原因不是它们不重要，而是：

- `antagonist_agenda / pov_permission` 的精度要求更高
- `QuestionPool` 当前根本不在正文主 bundle 链路里

如果现在一起动，变量会太多，不利于判断第一版放宽到底带来了什么变化

## 为什么这版是“保守放宽”

因为当前工程还没到“所有结构记忆都做完强制项 / 候选项 / 预算内追加”的阶段。

在 `P0-I4` 之前，如果直接放得过大，会有三个风险：

1. prompt 变长过快
2. 同类信息竞争加剧
3. `P0` 刚收住的去重成果被新召回量稀释

所以当前策略是：

- 先放宽一档
- 先观察体感与噪音
- 再决定第二版增强是否继续扩大

## 后续复核触发条件

后续如果出现下面任一情况，应重新回看 `P0-I2` 的阈值：

1. `P0-I5` 回归发现 bundle 噪音明显上升
2. 章节级人工抽样仍然明显觉得结构记忆参与感不足
3. `thread_ledger / foreshadow_plan / resource_continuity` 在真实样本里继续频繁掉出
4. `P0-I4` 分层完成后，发现可以把更多候选项安全塞回预算内

## 当前结论

`P0-I2` 的放宽限制，是按下面这条逻辑规划出来的：

1. 先遵守方案文档里的三条原则
2. 再用 `P0-I1` 审计结果确认“弱在哪”
3. 然后只对最直接影响正文参与感的三条线做小步放宽
4. 同时保留压缩和排序约束
5. 刻意不跨到第二版增强的范围

一句话总结：

**这轮不是“把限制拆掉”，而是“在不破坏去重与压缩前提下，给当前章真相关的结构记忆多一档进入机会”。**

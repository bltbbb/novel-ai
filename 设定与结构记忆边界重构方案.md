# 设定与结构记忆边界重构方案

## 1. 文档定位

这份文档用于收敛当前关于以下问题的讨论结果：

- `设定库` 是否继续保留
- `伏笔追踪` 是否还有必要作为一级入口存在
- `设定库 / 伏笔追踪 / 结构记忆 / generation_*` 之间如何划分职责
- 生成时如何避免多来源信息冲突
- 前台信息架构如何瘦身

它不是执行排期文档，也不是单纯的 UI 稿说明，而是一份“边界定义 + 来源优先级 + 产品结构调整”的总方案。

---

## 2. 当前问题

当前系统已经同时存在几层相似但职责不同的数据：

1. `设定库 LoreEntity`
2. `伏笔追踪 Foreshadow`
3. `结构记忆`
   - `剧情线账本 ThreadLedger`
   - `伏笔规划 ForeshadowPlan`
   - `世界状态 WorldStateEntry`
   - `资源连续性 ResourceContinuity`
   - 以及其他长篇约束台账
4. `generation_*` 运行态资产
   - `generation_entities`
   - `generation_foreshadows`
   - 章节摘要、卷总结、状态变化、记忆切片等

这些层同时存在带来了三个核心问题：

### 2.1 同一事实多处维护

例如：

- 人物当前状态可能既写在 `设定库`，也写在 `资源连续性`
- 某地点当前是否有反应，可能既写在 `设定库` 描述里，也写在 `世界状态`
- 某伏笔的事实状态可能在 `伏笔追踪`，而它的计划安排又在 `伏笔规划`

### 2.2 生成时存在多来源重复注入

当前生成上下文会同时使用：

- `设定库` 中的实体
- `结构记忆` 各 section
- `generation_*` 运行态资产
- 前台手工伏笔快照

这意味着如果多个来源对同一事实描述不一致，模型会同时看到冲突内容。

### 2.3 入口层级过重

前台现在的一级入口过多：

- 创作工作台
- 结构记忆
- 设定库
- 伏笔追踪
- 关系图谱展示

其中：

- `伏笔追踪` 和 `结构记忆 -> 伏笔规划`
- `设定库` 和 `结构记忆` 中部分动态状态能力

已经产生明显重叠。

### 2.4 `关系图谱展示` 页面和生成职责容易被误解

当前的 `关系图谱展示` 页面本身更接近：

- 示意层
- 观察层
- 诊断层

它会把：

- 章节
- 伏笔
- 设定
- 手工关系
- 运行态关系调试数据

可视化出来，但页面本身不直接参与正式生成。

真正进入生成链路的是：

- `relationSnapshot`
- `generation_relationships`
- 以及上下文组装时选出的关系 blocks

因此现在的问题是：

- 用户容易把“图谱页面”和“关系数据本身”混为一谈
- 页面职责更偏观察和诊断，而不是主维护

---

## 3. 这次重构的核心结论

### 3.1 `设定库` 不删，但必须收窄职责

`设定库` 仍然有必要保留，因为它承担的是“稳定事实底座”的角色：

- 人物是谁
- 地点是什么
- 势力结构
- 物品来历
- 事件定义
- 别名、标签、基础关系

这些不是 `结构记忆` 的长项，`结构记忆` 更像“推进台账”，不是“知识底座”。

因此：

- `设定库` 保留
- 但不再承担会随卷/阶段变化的动态状态

### 3.2 `伏笔追踪` 不建议继续作为一级主入口保留

`伏笔追踪` 当前的事实层能力本身不是没用，而是和 `伏笔规划 + generation_foreshadows` 组合后重叠过高。

因此更合理的方向是：

- `伏笔追踪` 作为能力保留
- 但一级入口降级
- 并入 `结构记忆` 作为事实页 / 辅助页

### 3.3 `generation_*` 必须明确降级为运行态缓存

`generation_*` 很重要，但它不应被视为正式真源。

它的定位应该是：

- 运行态缓存
- 调试观察层
- 回填候选来源
- 检索输入资产
- AI 自动沉淀的主要落点

不应被用户当成“正式维护台账”。

这条口径需要特别明确：

- AI 自动沉淀、自动同步、自动回填出来的内容，主要应该进入 `generation_*`
- 不应直接把 AI 结果写成正式真源

### 3.4 `关系图谱展示` 页面保留一级入口，但明确只做展示层

`关系图谱展示` 页面本身当前不直接参与正式生成，更适合作为：

- 关系观察视图
- 诊断页
- 辅助分析页

因此更合理的方向是：

- `关系图谱展示` 能力保留
- 一级入口保留
- 但页面定位明确为“展示/观察”，不再被视为正式维护台账

---

## 4. 推荐的新边界

## 4.1 设定库

### 唯一职责

只维护稳定事实。

### 应该放这里的信息

- 人物身份
- 地点定义
- 势力归属
- 物品性质
- 事件定义
- 能力体系规则
- 世界规则
- 别名、标签、基础静态描述

### 不应放这里的信息

- 当前卷里的动态态度变化
- 当前阶段的人物透支状态
- 卷级世界变化
- 资源消耗与恢复条件
- 计划在第几卷回收某伏笔

### AI 发现新设定时的处理方式

AI 发现的新人物 / 地点 / 物品 / 事件，不应直接写成正式设定。

推荐处理流程：

1. AI 先把结果写入 `generation_*` 运行态层，作为候选来源
2. `设定库` 页面内提供 `候选设定 / 待确认设定` 区域
3. 候选条目展示给用户人工检查
4. 用户再决定：
   - `确认收录`
   - `合并到现有条目`
   - `忽略`

### 这套机制的意义

- 防止 AI 自动把不稳定推断直接写成正式真源
- 让 `设定库` 继续保持“稳定事实层”的定位
- 把 AI 的价值保留在“发现候选”和“辅助补档”，而不是替代人工确认

### 页面口径

`设定库` 后续应至少分成两层：

- `正式设定`
- `候选设定 / 待确认设定`

其中：

- `正式设定` 才是生成时优先采用的稳定事实真源
- `候选设定` 只是待人工确认的候选，不直接等于正式事实

### 结论

`设定库 = 稳定事实真源`

---

## 4.2 伏笔追踪

### 唯一职责

只维护伏笔事实状态。

### 应该放这里的信息

- 伏笔标题
- 埋设来源章
- 当前状态
  - `planted`
  - `activated`
  - `resolved`
  - `overdue`
- 简短摘录 / 备注

### 不应放这里的信息

- 第几卷激活
- 第几卷回收
- 回收条件
- 回收效果
- 依赖哪些事件 / 哪些伏笔

### 结论

`伏笔追踪 = 伏笔事实真源`

---

## 4.3 伏笔规划

### 唯一职责

只维护规划与安排。

### 应该放这里的信息

- 计划第几卷激活
- 计划第几卷回收
- 激活条件
- 回收条件
- 回收效果
- 依赖伏笔
- 依赖事件
- 重要度

### 不应放这里的信息

- “这个伏笔现在已经 resolved / activated” 这种事实状态

### 结论

`伏笔规划 = 伏笔时间表与计划真源`

---

## 4.4 世界状态

### 唯一职责

维护卷级 / 里程碑级世界变化。

### 应该放这里的信息

- 势力格局变化
- 制度变化
- 规则变化
- 卷级公开事件 / 秘密事件
- 当前风险
- 舆论状态

### 不应放这里的信息

- 长期稳定设定说明
- 地点基础介绍
- 人物基础背景

### 结论

`世界状态 = 世界变化真源`

---

## 4.5 资源连续性

### 唯一职责

维护代价、伤势、限制、恢复条件。

### 应该放这里的信息

- 当前状态
- 性能影响
- 最近消耗
- 恢复条件
- 隐藏代价
- 连续性风险

### 不应放这里的信息

- 角色长期设定简介
- 世界规则本体
- 伏笔安排

### 结论

`资源连续性 = 代价与限制真源`

---

## 4.6 剧情线账本

### 唯一职责

维护叙事推进状态。

### 应该放这里的信息

- 主线 / 支线 / 暗线 / 情感线
- 当前阶段
- 核心问题
- 最近推进
- 下一触发
- 阻塞点
- 预计收束卷

### 不应放这里的信息

- 角色设定本体
- 伏笔事实状态
- 世界制度细节

### 结论

`剧情线账本 = 叙事推进真源`

---

## 4.7 generation_*

### 唯一职责

只做运行态缓存和候选来源。

### 应该做的事

- 存储章节摘要
- 存储卷总结
- 存储运行态实体 / 运行态伏笔
- 存储状态变化
- 存储关系快照
- 存储检索切片
- 生成回填候选
- 承接 AI 自动沉淀的运行态结果

### 不应做的事

- 作为正式真源给用户手工维护
- 覆盖 `设定库 / 伏笔追踪 / 结构记忆`

### 结论

`generation_* = 运行态缓存层，不是正式真源`

---

## 4.8 关系图谱展示页面

### 唯一职责

只做关系可视化、观察和诊断。

### 应该做的事

- 展示章节、伏笔、设定、关系的连接情况
- 帮用户看遗漏、冲突和联动是否合理
- 作为关系观察层辅助排查问题

### 不应做的事

- 作为正式关系真源
- 作为正式关系维护台账
- 被误认为“图谱页面本身参与生成”

### 结论

`关系图谱展示页面 = 关系观察视图，不是生成真源`

---

## 5. 来源优先级规则

为了避免冲突，需要给生成上下文增加明确优先级。

## 5.1 同类信息冲突时的优先顺序

### 人物 / 地点 / 物品的稳定事实

优先级：

1. `设定库`
2. `generation_entities`

规则：

- 正式生成时，以 `设定库` 为准
- `generation_entities` 只做补充候选，不反向改写正式设定

### 伏笔事实状态

优先级：

1. `伏笔追踪`
2. `generation_foreshadows`

规则：

- 事实状态以手工伏笔表为准
- 运行态伏笔只做补充观察和候选

### 伏笔计划

优先级：

1. `伏笔规划`

规则：

- 一律不从 `伏笔追踪` 或 `generation_foreshadows` 推断计划真源

### 世界变化

优先级：

1. `世界状态`
2. `generation_state_changes / volume recap` 只做候选

### 资源消耗 / 限制

优先级：

1. `资源连续性`
2. `generation_state_changes` 只做候选

### 剧情推进

优先级：

1. `剧情线账本`
2. `generation volume recap / chapter summary` 只做参考和回填候选

---

## 5.2 生成上下文的消费原则

后续生成时应遵守：

- 正式真源优先
- 运行态缓存只补充，不主导
- 同类信息不重复注入两份

例如：

- 如果 `资源连续性` 已存在某角色的代价记录
  - 则不再把同类 `generation_state_changes` 文本块重复作为主信息注入

- 如果 `伏笔追踪` 已经给出某伏笔的正式状态
  - 则 `generation_foreshadows` 只作为运行态观察，不再作为同级事实注入

- 如果 `世界状态` 已有人手维护卷级 delta
  - 则运行态状态变化只能做补充依据，不再和正式状态并列输出

- 如果规划链路已经明确给出 `availableCharacterNames`
  - 则这批名字只进入 `candidate_entities` 候选层
  - 不再反向参与 `focus_entities` 焦点判定
  - 已进入焦点人物的角色也不再重复进入候选区

---

## 5.3 结构记忆参与正文生成的现状与放宽方向

### 先明确一个事实

结构记忆并不是“完全没有参与正文生成”。

当前前台结构记忆系统共有 7 块：

1. `剧情线账本 ThreadLedger`
2. `伏笔规划 ForeshadowPlan`
3. `世界状态 WorldStateEntry`
4. `未解问题 QuestionPool`
5. `反派议程 AntagonistAgenda`
6. `信息权限 POVPermission`
7. `资源连续性 ResourceContinuity`

其中，当前服务端生成上下文里已经明确作为 section 注入的有 6 类：

- `剧情线提醒 / thread_ledger`
- `伏笔规划 / foreshadow_plan`
- `反派议程 / antagonist_agenda`
- `信息控制 / pov_permission`
- `世界状态 / world_state_delta`
- `资源连续性 / resource_continuity`

`QuestionPool` 目前更偏：

- 规划提醒
- 搜索与回填候选
- 统一提醒与窗口管理

还没有作为正文主上下文里的独立 section 注入。

### 为什么用户体感上会觉得“像没参与”

不是因为它真的没进，而是因为当前筛选很重：

- 条数上限偏小
- 命中条件偏严格
- 注入文本块偏长
- 同类信息还没有做足够的优先级去重与压缩

结果就是：

- 代码上参与了
- 但用户体感上不明显

### 当前改造方向

不建议把“放宽筛选”理解成简单全量放开，而应该理解成：

**提高有效注入量，而不是无脑增加注入量。**

---

## 5.4 放宽筛选的原则

### 原则一：先去重，再放宽

在提高结构记忆注入量之前，必须先完成：

- 来源优先级
- 同类信息去重
- 正式真源与运行态缓存分层

否则放得越开，冲突越多。

### 原则二：强制项与候选项分层

后续结构记忆注入建议分为两层：

#### 强制项

这些信息在命中时应优先保留：

- 当前卷相关世界状态
- 当前里程碑相关状态
- 明确命中的 `requiredEntityNames`
- 明确命中的 `requiredForeshadowTitles`
- 当前章节直接相关的高风险资源连续性
- 当前活跃主线

#### 候选项

这些信息在预算允许时追加：

- 次级剧情线
- 次级伏笔规划
- 中风险资源连续性
- 前一卷残留状态
- 次级信息权限约束
- 非主焦点反派议程

### 原则三：低优先级信息必须压缩

如果只是单纯提高条数，不压缩表达，会导致：

- prompt 变长
- 关注点稀释
- 模型注意力发散

因此应采用：

- 高优先级：完整块
- 低优先级：一行摘要

---

## 5.5 具体放宽建议

### 剧情线账本

当前问题：

- 热度阈值偏高
- 上限偏小

建议：

- `active` 线的热度阈值从较高门槛放宽到 `>= 2`
- `dormant` 线继续保持更高门槛
- 注入上限从 `2` 提高到 `3-4`
- 低优先级线只保留 `线名 + 当前阶段 + 热度`

### 伏笔规划

当前问题：

- 上限过小
- 对次级但相关的伏笔覆盖不足

建议：

- 上限从 `2` 提高到 `3-4`
- 命中 `requiredForeshadowTitles` 的条目强制保留
- 次级伏笔用压缩摘要展示

### 世界状态

当前问题：

- 当前卷与前一卷的表达还偏保守

建议：

- 当前卷变化强制保留
- 当前里程碑变化优先覆盖当前卷默认值
- 列表维度按“里程碑 winner 主导 + 卷级补并唯一项 + 去重”收口
- 前一卷残留只保留高风险、高影响项

### 资源连续性

当前问题：

- 分数阈值偏高
- 有效命中角色时召回不足

建议：

- 适当降低分数阈值
- 高压章节场景下提高条数上限
- 低优先级资源条目压缩为摘要形式

### 反派议程

当前问题：

- 当前更偏强焦点匹配，容易漏掉次级但重要的推进动作

建议：

- 焦点角色强命中时保留完整块
- 非主焦点但高相关条目允许作为候选项追加

### 信息权限

当前问题：

- 这类信息精准度要求高，不能粗暴放开

建议：

- 条数上限不宜大幅增加
- 但可在“当前卷命中 + 当前章节命中”时适度提高保留优先级

### 未解问题

当前定位：

- 目前不作为正文主上下文 section

建议方向：

- 暂不直接大量注入正文主上下文
- 更适合作为规划期、检查期、提醒期的候选层
- 后续可探索以“当前章节明确相关问题”的轻量摘要形式注入

### 候选出场人物

当前定位：

- 只作为规划链路 `availableCharacterNames` 的消费层
- 不作为正式设定真源
- 不反向决定正文焦点人物

建议方向：

- 只消费人工或规划阶段明确给出的 `availableCharacterNames`
- 已进入 `focus_entities` 的角色不再重复进入候选区
- 当前预算上限先固定为 `5` 条弱提示卡，避免挤占正文主块

---

## 5.6 最终口径

后续关于“结构记忆要不要放开注入”的统一口径应为：

- 结构记忆已经参与正文生成
- 当前问题不是“有没有参与”，而是“筛选太严、参与感偏弱”
- 后续应做的是“提高有效注入量”
- 不是简单全量放开

一句话总结：

**结构记忆参与正文生成这件事成立，下一步要优化的是召回强度、去重逻辑和表达压缩。**

---

## 6. 前台信息架构调整建议

## 6.1 建议保留的一级入口

建议前台一级入口调整为：

1. `创作工作台`
2. `结构记忆`
3. `设定库`
4. `关系图谱展示`

## 6.2 建议降级的入口

`伏笔追踪` 降级，不再作为一级主入口。

### 建议并入方式

并入 `结构记忆`：

- 可以作为 `结构记忆` 下的事实层子页
- 或者在 `伏笔规划` 页内提供“事实状态 / 规划状态”双模式切换

### 原因

- 事实伏笔和伏笔规划天然关联
- 用户在长篇里更关心“事实状态 + 计划安排”的整体
- 分成两个一级入口只会增加切换成本

`关系图谱展示` 保留一级入口，但文案和定位需要明确为展示层。

### 建议产品口径

- 名字改成 `关系图谱展示`
- 明确告诉用户：这是观察视图，不是正式维护台账
- 页面内避免继续出现会让人误以为“这里也能维护正式真源”的交互

---

## 6.3 设定库页面需要同步收窄

`设定库` 保留，但其编辑能力必须调整：

- 人格内核保留
- 静态资料保留
- 动态状态字段降级

### 建议方式

#### 人物

保留：

- 基础信息
- 人格内核
- 静态标签
- 别名

弱化 / 迁移：

- 当前阶段状态
- 当前态度
- 当前透支 / 伤势

这些动态字段后续应迁移到：

- `世界状态`
- `资源连续性`
- `剧情线账本`

---

## 7. 是否可以今天直接一刀切

可以。

因为当前没有正式项目，只有调试项目，所以现在不需要保留“老项目迁移口径”。

这意味着可以直接采用硬切策略：

- 直接按唯一真源规则改
- 直接收窄 `设定库`
- 直接把 `伏笔追踪` 降级并入 `结构记忆`
- 直接把 `generation_*` 明确收口为运行态缓存
- `关系图谱展示` 保留一级入口，但明确为展示层

此时主要承担的是工程改造风险，而不是业务迁移风险。

---

## 8. 推荐迁移顺序

当前既然只有调试项目，可以直接把下面三个阶段压缩成一次硬切实施；下面的顺序只是为了保持工程改造时的收口顺序。

## 阶段 1：先立规则

目标：

- 文档明确唯一真源
- UI 上开始提示“这里不是动态状态真源”

动作：

- 在 `设定库` 动态字段旁标记“后续迁移到结构记忆”
- 在 `伏笔追踪` 中弱化计划性文案
- 在结构记忆中突出“正式真源”地位

## 阶段 2：改生成消费顺序

目标：

- 正式真源优先
- 运行态缓存降级

动作：

- 调整生成上下文组装逻辑
- 加同类信息去重
- 加来源优先级过滤

## 阶段 3：迁移旧入口

目标：

- 把旧混用字段逐步收掉

动作：

- 收窄 `设定库` 动态字段
- `伏笔追踪` 降级并入 `结构记忆`
- 对旧项目增加冲突检测与迁移提示

---

## 9. 推荐的产品口径

后续所有页面与文档建议统一使用下面这套口径：

- `设定库`：稳定事实
- `伏笔追踪`：伏笔事实状态
- `伏笔规划`：伏笔计划安排
- `剧情线账本`：叙事推进状态
- `世界状态`：卷级 / 阶段级世界变化
- `资源连续性`：代价与限制
- `generation_*`：运行态缓存与候选
- `关系图谱展示页面`：关系观察与诊断视图

如果用户问“这件事应该写在哪”，默认先按这套口径回答。

---

## 10. 最终结论

### 结论一

`设定库` 不能删，但必须收窄为稳定事实层。

### 结论二

`伏笔追踪` 不建议继续作为一级主入口保留，应降级并入 `结构记忆`。

### 结论三

`generation_*` 必须明确降级为运行态缓存层，不得继续被视为正式真源。

### 结论四

`关系图谱展示` 页面本身不直接参与正式生成，但可以保留一级入口，前提是明确它只是展示/观察视图。

### 结论五

后续真正要解决的不是“有没有这些层”，而是：

**同一信息只能有一个正式真源。**

只要这条规则不立住，自动落库越多，冲突概率只会越高。

---

## 11. 后续建议补充文档

这份文档确认后，建议继续补两份配套文档：

1. `来源优先级与生成上下文消费规则.md`
   - 细到每一类信息如何注入、何时跳过低优先级来源

2. `设定库与伏笔系统迁移执行计划.md`
   - 拆成 UI、上下文、旧项目迁移三个阶段推进

本文先定边界和方向，不展开执行拆解。

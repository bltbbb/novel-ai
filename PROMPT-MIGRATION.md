# Prompt 移植计划：将 webnovel-writer 核心规则引入 AI Novel Studio

> 对应 `ROADMAP.md` 的 **阶段 0**
> 参考来源：`lingfengQAQ/webnovel-writer`（GitHub 开源）

---

## 一、背景

当前续写链路的 system prompt 非常简单：

```
[用户风格提示（可选）]
你正在辅助长篇小说写作。当前章节标题：XXX。
[命中的设定实体和检索结果]
```

User prompt 直接说"续写 2-3 句，80-120 字"。

webnovel-writer 沉淀了一套极其成熟的写作规则引擎，这些规则以 Prompt 模板形式存在，可以直接注入到我们的 system prompt 中，不需要改动任何数据结构、API 或 UI。

## 二、改动范围

| 类型 | 文件 | 说明 |
|------|------|------|
| 新增 | `server/src/prompts/core-constraints.ts` | 防幻觉三定律 |
| 新增 | `server/src/prompts/anti-ai-rules.ts` | Anti-AI 七层检查 + 200 高危词库 |
| 新增 | `server/src/prompts/strand-weave.ts` | 三线交织节奏规则 |
| 新增 | `server/src/prompts/cool-points.ts` | 爽点工程 |
| 新增 | `server/src/prompts/no-poison.ts` | 毒点规避 |
| 新增 | `server/src/prompts/index.ts` | 统一导出 + 组装函数 + 可配置开关 |
| 修改 | `app/src/lib/context-assembler.ts` | `buildSystemPrompt()` 注入规则 |
| 修改 | `server/src/services/openai.ts` | `buildSystemMessage()` 注入规则 |

**不改**：前端 UI、数据模型、数据库、状态管理、API 端点。

## 当前进度

- [x] 已新增前后端规则模板目录与统一组装函数
- [x] 已在前端 `context-assembler.ts` 注入规则块，Inspector 可直接预览
- [x] 已在服务端 `openai.ts` 注入同样规则作为兜底
- [x] 已补 `server/.env.example` 中的 Prompt 开关
- [x] 阶段 1 的 `plan / write / extract` 端点已开始复用同一套规则组装逻辑
- [ ] 尚未执行专门的本地联调与质量对比验证

---

## 三、5 套规则模板详细内容

### 规则 1：防幻觉三定律（core-constraints）

来源：`webnovel-writer/references/shared/core-constraints.md`

```
## 写作核心约束（三大定律）

1. **大纲即法律**：严格执行大纲和章节规划，不得擅自发挥或偏离既定情节走向。
2. **设定即物理**：人物实力、招式、物品等能力边界必须与已建立的设定一致。
   角色不能突然拥有未经描写获取过程的能力。
3. **发明需识别**：若正文中为推进剧情引入了新的角色、物品、地点或势力，
   必须给予清晰的名称和足够的描写。

## 章节硬约束

- 本章必须可读：读者能回答"发生了什么 / 谁在做什么 / 为什么"
- 本章必须有清晰推进：问题、目标、代价、关系变化、信息变化至少一项可被识别
- 若上文有明确承诺（钩子 / 未闭合问题），本章必须回应
- 禁止输出占位正文（如 [待补充]、[TODO]、...省略...）

## 章节建议

- 开头尽早进入冲突 / 风险 / 强情绪（前 200-400 字）
- 对话带意图（试探 / 回避 / 施压 / 诱导），减少纯说明句
- 避免连续大段纯解释；若必须解释，切分为"信息 + 行动 / 反应"
- 避免"回去休息了"式机械收尾；若使用平缓收尾，需保留未闭合期待
```

---

### 规则 2：Anti-AI 七层检查（anti-ai-rules）

来源：`webnovel-writer/skills/webnovel-write/references/polish-guide.md` — Phase 1 增补

#### 第 1 层：高风险词汇（200+ 词库）

```
A. 总结归纳词（22）
综合、总之、总而言之、由此可见、可以看出、不难发现、归根结底、说到底、
总体来看、从这个角度看、换句话说、简而言之、概括来说、可以说、由此得出、
结论是、最终可知、总的来说、总括起来、整体而言、总体上、综上。

B. 枚举模板词（24）
首先、其次、再次、最后、第一、第二、第三、其一、其二、其三、一方面、
另一方面、再者、此外、另外、同时、接着、然后、随后、紧接着、最后一步、
下一步、第一点、第二点。

C. 书面学术腔（24）
某种程度上、本质上、意义上、维度上、层面上、在于、体现为、构成了、
形成了、实现了、完成了、进行了、展开了、推动了、促进了、提供了、具备了、
拥有了、达成了、呈现出、表现出、反映出、蕴含着、折射出。

D. 逻辑连接滥用词（22）
因此、因而、所以、由于、然而、不过、但是、与此同时、同样地、对应地、
相应地、进一步、更进一步、从而、进而、于是、结果是、于是乎、故而、
由此、相较之下、反过来说。

E. 情绪直述词（22）
非常愤怒、非常开心、非常难过、心中五味杂陈、百感交集、情绪复杂、
内心震撼、不由得感慨、感到无奈、感到痛苦、感到欣慰、感到恐惧、
深受触动、心潮起伏、心情沉重、心情复杂、心里一暖、心里一沉、
心中一紧、不禁一愣、不由一怔、内心一震。

F. 动作套话（22）
皱起眉头、叹了口气、深吸一口气、缓缓开口、沉声说道、淡淡说道、
冷冷说道、轻声说道、嘴角上扬、嘴角抽了抽、眼神一凝、目光一闪、
身形一滞、脚步一顿、浑身一震、心头一跳、不由后退半步、猛地转身、
抬手一挥、缓缓点头、轻轻摇头、下意识后退。

G. 环境套话（22）
空气仿佛凝固、气氛骤然紧张、气压陡然下降、夜色如墨、月色如水、
寒风刺骨、四周一片寂静、死一般的寂静、时间仿佛静止、空间仿佛扭曲、
房间里弥漫着、唯一的光源、摇摇欲坠、压抑得让人喘不过气、沉默像潮水、
空气中充满了、一切都显得、世界仿佛、就在这一刻、忽然之间、刹那间、
顷刻之间。

H. 叙事填充词（22）
事实上、实际上、某种意义上、严格来说、客观而言、主观上、一般来说、
通常情况下、在这种情况下、在这个时候、在此基础上、在这个意义上、
从某种角度、对于他来说、对她而言、这意味着、这说明、这代表着、
这并不奇怪、并非偶然、不可否认、毋庸置疑。

I. 抽象空泛词（22）
命运、成长、蜕变、升华、价值、意义、抉择、坚持、信念、初心、希望、
绝望、勇气、正义、邪恶、真实、虚伪、复杂、深刻、宏大、渺小、沉重。

J. 机械开场/收尾词（24）
故事要从、让我们把视线、镜头转到、与此同时在另一边、回到现在、再说回、
这一切都要从、他并不知道、命运的齿轮开始转动、新的篇章开始了、未完待续、
故事才刚刚开始、真正的考验还在后面、一场风暴即将来临、更大的阴谋正在酝酿、
这只是开始、答案尚未揭晓、未来会怎样、谁也不知道、他深知、她明白、
可他不知道的是、可她不知道的是、然而一切才刚开始。
```

#### 第 2 层：句式规则（反模板）

```
- 禁止"首先 / 其次 / 最后"三段式说明写法
- 禁止连续三句"主语 + 谓语 + 宾语"同构句
- 禁止"他认为自己需要做三件事：一、二、三"清单化叙事
- 每 3-5 段至少出现一次句长变化（短句打断 / 插入动作 / 对话反问）
```

#### 第 3 层：形容词与副词限制

```
- 连续两个以上形容词修饰同一名词，默认重写
- 每 300 字中"很 / 非常 / 极其 / 格外 / 十分"总数不超过 4
- 优先"动作 + 结果 + 代价"，弱化"抽象评价词"
```

#### 第 4 层：四字套语 / 成语限用

```
- 禁止高频空转四字词密集堆叠（如"心潮澎湃、百感交集、五味杂陈"连用）
- 每 500 字四字套语不超过 3 个，且不得连续出现
- 若使用成语，必须承担叙事功能（推进信息 / 冲突 / 角色态度）
```

#### 第 5 层：对话去 AI 化

```
- 禁止"说明书式对话"（完整解释背景、逻辑过满）
- 对话必须有意图（试探 / 回避 / 施压 / 诱导 / 防御）
- 允许真实口语停顿、打断、反问，避免全员"标准书面语"
```

#### 第 6 层：段落结构

```
- 单句成段建议占比 25%-45%（冲突章节可提高）
- 段落长度建议 20-100 字，避免连续大段说明
- 场景切换优先用动作 / 声音 / 位置变化，不用"第一幕 / 第二幕"标签
```

#### 第 7 层：标点节奏

```
- 禁止连续使用 ...... 或 ！！！
- 省略号、感叹号每段最多 1 次（特殊情绪爆点除外）
- 长句中逗号超过 4 个时，优先拆句
```

#### Anti-AI 改写算法（表达替换规则）

```
1. 抽象情绪句 → 生理反应 + 当下意图 + 下一动作
   例：他非常愤怒 → 他指节捏得发白，喉咙里压着一口气。再等一秒，他就会动手。

2. 结论句 → 事实细节 + 代价/风险 + 决策
   例：首先，他要拿到证据；其次，稳住队友；最后，公开反击。
     → 他先把证据攥在手里，转身去找队友。人得先稳住，反击才能一刀见血。

3. 连续说明句(>=3) → 改成"对白 / 动作 / 反问"混排

4. 连续同构句(>=3) → 至少打断 1 句为短句或插入动作锚点

5. 对话整段解释背景 → 改为"带意图的对抗式对白"
```

---

### 规则 3：Strand Weave 三线交织节奏（strand-weave）

来源：`webnovel-writer/references/shared/strand-weave-pattern.md`

```
## 三条线定义与占比

| 线条 | 占比 | 定义 | 典型剧情 |
|------|------|------|----------|
| Quest（主线） | 55-65% | 核心任务、升级、战斗、夺宝 | 宗门大比、秘境、突破境界 |
| Fire（感情线） | 20-30% | 情感关系发展 | 相识暧昧、英雄救美、确认关系 |
| Constellation（世界观线） | 10-20% | 扩展设定、展示新势力/地点 | 揭示隐藏势力、主角身世 |

## 交织硬规则

| 规则 | 警告条件 | 建议操作 |
|------|----------|----------|
| Quest 不连续超过 5 章 | chapters_since_switch >= 5 | 切换到 Fire 或 Constellation |
| Fire 不超过 10 章不出现 | current - last_fire > 10 | 安排感情戏 |
| Constellation 不超过 15 章不出现 | current - last_constellation > 15 | 展示新设定/势力 |

## 前 30 章织网模板

第 1-5 章:   Quest ×5（开局快速推进主线）
第 6 章:     Fire（首次与女主相遇）
第 7-10 章:  Quest ×4（宗门大比）
第 11 章:    Fire（英雄救美）
第 12-14 章: Quest ×3（秘境夺宝）
第 15 章:    Constellation（揭示"圣地"存在）
第 16-19 章: Quest ×4（击败强敌）
第 20 章:    Fire（确认关系）
第 21-24 章: Quest ×4（突破境界）
第 25 章:    Constellation（发现主角身世线索）
第 26-30 章: Quest ×5（卷末高潮）
```

---

### 规则 4：爽点工程（cool-points）

来源：`webnovel-writer/references/shared/cool-points-guide.md`

```
## 六种爽点执行模式

1. 装逼打脸：对方轻视 → 主角展示实力 → 对方震惊/后悔
2. 扮猪吃虎：表面弱小 → 关键时刻爆发 → 众人惊艳
3. 越级反杀：实力差距明显 → 主角逆袭 → 敌人不可置信
4. 打脸权威：权威质疑 → 主角用实力证明 → 权威认可
5. 反派翻车：反派得意 → 计划破产 → 反派狼狈
6. 甜蜜超预期：平淡日常 → 意外惊喜 → 情感升温

## 爽点三段式结构（30/40/30 参考框架）

- 铺垫(30%): 建立预期、制造反差、设置信息差
- 兑现(40%): 触发时机、展现方式、情绪高峰
- 微反转(30%): 假结束、还有一手、余韵

## 爽点密度建议（滚动窗口）

| 周期 | 要求 |
|------|------|
| 逐章 | 优先保证有爽点或同等兑现，允许过渡章低密度 |
| 每 5 章 | 建议 ≥1 个组合爽点（2 种模式叠加） |
| 每 10-15 章 | 建议 ≥1 个里程碑爽点（改变主角地位） |

## 爽点强度分级

| 级别 | 说明 | 频率 |
|------|------|------|
| 小爽点 | 单一模式，日常打脸 | 常见（多数章节） |
| 组合爽点 | 2+ 模式叠加，重要转折 | 中频（阶段节点） |
| 里程碑爽点 | 改变主角地位的阶段性胜利 | 低频（大节点） |

## 打脸四步法（核心套路）

Step 1 铺垫: 提前 1-2 章建立信息差（读者知道主角底牌，反派不知道）
Step 2 挑衅: 通过 1-3 次挑衅建立对照组（反派被追捧 vs 主角被贬低）
Step 3 拉扯: 2-3 轮交锋，主角示弱 → 反派得意 → 期待拉满
Step 4 爆发: 物理碾压 + 精神打击 + 围观群众反应 + 实质收获

## 节奏脉冲

慢铺垫（压） → 快爆发（扬） → 停顿 → 再加速
- 短句：快节奏，动作密集
- 长句：慢节奏，细节描写
- 交替：制造节奏变化

## 压扬比例

| 题材类型 | 压扬比例 | 说明 |
|---------|---------|------|
| 传统爽文 | 压3扬7 | 轻度压迫，快速释放 |
| 硬核正剧 | 压5扬5 | 平衡叙事 |
| 虐恋/黑深残 | 压7扬3 | 长期压抑，爆发更爽 |
```

---

### 规则 5：毒点规避（no-poison）

来源：`webnovel-writer/skills/webnovel-write/references/polish-guide.md` — No-Poison 章节

```
## 毒点规避（五类禁止事项）

1. 降智推进：角色忽略常识仅为推进剧情服务
   → 必须给出合理动机

2. 强行误会：可一句话说清却长期拖延
   → 必须有阻碍说清的合理阻力

3. 圣母无代价：无边界原谅高风险对象
   → 原谅必须有条件或代价

4. 工具人配角：只在功能节点出现，没有独立动机
   → 配角需有自身目标

5. 双标裁决：同类行为评价标准不一致且无叙事解释
   → 需有立场/信息差等合理解释

命中任一毒点时，补"动机 / 阻力 / 代价"中的至少两项。
```

---

## 四、代码改动详情

### 4.1 新增 `server/src/prompts/index.ts`

```typescript
export interface PromptConfig {
  enableCoreConstraints?: boolean;  // 默认 true
  enableAntiAI?: boolean;           // 默认 true
  enableStrandWeave?: boolean;      // 默认 true
  enableCoolPoints?: boolean;       // 默认 false（需要大纲支持时开启）
  enableNoPoison?: boolean;         // 默认 true
}

export function buildWritingRulesPrompt(config?: Partial<PromptConfig>): string;
```

每套规则独立文件导出字符串常量，`index.ts` 提供 `buildWritingRulesPrompt()` 按配置组装。

### 4.2 修改 `app/src/lib/context-assembler.ts`

`buildSystemPrompt()` 改造：

```
[用户风格提示（可选）]
你正在辅助长篇小说写作。当前章节标题：XXX。
─── 以下为写作规则约束 ───
[防幻觉三定律]
[Anti-AI 规则]
[Strand Weave 节奏]
[毒点规避]
─── 以下为上下文参考 ───
[命中的设定实体和检索结果]
```

新增可选字段 `writingRules?: string` 到 `ContextAssemblerInput`。

### 4.3 修改 `server/src/services/openai.ts`

`buildSystemMessage()` 改造：在 systemPrompt 和 references 之间注入规则，确保即使前端未传规则，服务端也有默认规则兜底。

### 4.4 续写提示词预留扩展

保留当前"短续写"（80-120 字）作为默认模式，在 `createContinueWritingPrompt()` 中预留 `mode` 参数：
- `'short'`（默认）：当前行为，2-3 句
- `'chapter'`（后续）：按 beat 生成 800-1000 字

---

## 五、验证方式

1. 启动 dev server，点击 AI 续写
2. 检查 ContextInspector 的 System Prompt 预览，确认规则已注入
3. 观察 AI 输出是否减少了典型 AI 味道
4. 执行 `npm run test:e2e:ai` 确保不回归

## 六、风险与注意

| 风险 | 影响 | 缓解 |
|------|------|------|
| Token 开销增加 ~1500-2000 | 可能超出小模型上下文窗口 | PromptConfig 可关闭部分规则 |
| 规则过多导致 AI 输出僵硬 | 创作自由度下降 | 规则分 Hard/Soft/Style 三层 |
| 部分规则依赖大纲数据 | 当前无大纲结构 | CoolPoints 默认关闭，待阶段 1 |

## 七、后续衔接

本计划完成后，进入 `ROADMAP.md` 阶段 1（最小生成引擎），届时将：
- 引入 ChapterOutline / ChapterSummary / StrandTracker 数据结构
- 新增 `/api/ai/plan`、`/api/ai/write`、`/api/ai/extract` 端点
- 开启 CoolPoints 规则
- 改造上下文组装为分层策略

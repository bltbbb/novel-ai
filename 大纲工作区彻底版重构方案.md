# 大纲工作区彻底版重构方案

## 1. 文档定位

这份文档用于定义 `创作工作台 -> 大纲` 的彻底版重构方案。

它不是纯视觉稿，也不是实现进度表，而是一个面向后续开发落地的交互与信息架构蓝图。

目标是解决当前大纲页在长篇场景，尤其是 `100-500 章` 场景下的核心问题：

- 页面纵向堆叠过长
- 高优先级工作区被头部大卡片挤压
- 重复按钮过多，系统级操作和当前上下文操作混在一起
- `全书大纲 / 结构记忆概览 / 卷大纲 / 里程碑 / 章节拍` 被串成一条长页面
- 章节拍默认按整卷长列表展开，无法支撑快速跳到第 `500` 章
- 页面整体一起滚动，缺少独立工作区滚动边界

---

## 2. 当前问题归纳

### 2.1 壳层占用首屏过高

当前进入项目后，`Command Deck` 与 `Writer Room` 都采用大面积头部卡片形式：

- `Command Deck` 高度过大
- `Writer Room` 又重复展示一次项目摘要、统计和按钮
- 两块区域叠加后，几乎吃掉首屏 `2/3`
- 真正高频的工作区，例如大纲编辑、卷切换、章节拍列表，被明显压缩

这会带来一个直接后果：

- 用户一进来先看到“说明和摘要”
- 但真正要操作的内容只能在首屏以下
- 对日常高频创作来说，进入成本过高

### 2.2 系统级按钮重复出现

当前存在明显的重复入口：

- `项目设置` 在多个区域重复出现
- `AI 设置` 在多个区域重复出现
- `结构记忆`、`伏笔追踪` 已经在左侧主导航中存在，但 `Writer Room` 里又重复放了一次
- `兼容控制台` 的位置和层级还不够统一

问题不只是“按钮多”，而是：

- 系统级入口和当前页面操作没有分层
- 页面会给人一种“到处都能点，但不知道真正该从哪进”的感觉

### 2.3 大纲页仍然是长表单思维

当前大纲页仍然是典型的单主滚动结构：

1. 全书大纲
2. 结构记忆概览
3. 卷大纲
4. 里程碑
5. 章节拍

这套结构在中短篇下还能使用，但在长篇下问题会迅速放大：

- 纵向距离太长
- 上下文切换成本高
- 用户每次定位一个对象时都要穿过大量无关区域

### 2.4 章节拍不适合长篇

当前章节拍的核心问题是：

- 默认按整卷渲染完整 `beatRows`
- 里程碑当前更接近“裂变辅助选择”，还不是一个真正的内容过滤器
- 单章编辑采用大卡片展开式结构

这会导致：

- 章节越多，列表越长
- 到第 `300` 章、第 `500` 章时几乎不可操作
- 用户不是在“导航和编辑”，而是在“翻页式找位置”

### 2.5 滚动边界错误

当前主要问题不是“某个区块会滚”，而是“整页一起滚”。

长篇工作区最需要的是：

- 导航区独立滚动
- 主内容区独立滚动
- 检查器独立滚动

如果所有内容都放在一个纵向滚动容器里，就一定会出现：

- 上下文丢失
- 切换对象后定位困难
- 顶部摘要重复占据注意力

---

## 3. 重构目标

本次彻底版重构，目标不是“让页面更漂亮”，而是把大纲页从长表单页面升级成真正的长篇工作区。

### 3.1 一级目标

- 首屏直接进入可操作内容
- 让 `500 章` 级别的导航和编辑依然可用
- 明确系统级入口与上下文操作的分层
- 让用户一次只处理一个主要任务，而不是面对整页混合内容
- 把滚动边界拆开，避免整页联动滚动

### 3.2 二级目标

- 缩短高频路径
- 减少重复按钮
- 减少视觉噪音
- 保留未来继续扩展 `结构记忆 / 伏笔 / 图谱 / 生成` 联动的空间

### 3.3 非目标

本方案当前不直接处理以下内容：

- 大纲字段本身的业务扩容
- 结构记忆的数据结构调整
- 生成链路的 prompt 策略变更
- 移动端完整重设计

本方案优先解决的是：

- 桌面端壳层
- 大纲页信息架构
- 长篇导航与滚动模型

---

## 4. 总体设计结论

最终采用以下方向：

- 删除 `Writer Room`
- 把 `Command Deck` 压缩为固定顶部瘦指挥条
- 应用壳层改为 `左主导航 + 顶部指挥条 + 主工作区`
- 大纲页改为 `左导航栏 + 中主编辑区 + 右检查器`
- 页面外层不再整体滚动，三个工作区独立滚动
- 章节拍从“大卡片纵向堆叠”改成“列表 + 右侧详情检查器”

一句话概括：

**从“页面式写大纲”改成“工作台式管理大纲”。**

---

## 5. 应用壳层重构

## 5.1 新壳层结构

应用主壳层统一为：

```text
┌────────左主导航────────┬──────────────────── 顶部指挥条 ────────────────────┐
│ 创作工作台              │ 项目名 / 当前视图 / 当前卷 / 当前阶段 / 保存状态     │
│ 结构记忆                │ AI状态 / 字数 / 章节数 / 设置 / 兼容控制台 / 返回列表 │
│ 设定库                  ├─────────────────────────────────────────────────────┤
│ 伏笔追踪                │ 左栏：卷树/跳章   │ 中栏：主编辑区 │ 右栏：检查器     │
│ 关系图谱                │ 独立滚动          │ 独立滚动      │ 独立滚动        │
└────────────────────────┴─────────────────────────────────────────────────────┘
```

### 设计原则

- 左侧主导航是全局入口，不重复出现在页面内部
- 顶部指挥条是当前项目和当前上下文的总控栏
- 主工作区只承载当前任务，不承载重复说明

---

## 5.2 左侧主导航

左侧主导航继续保留以下 5 个入口：

1. `创作工作台`
2. `结构记忆`
3. `设定库`
4. `伏笔追踪`
5. `关系图谱`

### 规则

- 主导航宽度建议 `272-288px`
- 左侧只放一级系统导航，不放二级按钮
- 当前激活项高亮
- 左侧区域独立滚动

### 明确约束

既然 `结构记忆`、`伏笔追踪` 已经在这里：

- `Writer Room` 中不再重复出现
- `大纲` 页中不再用大按钮重复放同样的入口

---

## 5.3 顶部瘦指挥条

`Command Deck` 取消大卡片结构，改为固定顶部细条。

### 高度

- 建议高度 `80-88px`
- 严禁再次长成接近卡片区块的视觉高度

### 左侧内容

左侧只保留上下文核心信息：

- 项目名
- 当前主视图
- 当前卷
- 当前阶段
- 保存状态

### 右侧内容

右侧只保留系统级控制：

- AI 状态
- 当前字数
- 章节总数
- `设置`
- `兼容控制台`
- `返回项目列表`

### 不再保留的内容

- 大段项目简介
- 二次项目摘要卡
- 大面积统计卡阵列
- 页面内部重复出现的系统级操作按钮

---

## 5.4 设置入口收口

现有重复设置入口全部收口为一个统一入口：

- `项目设置`
- `AI 设置`

统一合并成一个顶栏按钮：`设置`

### 打开方式

点击后进入设置面板，内部再分 Tab：

1. `项目`
2. `AI`
3. `模板`

### 这样做的收益

- 系统级入口集中
- 顶栏更干净
- 页面内部不再出现多个设置按钮

---

## 5.5 兼容控制台位置

`兼容控制台` 只保留在顶部指挥条。

### 原则

- 它属于“系统级辅助入口”
- 不属于当前卷编辑或章节拍编辑
- 不应该和正文工作流按钮混在一起

### 结果

- `Writer Room` 中不再保留 `兼容控制台`
- 大纲页内部也不再重复出现

---

## 6. 大纲页新信息架构

大纲页从长页面重构为三栏工作区：

```text
┌──────── 左栏：导航与过滤 ────────┬──────── 中栏：主编辑区 ────────┬──────── 右栏：检查器 ────────┐
│ 卷列表                           │ 模式切换                       │ 当前对象详情                 │
│ 里程碑树                         │ 当前模式内容                   │ 补充编辑                     │
│ 章节跳转                         │                                 │ AI辅助操作                   │
│ 搜索 / 状态筛选                  │                                 │ 结构记忆摘要                 │
└──────────────────────────────────┴──────────────────────────────────┴────────────────────────────────┘
```

### 宽度建议

- 左栏：`280-320px`
- 中栏：主弹性区
- 右栏：`360-420px`

### 核心原则

- 左栏负责“找对象”
- 中栏负责“看主内容”
- 右栏负责“改细节”

---

## 6.1 一级模式切换

中栏顶部只保留四个一级模式：

1. `全书总纲`
2. `当前卷纲`
3. `里程碑`
4. `章节拍`

### 原因

当前问题不是“内容不够”，而是“内容全堆在一起”。

改成一级模式切换后：

- 一次只处理一个工作主题
- 切换成本低
- 不再需要从全书滚到章节拍

### 默认打开策略

- 从卷目录进入时，默认落在 `当前卷纲`
- 从章节或章节拍入口进入时，默认落在 `章节拍`
- 从阶段入口进入时，默认落在 `里程碑`

---

## 6.2 全书总纲模式

`全书总纲` 变成一个低频但完整的独立模式，不再永远挂在最上面。

### 放什么

- 核心前提
- 核心冲突
- 主题内核
- 主角弧线
- 世界规则
- 副线规划
- 角色弧线
- 结局方向

### 不再承担什么

- 不再作为所有卷编辑前必须先穿过的一层
- 不再和卷大纲、章节拍串在同一滚动路径里

---

## 6.3 当前卷纲模式

这是长篇阶段最常用的主编辑模式之一。

### 中栏展示

- 卷目标
- 核心冲突
- 弧线概述
- 卷初状态 / 卷末状态
- 明面对手
- 暗线
- 关键事件
- 必需实体
- 必需伏笔

### 右栏展示

- 当前卷摘要
- 上一卷承接提示
- 当前卷关联结构记忆摘要
- AI 操作：
  - `AI 生成卷纲`
  - `AI 补全里程碑`
  - `修正规划`

### 设计原则

- 卷纲是中频主任务，应占据中栏主位
- AI 操作不和整页按钮混在一起，而是绑定当前卷上下文

---

## 6.4 里程碑模式

里程碑不再只是一个夹在卷纲和章节拍之间的大块区域，而是一个独立工作模式。

### 左栏

- 当前卷下的阶段树
- 每个阶段显示：
  - 标题
  - 章节范围
  - 规划状态
  - 已裂变 / 未裂变状态

### 中栏

- 只展示当前选中的里程碑内容
- 当前阶段目标、冲突、进入/退出状态、关键转折等

### 右栏

- AI 裂变当前阶段
- 该阶段关联章节数
- 阶段关联结构记忆摘要
- 阶段注意事项

### 收益

- 里程碑真正成为“内容导航器”
- 不再只是卷内某个次级卡片区块

---

## 6.5 章节拍模式

章节拍是本次重构的核心重点。

它不再使用“整卷大卡片纵向展开”的结构，而改成：

- 中栏：紧凑列表
- 右栏：详情检查器

### 中栏章节拍列表，每行显示

- 章号
- 标题提示
- 所属阶段
- 规划状态
- 焦点角色
- 主线推进一句话
- 更新时间 / 保存状态

### 右栏章节拍检查器，显示完整编辑字段

- 标题提示
- 场景功能
- 焦点角色
- 必须出场
- 可出场候选
- 主线推进
- 支线推进
- 节奏
- 新意要求
- 钩子
- 力量变化
- 关键物件
- 禁用短语
- 禁用场景模板

### 交互规则

- 点击列表行后，右栏切换到该章详情
- 中栏不再展开大表单卡
- 支持上下章快速切换
- 支持保留当前选中位置

---

## 7. 500 章场景专门设计

本方案必须显式支持 `500 章` 级别的使用场景。

## 7.1 章节跳转

左栏必须提供：

- `跳到章节` 输入框
- 支持直接输入 `500`
- 回车后直接定位到第 `500` 章

### 扩展能力

后续可支持：

- 输入范围 `481-500`
- 输入阶段名模糊匹配
- 输入章节标题关键词搜索

---

## 7.2 阶段过滤必须变成真过滤

当前里程碑不能只作为裂变模式选择器，必须升级为真正的过滤器。

### 规则

- 选择某里程碑后，中栏章节拍列表只显示该阶段章节
- 顶部状态条同步显示：
  - 阶段名
  - 章节范围
  - 已规划数
  - 未规划数

### 回退入口

- 提供 `查看整卷`
- 一键退出阶段过滤

---

## 7.3 状态筛选

左栏增加状态筛选：

- `全部`
- `未规划`
- `已规划`
- `待补全`
- `已保存`

### 使用意义

在几百章场景下，用户常见任务不是“看所有章”，而是：

- 找还没写的
- 找需要修的
- 找当前阶段的
- 找刚刚生成的

所以筛选必须成为一级能力。

---

## 7.4 列表虚拟化预留

即使第一版不立即做虚拟滚动，结构也必须为此预留。

### 原因

- 500 章列表如果每行都包含复杂子内容，会产生明显性能问题
- 当前方案改成紧凑列表后，天然更适合后续接入虚拟列表

### 实施要求

- 章节拍中栏列表与右栏详情彻底解耦
- 中栏只渲染列表项，不嵌套复杂表单
- 右栏承担重字段编辑

---

## 8. 结构记忆与辅助信息降级

`结构记忆概览` 不再在大纲页中占据一整块主区。

### 调整原则

- 结构记忆仍然重要
- 但在大纲页里，它是“辅助信息”，不是主任务

### 新位置

放入右栏，作为摘要区块出现：

- 当前卷相关结构记忆摘要
- 当前阶段相关结构记忆摘要
- 跳转到 `结构记忆` 主页面的入口

### 明确约束

- 不再以大块独立 section 挂在大纲主滚动路径里
- 不再和卷纲、章节拍抢中栏空间

---

## 9. 滚动模型

本方案的一个硬性规则是：

**页面外层不整体滚动。**

## 9.1 滚动边界

只允许以下区域独立滚动：

- 左侧主导航
- 大纲页左栏
- 大纲页中栏
- 大纲页右栏
- 必要的弹窗内部滚动

## 9.2 禁止行为

禁止以下情况继续出现：

- 整个大纲页作为一个超长滚动页
- 用户滚章节拍时，把卷纲和顶栏一起拖走
- 用户定位第 `500` 章时，需要从上往下翻完整页面

## 9.3 状态保持

切换对象时，尽量保留以下状态：

- 左栏卷树展开状态
- 左栏章节跳转和筛选条件
- 中栏当前滚动位置
- 右栏当前对象

---

## 10. 空间预算

重构后的首屏必须满足以下目标：

### 10.1 顶部空间

- 顶部指挥条高度控制在 `80-88px`
- 首屏不能再出现第二块“大头部卡片”

### 10.2 有效工作区占比

- 首屏进入大纲页后，至少 `70%` 以上高度用于有效工作区
- 用户进入后无需先滚才能开始工作

### 10.3 章节拍首屏信息密度

在桌面端章节拍模式下，首屏应能看到：

- 至少 `10-16` 条章节摘要行

而不是：

- 只能看到 `1-2` 张大表单卡

---

## 11. 桌面端布局建议

## 11.1 视口宽度

桌面端建议不再把项目壳层卡死在 `1600px`。

建议策略：

- 常规桌面：`1760px` 左右
- 大屏：允许接近 `1920px`
- 或者采用流式宽度 + 最大内边距控制

### 原因

当前系统本质上已经是工作台型产品：

- 导航多
- 数据多
- 长文本多
- 三栏结构更依赖横向空间

如果宽度过窄，就会不断通过纵向堆叠补偿，最终再次挤压工作区。

---

## 11.2 推荐宽度分配

桌面端推荐：

- 左主导航：`272-288px`
- 大纲页左栏：`280-320px`
- 大纲页右栏：`360-420px`
- 中栏占据剩余全部空间

### 中栏最低要求

中栏必须保证：

- 在正常桌面宽度下仍能完整显示章节拍列表
- 不需要依赖横向滚动才能完成主编辑任务

---

## 12. 响应式退化原则

本方案以桌面端为优先，但仍需给出后续移动端退化规则。

### 平板

- 左主导航可收缩
- 大纲页三栏可退化成 `左栏抽屉 + 中栏 + 右栏抽屉`

### 手机

- 不保留三栏同时可见
- 改成“一级模式切换 + 抽屉式导航 + 全屏详情”

### 但当前阶段不做什么

- 不以移动端布局反向牵制桌面端信息架构
- 当前优先先把桌面端长篇工作区做对

---

## 13. 用户主路径

## 13.1 进入大纲页

1. 用户从左主导航进入 `创作工作台`
2. 默认进入当前项目最近一次使用的子模式
3. 若最近一次在大纲页，则恢复到：
   - 当前卷
   - 当前阶段
   - 当前章节
   - 当前滚动位置

## 13.2 写卷纲

1. 左栏选卷
2. 中栏进入 `当前卷纲`
3. 右栏给出 AI 操作与结构记忆摘要
4. 保存后保持当前卷不跳走

## 13.3 写阶段

1. 左栏选里程碑
2. 中栏进入 `里程碑`
3. 右栏显示阶段详情与裂变入口

## 13.4 找第 500 章

1. 左栏输入 `500`
2. 列表直接定位到第 `500` 章
3. 中栏高亮该章
4. 右栏打开该章详情

## 13.5 修当前阶段未补齐的章节拍

1. 左栏选当前阶段
2. 左栏再选状态筛选 `待补全`
3. 中栏只显示该阶段待补全章节
4. 右栏逐章补齐

---

## 14. 按钮分层规则

后续所有实现，按钮必须遵守下面的分层规则。

## 14.1 顶栏按钮

只允许放系统级入口：

- `设置`
- `兼容控制台`
- `返回项目列表`

## 14.2 左侧主导航按钮

只允许放一级系统导航：

- `创作工作台`
- `结构记忆`
- `设定库`
- `伏笔追踪`
- `关系图谱`

## 14.3 中栏按钮

只允许放当前模式主操作：

- 保存卷纲
- AI 生成卷纲
- AI 补全里程碑
- AI 裂变当前阶段

## 14.4 右栏按钮

只允许放当前对象相关操作：

- 保存当前章节拍
- 上一章 / 下一章
- 应用建议
- 跳转关联对象

### 明确禁止

页面内部禁止继续出现：

- 重复的 `项目设置`
- 重复的 `AI 设置`
- 已存在于主导航中的 `结构记忆`
- 已存在于主导航中的 `伏笔追踪`

---

## 15. 视觉层面的简化要求

虽然本方案主要讲交互，但视觉上也必须遵守简化原则。

### 原则

- 头部减少大面积信息墙
- 卡片数量减少
- 统计信息改成短状态块
- 文案只保留当前任务所需信息
- 同层级元素使用统一密度

### 不再采用的模式

- 连续两块大型摘要卡
- 统计卡阵列 + 操作按钮阵列叠加
- 同一个动作在多个板块重复出现

---

## 16. 验收口径

彻底版重构完成后，应满足以下验收标准。

### 16.1 壳层验收

- `Writer Room` 被彻底移除
- `Command Deck` 被压缩为顶部瘦指挥条
- `项目设置` 与 `AI 设置` 收口为一个 `设置`
- `兼容控制台` 只保留一个入口

### 16.2 大纲页验收

- 不再是单长页面
- 改为三栏工作区
- `全书总纲 / 当前卷纲 / 里程碑 / 章节拍` 通过一级切换进入
- `结构记忆概览` 不再单独占据主区块

### 16.3 长篇可用性验收

- 能直接跳到第 `500` 章
- 能按里程碑过滤章节拍
- 能按状态过滤章节拍
- 查看和编辑章节拍时不需要整页滚动

### 16.4 首屏空间验收

- 进入大纲页后，首屏应直接看到可编辑主内容
- 顶部说明区不再占据接近 `1/3` 高度

---

## 17. 建议实施顺序

为了降低重构风险，建议按以下顺序推进：

1. 先重做项目壳层，删除 `Writer Room`，压缩 `Command Deck`
2. 再实现大纲页三栏结构与独立滚动边界
3. 再把章节拍从大卡片模式改成“列表 + 右检查器”
4. 最后接入跳章、筛选、阶段过滤与状态恢复

### 原因

- 先做壳层，可以立刻回收首屏空间
- 再做三栏，能先把结构搭稳
- 章节拍是最大改动，放在结构稳定后改更安全

---

## 18. 后续补充文档建议

本方案确认后，后续可继续拆出两份配套文档：

1. `大纲工作区详细线框说明`
   - 细到每一栏的控件和状态

2. `大纲工作区实施拆解计划`
   - 拆成前端壳层、导航、章节拍、状态保持四个阶段执行

这两份文档不在本文内展开，等确认进入实现期后再补。

cC # 生成节奏优化方案

## 1. 背景

当前项目已能稳定生成长篇章节，但从本次 `demo15.txt` 的 15 章结果看，生成流水线存在明显的“过稳、过慢、过细拆”倾向。问题不只在单条 prompt，也不只在小说大纲，而是分布在以下几层：

- 卷级目标已经在前端准备好，但正文生成阶段没有真正吃到。
- 章节规划天然偏保守，导致每章只肯往前挪半步。
- 审查层重点盯设定连续性，对“节奏拖沓、重复复述、章感弱”打得不够狠。
- 摘要与上下文回写偏“阶段性推进”，不够强调“本章到底发生了什么硬事件”。
- 前端没有显式的节奏档位，用户只能通过文风 prompt 间接影响节奏。

这份文档的目标是把“节奏慢、重复多、章感弱、兑现不足”拆成可落地的代码改造项，并给出建议顺序与验收标准。

## 2. 当前问题总结

基于本次 `demo15.txt` 的观察，核心问题集中在以下几类：

- 章感弱：更像连续续写素材，而不是 15 个各自有推进与收束的章节。
- 推进慢：同一个信息点会被分成“试探 -> 确认 -> 再确认 -> 再收束”多轮表达。
- 重复高：有限资源、守夜、藏镜、门闩、窗缝、分工等信息反复出现。
- 悬念多、兑现少：新名词不断追加，但真正改变局势的事件偏少。
- 外压来得晚：外部冲突进入太慢，前半段大多在内屋、祠堂、井边打转。
- 说明化偏重：修炼与规矩部分容易变成“步骤解释”，戏剧性下降。
- 连续性偶有失真：前文刚定下试修顺序，后文又发生角色位次跳转。

## 3. 根因判断

结论不是“单纯大纲写坏了”，而是当前系统默认策略在系统性放慢节奏。

### 3.1 正文生成没有真正使用卷级大纲

前端在写作请求里已经传入了 `volumeOutline`：

- `app/src/components/GenerationLabDialog.tsx`
- `app/src/components/GenerationView.tsx`

但服务端正文 prompt 的核心入口 `server/src/services/generation.ts` 中，`buildWritePrompt` 只注入了：

- `bookOutline`
- `volumeGoal`
- `chapterBeat`
- `nextChapterPreview`
- `forbiddenZone`

没有注入 `volumeOutline` 本身。

这会造成一个直接后果：

- 模型知道“当前卷目标”一句话。
- 模型知道“当前章节拍”这一小块。
- 但模型不知道“本卷这一阶段从哪里进，到哪里出，中间必须完成哪些关键事件和里程碑”。

结果就是正文更容易盯着当前小 beat 慢慢磨，而不是朝卷内阶段目标压过去。

### 3.2 章节规划阶段过度保守

`server/src/services/generation.ts` 中的规划链路本身就在主动限速：

- `buildPlanPrompt` 要求 `beats` 为 `3 到 4 条`
- `buildWritePrompt` 要求“本次只推进当前 beat，不要一次写完后续 beats”
- 卷级章节拍裂变还多次强调“不要提前透支后半卷大事件”

这套设计适合防止失控，但副作用是：

- 单章推进被切得过碎
- 模型更倾向把一个动作拆成多个 beat
- 早期章节容易出现“观察、试探、确认、再次确认、收束”串联

### 3.3 审查层没有把“节奏慢”和“章感弱”当成强问题

当前 review 层更强调：

- 设定冲突
- 连续性错误
- 资源与关键物件状态
- 局部读者追读风险

但缺少硬性判退规则，例如：

- 本章没有局势位移
- 本章没有新的风险升级
- 本章只有试探没有兑现
- 同一类场景功能连续两章复用
- 同一层判断或情绪被两段以上重复表达

结果就是：内容虽然“没明显错”，但依然可能“很慢”。

### 3.4 摘要、回写和卷内记忆过于温和

当前回写与上下文聚合更偏“阶段性推进”表达，而不是“本章硬事件表达”。

例如：

- `server/src/services/generation-context.ts` 的卷内高亮更偏“近期推进摘要”
- `server/src/services/generation-volume-recap-store.ts` 的默认摘要也偏温和
- `server/src/services/generation.ts` 中的 `buildExtractPrompt` 还没有强制输出“本章硬事件 / 局势位移 / 风险升级”

这会影响后续章节：

- 下一章拿到的是模糊推进摘要
- 而不是“上一章已经发生了什么不可逆事件”
- 于是模型更容易继续绕着同一问题打转

### 3.5 自动去重目前偏词汇与模板，没打到“结构重复”

`app/src/lib/generation-repetition.ts` 已经能检测：

- 高频词
- 比喻触发词
- 常见动作模板
- 部分场景模板
- 场景功能与焦点角色重复

但还缺少更高层的重复识别，例如：

- 连续两章都是“家内议事 + 夜间试探 + 父辈收束”
- 连续多段都只在重申同一个判断
- 本章和前章的真正硬事件类型没有变化

也就是说，当前系统能发现“写法像”，但不太能发现“剧情功能还在原地打转”。

### 3.6 大纲结构可能也偏软，但不是唯一主因

如果卷纲写法偏：

- 逐步接触
- 慢慢试探
- 建立联系
- 谨慎推进
- 暂不暴露

模型一定会更慢。

但即便大纲不够硬，当前系统也放大了这个问题，因为：

- 正文没吃到完整卷纲
- 章节 beat 拆得太细
- review 没把拖沓打回
- 回写没有强化硬事件

所以大纲要改，但不应把锅全甩给大纲。

## 4. 代码改造清单

这一部分按层拆开，尽量落到函数与文件。

### 4.1 P0：正文生成层

#### 4.1.1 在 `buildWritePrompt` 中真正注入 `volumeOutline`

目标文件：

- `server/src/services/generation.ts`

目标函数：

- `buildWritePrompt`

建议改动：

- 在 `projectDescription` 之后、`volumeGoal` 之前插入 `【当前卷大纲】`
- 不只注入整段卷纲，还要显式强调：
  - 当前卷阶段里程碑
  - 本章处于卷内哪个阶段
  - 本章必须兑现的卷内事件
  - 本章禁止继续拖延的事项

建议新增规则：

- 本章必须让卷内状态往前移动，而不是只做氛围确认。
- 如果卷纲或里程碑里出现明确关键事件，本章必须回应“推进了 / 推迟了 / 失败了”，不能无声略过。
- 如果已经连续两章都在铺垫，本章必须出现一次显性兑现或外压升级。

#### 4.1.2 给正文生成增加“有效变化 / 硬推进节奏”约束

目标文件：

- `server/src/services/generation.ts`

目标函数：

- `buildWritePrompt`

建议新增规则：

- 每章至少要有一次“有效变化”，禁止整章原地复述。
- 有效变化分为两类：
  - 软推进：关系位置变化、决策明确、代价落地、资源更紧、风险被看见、下章行动条件准备完成。
  - 硬推进：事件爆发、信息揭晓、冲突升级、外压进门、目标达成或失败、被迫转场或改计划。
- 过渡章允许没有硬推进，但必须完成至少一项软推进。
- 连续两章内至少一章要出现硬推进。
- 连续三章内不能有两章以上都只是铺垫与确认。
- 禁止整章只做“观察、试探、确认、收束”。
- 若本章被判定为过渡章，结尾必须把读者明确推向下一章行动，而不是把同一问题再问一遍。

#### 4.1.3 给正文生成增加“章收束感”要求

目标文件：

- `server/src/services/generation.ts`

目标函数：

- `buildWritePrompt`

建议新增规则：

- 本章结尾必须形成明确的章级落点：
  - 完成一个动作
  - 确认一个结果
  - 触发一个新麻烦
  - 付出一个具体代价
- 禁止以“同一问题再问一遍”作为默认结尾。

### 4.2 P0：章节规划层

#### 4.2.1 缩减 `beats` 数量

目标文件：

- `server/src/services/generation.ts`

目标函数：

- `buildPlanPrompt`

当前问题：

- 默认 `3 到 4` 个 beat 对慢热题材会进一步放大拖沓。

建议改动：

- 开篇与前中期默认改为 `2 到 3` 个 beat
- 只有明确动作大章、逃亡章、复杂交锋章才允许 `4` 个 beat

建议新增规则：

- 禁止把同一种观察、试探、确认拆成多个 beat。
- 若两个 beat 的主要功能接近，必须合并。

#### 4.2.2 给 `beats` 强制加入“章级事件位移”

目标文件：

- `server/src/services/generation.ts`

目标函数：

- `buildPlanPrompt`

建议新增规则：

- `beats` 中至少一条必须对应“硬事件”，不能全是铺垫型 beat。
- 如果本章定位为过渡章，也必须明确“过渡到了哪里”，不能只有情绪过渡。

#### 4.2.3 放宽“不要透支后半卷大事件”的保守程度

目标文件：

- `server/src/services/generation.ts`

目标位置：

- 卷章节拍裂变 prompt 相关段落

建议改动：

- 保留“不要提前写终局翻转”
- 但改成“允许提前兑现小事件、小转折、小回收，只禁止提前写终局揭晓或卷尾底牌”

建议新增规则：

- 每 2 到 3 章至少要有一个可被读者感知的阶段性兑现。
- 不允许所有节点都往卷后半段堆积。

### 4.3 P0：卷级章节拍裂变层

目标文件：

- `server/src/services/generation.ts`

目标位置：

- 卷章节拍表生成相关逻辑

建议改动：

- 让 `scenePurpose` 必须更具体，禁止“继续试探 / 深入了解 / 稳步推进”这类虚目标。
- 让 `mainPlot` 必须可验证，例如：
  - 第 X 章必须完成第一次开口
  - 第 X 章必须完成第一次试修
  - 第 X 章必须暴露给外人
  - 第 X 章必须被迫转移藏点

建议新增字段或规则：

- `mustHappenNow`
- `cannotDelayAfterThisChapter`
- `chapterEventType`

如果不想改数据结构，也可以先通过 `scenePurpose` 或 `noveltyRequirement` 承载。

### 4.4 P1：大纲与序列化层

#### 4.4.1 把卷纲从“描述性大纲”改成“截止章数大纲”

目标文件：

- `app/src/components/OutlineView.tsx`
- `app/src/lib/outline-serializer.ts`

建议改法：

- 当前卷纲字段保留
- 但阶段里程碑的表达要更硬

从：

- 逐步接触镜子
- 慢慢建立信任
- 谨慎试探入门

改为：

- 第 3 章前，镜子第一次完整开口
- 第 5 章前，第一次试修成功
- 第 7 章前，外人第一次起疑
- 第 9 章前，被迫调整藏镜点

建议序列化强化：

- `serializeVolumeOutline` 输出时，优先突出：
  - 阶段目标
  - 阶段冲突
  - 关键转折
  - 必埋伏笔
  - 必回收
  - 目标章数

必要时可追加专门的“阶段截止要求”文本块。

#### 4.4.2 提醒用户不要用软性动词写里程碑

目标文件：

- `app/src/components/OutlineView.tsx`

建议：

- 在里程碑输入区加提示：
  - 少写“逐步、慢慢、进一步、继续”
  - 多写“必须发生什么”

### 4.5 P1：审查层

目标文件：

- `server/src/services/generation.ts`
- `app/src/lib/generation-repetition.ts`

#### 4.5.1 在 review prompt 中新增节奏与章感打回项

目标函数：

- `buildReviewPrompt`

建议新增检查维度：

- 本章是否存在有效变化
- 若属于过渡章，是否完成了有效软推进
- 本章是否与前章功能重复
- 是否出现重复复述同一判断
- 是否悬念新增多于兑现
- 本章结尾是否真正形成章钩子，而不是简单复问

建议新增打回条件：

- 没有局势变化
- 没有新增风险
- 没有新增信息
- 同类场景模板连续两章复用
- 连续两段表达同一层意思

#### 4.5.2 扩展本地重复检测

目标文件：

- `app/src/lib/generation-repetition.ts`

当前已覆盖：

- 高频词
- 动作模板
- 场景模板
- 场景功能
- 焦点角色重复

建议新增：

- 章节事件类型重复检测
- “家内议事 / 夜间试探 / 父辈收束”这类结构模板检测
- “本章只有确认、没有位移”的空转检测

可新增的模板示例：

- 家内收束段
- 夜间呼吸试修段
- 发现异物后封口段
- 父辈总结分工段
- 同一问题的再提问段

### 4.6 P1：提取、回写与上下文层

目标文件：

- `server/src/services/generation.ts`
- `server/src/services/generation-context.ts`
- `server/src/services/generation-volume-recap-store.ts`

#### 4.6.1 强化提取结果中的“本章硬事件”

目标函数：

- `buildExtractPrompt`

建议新增提取字段：

- `chapterEvent`
- `situationShift`
- `newPressure`
- `paidCost`
- `payoffOrPlant`

如果暂时不改类型，也至少要在摘要模板中强制输出：

- 本章发生的不可逆事件
- 本章新增的外压
- 本章真正付出的代价

#### 4.6.2 卷内高亮不要只写“近期推进”

目标文件：

- `server/src/services/generation-context.ts`

当前问题：

- 卷内高亮偏“近期推进摘要”

建议改成：

- 近期硬事件
- 当前未解外压
- 已兑现节点
- 下个必须兑现的节点

让后续章节拿到的不是模糊综述，而是卷内倒计时。

#### 4.6.3 卷 recap 默认文案去温和化

目标文件：

- `server/src/services/generation-volume-recap-store.ts`

建议：

- 少用“本卷已完成阶段性推进”
- 多用“本卷最近一次硬事件是……；当前最大外压是……；下一步必须处理的是……”

### 4.7 P2：前端设置与运行时策略层

目标文件：

- `app/src/components/SettingsDialog.tsx`
- `app/src/stores/settings-store.ts`
- `app/src/lib/runtime-config.ts`
- `app/src/types/domain.ts`

#### 4.7.1 新增“节奏档位”

建议新增字段：

- `generationPacingPreset`

建议档位：

- `开篇快推`
- `标准连载`
- `慢热铺垫`

每个档位建议影响：

- 默认 beat 数量
- 是否要求至少发生有效变化
- review 的节奏判退阈值
- 是否允许长段规则解释或修炼说明

#### 4.7.2 档位持久化

需要同步改动：

- `app/src/types/domain.ts` 中的 `AppSettings`
- `app/src/lib/runtime-config.ts` 中的 `DEFAULT_SETTINGS`
- `app/src/stores/settings-store.ts`
- `app/src/components/SettingsDialog.tsx`

### 4.8 P2：上下游类型层

目标文件：

- `app/src/types/ai.ts`
- `server/src/types/ai.ts`

建议：

- 如果后续要把“本章硬事件”“当前卷阶段位置”“节奏档位”等内容显式透传，就要补字段定义。
- 当前 `AIWriteRequest` 虽已有 `volumeOutline`，但若要加更明确的节奏控制项，仍需扩类型。

## 5. 推荐实施顺序

建议不要一口气全改，按收益与风险排序。

### 第一阶段：先压住“慢”和“重复”

优先改：

1. `server/src/services/generation.ts` 的 `buildWritePrompt`
2. `server/src/services/generation.ts` 的 `buildPlanPrompt`
3. `server/src/services/generation.ts` 中卷章节拍裂变 prompt
4. `server/src/services/generation.ts` 的 `buildReviewPrompt`

第一阶段目标：

- 同样大纲下，单章推进更硬
- 章感更清楚
- 连续 3 到 5 章内必须出现卷内实质位移

### 第二阶段：再补摘要回写与检测

优先改：

1. `server/src/services/generation.ts` 的 `buildExtractPrompt`
2. `server/src/services/generation-context.ts`
3. `app/src/lib/generation-repetition.ts`
4. `server/src/services/generation-volume-recap-store.ts`

第二阶段目标：

- 后续章节不再围绕模糊摘要空转
- 重复场景与空转章更早被发现

### 第三阶段：再补 UI 与设置

优先改：

1. `app/src/types/domain.ts`
2. `app/src/lib/runtime-config.ts`
3. `app/src/stores/settings-store.ts`
4. `app/src/components/SettingsDialog.tsx`

第三阶段目标：

- 用户能直接选节奏档位
- 不再需要靠文风 prompt 侧面影响节奏

### 第四阶段：最后回头修大纲生产方式

优先改：

1. `app/src/components/OutlineView.tsx`
2. `app/src/lib/outline-serializer.ts`

第四阶段目标：

- 卷纲从“软描述”改成“硬截止节点”
- 章节裂变有更清晰的阶段压迫

## 6. 最小改动闭环

如果当前只想做一轮最小改动，建议只改以下 5 项：

1. 在 `buildWritePrompt` 注入 `volumeOutline`
2. 在 `buildWritePrompt` 增加“每章至少一次有效变化，连续两章内至少一章硬推进”规则
3. 在 `buildPlanPrompt` 把 `beats` 改成 `2 到 3`，并禁止把同类试探拆多 beat
4. 在卷章节拍裂变 prompt 里放宽“小事件可提前兑现”
5. 在 `buildReviewPrompt` 新增“章感弱 / 无有效变化 / 重复复述”打回条件

只做这 5 项，通常就能先把“15 章像写了 5 章内容”的问题压下去。

## 7. 验收标准

改完后，建议按以下标准验证：

- 同样输入题材下，前 15 章至少出现 4 到 6 次明确硬事件。
- 任意一章都能一句话说清“这章真正发生了什么”。
- 连续两章不应再出现同类场景模板高重复。
- 卷内关键事件不应全部堆在后半段。
- 单章结尾不再大量使用“再问一遍同一问题”的伪钩子。
- review 层应能稳定识别：
  - 空转章
  - 复述章
  - 只铺垫不兑现的章节

## 8. 风险与注意事项

- 不能只为了快而放弃设定与资源连续性，否则会从“慢”变成“崩”。
- 不建议删掉所有保守约束，而应把“终局不能提前透支”和“小事件必须尽早兑现”分开处理。
- 不建议只改前端文风 prompt。若不改服务端正文 prompt 与章节规划，节奏问题仍会反复出现。
- 不建议先大修大纲再看效果。当前系统级限速问题不解决，大纲即便改硬，也会被生成链路再次磨慢。

## 9. 后续建议

文档落地后，推荐按以下顺序开工：

1. 先改 `server/src/services/generation.ts`
2. 用同一项目再跑一组 8 到 15 章样本
3. 观察是否仍出现“家内议事 + 夜间试探 + 父辈收束”循环
4. 再决定是否补 UI 节奏档位和卷纲结构改造

如果后续要继续推进，本文件可直接作为本轮“生成节奏专项优化”的工作清单使用。

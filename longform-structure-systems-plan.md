# 长篇结构记忆系统执行计划

对应设计文档：[longform-structure-systems.md](./longform-structure-systems.md)

## 文档用途

这份文档不是设计说明，而是执行拆解、排期跟踪和实时进度记录入口。

- 设计定义看 `longform-structure-systems.md`
- 执行拆解、状态维护、阻塞记录统一写在本文档
- 默认以“服务端权威态优先，前端编辑态后补”为实施原则
- 当前执行入口以“范围边界 + 总览看板 + 各任务进度字段”为准
- 各节“实时记录”保留历史推进轨迹，不直接等于当前待办顺序；若与顶部现状口径冲突，以顶部和各节进度字段为准

## 状态口径

| 字段 | 说明 |
|------|------|
| 状态 | `未开始 / 进行中 / 已完成 / 阻塞 / 暂缓` |
| 负责人 | 当前实际推进人，可留空 |
| 开始时间 | 首次进入执行的日期 |
| 完成时间 | 达到当前章节声明的验收口径后的日期；若仅完成代码收口，需在验收口径里明确运行态验证是否后置 |
| 最后更新 | 最近一次更新进度的日期 |
| 阻塞项 | 当前最直接的阻塞信息，没有则写 `无` |
| 下一动作 | 下一步要做的最小闭环动作 |
| 备注 | 记录实现口径、风险、偏差和临时决定 |

- `已完成` 默认表示当前章节声明的“代码收口验收”已达成；若仍需样章、老项目或真实运行观察，必须在备注、验收口径或预算观察清单中显式写出“运行态待验证/继续观察”

## 范围边界

当前执行入口纳入范围：

- `P0-1 Outline Schema Upgrade`
- `S0` 结构记忆底座
- `P0-2 ThreadLedger`
- `P0-3 ForeshadowPlan`
- `P0-4 WorldStateEntry`
- `P1 AntagonistAgenda / QuestionPool / POVPermission / ResourceContinuity(core)`
- `P2 ResourceContinuity(expanded)`
- `D1-D4` 原后置项的第一版已交付现状
- 结构记忆 / 单章记忆层预算观察与运行态验证

原计划后置、现已转为当前现状的能力：

- 前端 Dexie 镜像全量落地
- `StructureWorkspace` 大工作台
- 历史章节回填预览 / 确认写入流程
- 复杂跨系统自动联动守护告警

当前仍按“持续观察 / 后续增强”跟踪，不作为本文档里的未完成阻塞：

- `P2-1`、`D3`、`D4` 的样章实测、预算噪声、告警阈值与误报率观察
- `WorldStateEntry` 的 chapter 级正式记录层
- 预算观测从文档口径升级为可执行统计 / 报表

原后置项的实现与后续维护入口见下文“后置开发计划（现状与增强）”。

## 总览看板

| 任务ID | 任务 | 优先级 | 状态 | 当前进度 | 负责人 | 最后更新 | 依赖 | 阻塞项 |
|------|------|------|------|------|------|------|------|------|
| `P0-1` | Outline Schema Upgrade | P0 | 已完成 | 第一版字段、UI、序列化、prompt 已落地 |  | 2026-04-15 | 无 | 无 |
| `S0` | 结构记忆底座 | 基础 | 已完成 | 核心底座已稳定，统一 route / client 已被后续系统复用；通用查询形态作为非阻塞补项滚动完善 |  | 2026-04-16 | `P0-1` | 无 |
| `P0-2` | ThreadLedger | P0 | 已完成 | 已打通服务端 CRUD、最小 UI 与正文注入；当前消费未 `resolved` 且高热的剧情线，`active` 优先但 `dormant` 可见 |  | 2026-04-16 | `S0` | 无 |
| `P0-3` | ForeshadowPlan | P0 | 已完成 | 已补服务端 CRUD、前端 store/UI、卷纲注入、正文上下文注入 |  | 2026-04-15 | `S0`, `P0-2` 部分字段可并行 | 无 |
| `P0-4` | WorldStateEntry | P0 | 已完成 | 已补服务端 CRUD、前端 store/UI、delta 注入与前一卷叠加消费 |  | 2026-04-15 | `S0` | 无 |
| `P1-1` | QuestionPool | P1 | 已完成 | 已补问题池、卷纲注入与窗口告警 |  | 2026-04-15 | `P0-2`, `P0-3` | 无 |
| `P1-2` | AntagonistAgenda | P1 | 已完成 | 已补议程板与正文反派上下文消费；世界状态联动当前以提醒 / 建议复核为主 |  | 2026-04-16 | `S0`, `P0-4` | 无 |
| `P1-3` | POVPermission | P1 | 已完成 | 已补权限表与正文信息控制块 |  | 2026-04-15 | `S0` | 无 |
| `P1-4` | ResourceContinuity(core) | P1 | 已完成 | 已补结构化核心代价约束并接入上下文与 review |  | 2026-04-15 | `S0` | 无 |
| `P2-1` | ResourceContinuity(expanded) | P2 | 已完成 | 已补扩展类型预设、筛选、动态注入与审核补强；当前按代码收口完成，运行态继续观察 |  | 2026-04-16 | `P1-4` | 无 |
| `D1` | Dexie 镜像 | 后置 | 已完成 | 原后置项，现已落地镜像表、草稿回写与最小同步提示 |  | 2026-04-15 | `S0 + P0 + P1 + P2` | 无 |
| `D2` | StructureWorkspace | 后置 | 已完成 | 原后置项，现已提供统一工作台、搜索、提醒区和跳转入口 |  | 2026-04-15 | `D1` | 无 |
| `D3` | 历史回填 | 后置 | 已完成 | 代码收口已完成预览 / 确认写入闭环；老项目运行态验证继续观察 |  | 2026-04-16 | `D1` 或服务端结构记忆表稳定 | 无 |
| `D4` | 联动守护 | 后置 | 已完成 | 代码收口已完成守护告警链路；当前保持提示为主，噪声与阈值继续观察 |  | 2026-04-16 | `D1`，`D2` 最佳 | 无 |

2026-04-16 起，`D1-D4` 不再只是“原后置计划”，而是当前系统现状的一部分；历史推进轨迹保留在下文对应章节。

## 建议实施顺序（历史主线回看）

以下顺序用于回看主线推进逻辑，不等于当前待办顺序；当前执行入口以总览看板和各节进度字段为准。

1. 先完成 `P0-1`，把长篇骨架字段扩容到可正式消费的口径。
2. 再完成 `S0`，把服务端权威态、统一路由、前端请求封装打通。
3. 按 `ThreadLedger -> ForeshadowPlan -> WorldStateEntry` 的顺序做 3 个 P0 垂直切片。
4. `P1` 建议实际顺序调整为：`QuestionPool -> AntagonistAgenda -> POVPermission -> ResourceContinuity(core)`。
5. `P2` 等 `P1-4` 稳定后再扩。

## 建议提交粒度

建议按“一个闭环一提交”推进，避免一次改太多难回看：

1. `P0-1` Outline Schema Upgrade
2. `S0` 底座
3. `P0-2` ThreadLedger
4. `P0-3` ForeshadowPlan
5. `P0-4` WorldStateEntry
6. `P1-1` QuestionPool
7. `P1-2` AntagonistAgenda
8. `P1-3` POVPermission
9. `P1-4` ResourceContinuity(core)
10. `P2-1` ResourceContinuity(expanded)

---

## P0-1 Outline Schema Upgrade

### 进度字段

| 字段 | 内容 |
|------|------|
| 状态 | 已完成 |
| 负责人 |  |
| 开始时间 | 2026-04-15 |
| 完成时间 | 2026-04-15 |
| 最后更新 | 2026-04-15 |
| 阻塞项 | 无 |
| 下一动作 | 进入 `S0`，把结构记忆底座接上 |
| 备注 | 第一版已打通 longform 骨架字段、前端编辑、序列化与服务端 prompt/normalize |

### 目标

把 `BookOutline / VolumeOutline / VolumeMilestone` 从“中短篇可勉强使用”提升到“长篇可正式挂载结构记忆层”的骨架强度。

### 目标文件

- `app/src/types/domain.ts`
- `app/src/stores/outline-store.ts`
- `app/src/lib/project-archive.ts`
- `app/src/lib/outline-serializer.ts`
- `app/src/components/OutlineView.tsx`
- `server/src/services/generation.ts`

### 子任务拆解

| 子任务ID | 子任务 | 产出 | 状态 | 依赖 | 备注 |
|------|------|------|------|------|------|
| `P0-1.1` | 骨架字段设计 | `BookOutline / VolumeOutline / VolumeMilestone` longform 字段定稿 | 已完成 | 无 | 对齐设计文档里的长篇字段口径 |
| `P0-1.2` | 前端类型与存档兼容 | `domain.ts / outline-store / project-archive / demo data` 可读写新字段 | 已完成 | `P0-1.1` | 已补默认值与旧存档兼容 |
| `P0-1.3` | 序列化与反序列化 | `outline-serializer` 可稳定序列化新字段 | 已完成 | `P0-1.2` | 避免导入导出时丢字段 |
| `P0-1.4` | 编辑 UI | `OutlineView` 可编辑新增骨架字段 | 已完成 | `P0-1.2` | 第一版先落在现有骨架面板 |
| `P0-1.5` | 生成链路同步 | 书纲 / 卷纲 / 里程碑 prompt 与 normalize 同步扩容 | 已完成 | `P0-1.3` | 已接 `server/src/services/generation.ts` |

### 验收口径

- 新骨架字段可编辑、保存、归档、恢复
- 旧项目数据不因字段扩容而损坏
- 生成链路能正式消费新增骨架字段

### 实时记录

| 日期 | 动作 | 结果 | 后续 |
|------|------|------|------|
| 2026-04-15 | 完成长篇骨架字段扩容第一版 | 字段、UI、序列化、prompt 已打通 | 转入 `S0` |

---

## S0 结构记忆底座

### 进度字段

| 字段 | 内容 |
|------|------|
| 状态 | 已完成 |
| 负责人 |  |
| 开始时间 | 2026-04-15 |
| 完成时间 | 2026-04-15 |
| 最后更新 | 2026-04-16 |
| 阻塞项 | 无 |
| 下一动作 | 无独立主线动作；后续按公共底座继续承接查询口径和维护补项 |
| 备注 | 核心底座已完成并被后续系统复用；`S0-6` 通用查询形态仍作为非阻塞补项滚动完善，Dexie 已不再属于“后置未做” |

### 目标

给后续所有结构记忆系统提供统一的服务端权威存储、CRUD 接口、前端请求封装和最小接入规范。

### 目标文件

- `app/src/types/domain.ts`
- `app/src/lib/structure-memory-client.ts`
- `server/src/services/generation-sqlite.ts`
- `server/src/services/structure-memory-store.ts`
- `server/src/routes/structure-memory.ts`
- `server/src/server.ts`

### 子任务拆解

| 子任务ID | 子任务 | 产出 | 状态 | 依赖 | 备注 |
|------|------|------|------|------|------|
| `S0-1` | 统一结构记忆命名与状态口径 | 各系统接口命名、状态枚举、`Id + name/title` 双存口径明确 | 已完成 | 无 | 已先落 `ThreadLedger` 第一版 |
| `S0-2` | 服务端 SQLite 建表与索引 | 新增结构记忆相关表和基础索引 | 已完成 | `S0-1` | 已补 `thread_ledgers` |
| `S0-3` | 服务端 store 层 | 统一 CRUD、normalize、列表查询、按项目聚合查询 | 已完成 | `S0-2` | 已补 `structure-memory-store.ts` 第一版 |
| `S0-4` | 路由与注册 | `/api/structure-memory/*` 读写接口可用 | 已完成 | `S0-3` | 已注册 `thread-ledgers` 路由 |
| `S0-5` | 前端 client 封装 | `structure-memory-client.ts` 可直接供各 store 调用 | 已完成 | `S0-4` | 已可供后续结构记忆 store 复用 |
| `S0-6` | 通用查询形态 | 约定按 `projectId`、`volumeId`、`chapterId`、`status` 查询 | 部分完成 | `S0-3` | 当前先覆盖 `projectId/status/currentChapterOrder` |

### 验收口径

- 服务端存在统一结构记忆 route，可完成至少 1 个系统的 CRUD
- 服务端表结构和索引能支撑按 `projectId` 的常用读取
- 前端存在统一请求封装，不需要每个 store 手写 fetch 基础逻辑
- 后续 `P0-2/P0-3/P0-4` 不需要重复再造基础设施
- `S0-6` 若仍有补项，按增强项继续滚动，不回写为主线未完成

### 风险提醒

- 如果先做前端 Dexie，再补服务端权威态，后面会出现双份口径对齐成本
- 如果 route 粒度设计过散，后续 P1 系统会重复造轮子

### 实时记录

| 日期 | 动作 | 结果 | 后续 |
|------|------|------|------|
| 2026-04-15 | 建立执行计划 | 待开始 | 先做 `S0` |
| 2026-04-15 | 落地服务端权威态、route 与前端 client 第一版 | `S0` 可支撑 `ThreadLedger` 正式接入 | 进入 `P0-2` |
| 2026-04-16 | 回填当前口径 | 明确 `S0` 已作为稳定公共底座，不再以某个下游系统作为当前“下一动作” | 按下游需求继续补齐非阻塞查询形态 |

---

## P0-2 ThreadLedger

### 进度字段

| 字段 | 内容 |
|------|------|
| 状态 | 已完成 |
| 负责人 |  |
| 开始时间 | 2026-04-15 |
| 完成时间 | 2026-04-15 |
| 最后更新 | 2026-04-16 |
| 阻塞项 | 无 |
| 下一动作 | 无独立主线动作；继续观察剧情线注入排序、休眠线提示与热度阈值是否合理 |
| 备注 | 第一版已打通服务端、前端和生成上下文；当前正文消费未 `resolved` 且高热的剧情线，`active` 优先但 `dormant` 可见 |

### 目标

让系统能主动知道“有哪些剧情线、每条线推进到哪、下一步该怎么捡”。

### 目标文件

- `app/src/types/domain.ts`
- `app/src/stores/thread-ledger-store.ts`
- `app/src/lib/structure-memory-client.ts`
- `app/src/components/StructureWorkspace.tsx` 或临时落点组件
- `server/src/services/generation-sqlite.ts`
- `server/src/services/structure-memory-store.ts`
- `server/src/routes/structure-memory.ts`
- `server/src/services/generation-context.ts`

### 子任务拆解

| 子任务ID | 子任务 | 产出 | 状态 | 依赖 | 备注 |
|------|------|------|------|------|------|
| `P0-2.1` | 类型与字段定义 | `ThreadLedger` 接口落地，字段和状态枚举固定 | 已完成 | `S0-1` | 已补章节锚点字段以支持休眠告警 |
| `P0-2.2` | 服务端表与索引 | `thread_ledgers` 或统一命名表建好 | 已完成 | `S0-2` | 已补核心索引 |
| `P0-2.3` | 服务端 CRUD | 新增、编辑、删除、列表、按状态过滤 | 已完成 | `S0-3` | 已落 `structure-memory-store.ts` |
| `P0-2.4` | 前端 store | `load/create/update/delete/setActive` 形态与现有 store 对齐 | 已完成 | `S0-5` | 已补 `thread-ledger-store.ts` |
| `P0-2.5` | 最小编辑 UI | 可新建剧情线、编辑阶段、卡点、触发点、热度 | 已完成 | `P0-2.4` | 已落在 `OutlineView` 内嵌面板 |
| `P0-2.6` | 生成上下文注入 | 生成时注入未 `resolved` 且 `audienceHeat >= 3` 的剧情线提醒，`active` 优先但 `dormant` 也可进入 | 已完成 | `P0-2.3` | 已接 `generation-context.ts` |
| `P0-2.7` | 休眠告警 | dormant 线超过 N 章未推进可被查询/提示 | 已完成 | `P0-2.6` | 已有轻告警接口与 UI 展示 |
| `P0-2.8` | 调试可见性 | 调试视图可看到当前消费到的剧情线块 | 已完成 | `P0-2.6` | 当前可通过上下文预览看到新增 section |

### 验收口径

- 可为项目维护多条剧情线并持久化到服务端
- 正文生成前能从服务端读取并注入高热且未收束的剧情线提醒，活跃 / 休眠状态可见
- 能识别长期休眠但未收束的剧情线
- 调试面板或上下文预览可看到注入结果

### 实时记录

| 日期 | 动作 | 结果 | 后续 |
|------|------|------|------|
| 2026-04-15 | 建立执行计划 | 待开始 | 先做 `S0` |
| 2026-04-15 | 打通 `ThreadLedger` 服务端权威态、最小 UI 与上下文注入 | `P0-2` 第一版完成 | 转入 `P0-3` |
| 2026-04-15 | 将 `AntagonistAgenda` 与 `ThreadLedger` 面板联动 | 已支持“反派触发提醒”提示该推进哪条线 | 转做剩余 `WorldState -> Antagonist` 联动 |
| 2026-04-16 | 回填当前口径 | 明确正文注入为“未 `resolved` 且高热”的剧情线提醒，`active` 优先但 `dormant` 也可进入 | 继续在运行态观察排序和热度阈值 |

---

## P0-3 ForeshadowPlan

### 进度字段

| 字段 | 内容 |
|------|------|
| 状态 | 已完成 |
| 负责人 |  |
| 开始时间 | 2026-04-15 |
| 完成时间 | 2026-04-15 |
| 最后更新 | 2026-04-15 |
| 阻塞项 | 无 |
| 下一动作 | 转入 `P0-4 WorldStateEntry` |
| 备注 | 第一版已打通服务端、前端、卷纲 prompt 和正文上下文消费 |

### 目标

让系统不仅知道伏笔“埋了没、收了没”，还知道“打算什么时候动、依赖什么、收完改变什么”。

### 目标文件

- `app/src/types/domain.ts`
- `app/src/stores/foreshadow-plan-store.ts`
- `app/src/lib/structure-memory-client.ts`
- `app/src/components/StructureWorkspace.tsx` 或临时落点组件
- `server/src/services/generation-sqlite.ts`
- `server/src/services/structure-memory-store.ts`
- `server/src/routes/structure-memory.ts`
- `server/src/services/generation-context.ts`
- `server/src/services/generation.ts`

### 子任务拆解

| 子任务ID | 子任务 | 产出 | 状态 | 依赖 | 备注 |
|------|------|------|------|------|------|
| `P0-3.1` | 类型与字段定义 | `ForeshadowPlan` 接口落地 | 已完成 | `S0-1` | `foreshadowId + foreshadowTitle` 双存 |
| `P0-3.2` | 服务端表与索引 | `foreshadow_plans` 表和常用索引 | 已完成 | `S0-2` | 已补唯一索引防止同一伏笔重复规划 |
| `P0-3.3` | 服务端 CRUD | 支持按项目、按伏笔、按计划窗口查询 | 已完成 | `S0-3` | 已落 `structure-memory-store.ts` |
| `P0-3.4` | 前端 store | 可绑定现有 Foreshadow，并维护计划字段 | 已完成 | `S0-5` | 已补 `foreshadow-plan-store.ts` |
| `P0-3.5` | 最小编辑 UI | 允许选择关联伏笔、设置激活卷、回收卷和条件 | 已完成 | `P0-3.4` | 已落在 `OutlineView` 内嵌面板 |
| `P0-3.6` | 卷纲消费 | 生成 `VolumeOutline / Milestone` 时可注入相关伏笔计划 | 已完成 | `P0-3.3` | 已通过 `foreshadowPlanBundle` 注入 prompt |
| `P0-3.7` | 正文消费 | 当前章节可看到 activated 伏笔及回收条件提醒 | 已完成 | `P0-3.3` | 已接 `generation-context.ts` |
| `P0-3.8` | 超期告警 | `major` 伏笔超过计划回收卷 2 卷未 resolved 给出告警 | 已完成 | `P0-3.7` | 已补轻告警接口与 UI 展示 |

### 验收口径

- 每条核心伏笔都可拥有一条规划记录
- 卷纲和正文生成都能消费伏笔规划数据
- 可以识别超期未回收的重要伏笔
- 现有 `Foreshadow` 状态机不被破坏

### 实时记录

| 日期 | 动作 | 结果 | 后续 |
|------|------|------|------|
| 2026-04-15 | 建立执行计划 | 待开始 | 依赖 `S0` |
| 2026-04-15 | 打通 `ForeshadowPlan` 服务端权威态、最小 UI 与超期告警 | `P0-3` 第一版完成 | 转入 `P0-4` |
| 2026-04-15 | 将相关伏笔计划注入卷纲 prompt 与正文上下文 | 规划层开始被生成链路正式消费 | 继续做 `WorldStateEntry` |

---

## P0-4 WorldStateEntry

### 进度字段

| 字段 | 内容 |
|------|------|
| 状态 | 已完成 |
| 负责人 |  |
| 开始时间 | 2026-04-15 |
| 完成时间 | 2026-04-15 |
| 最后更新 | 2026-04-15 |
| 阻塞项 | 无 |
| 下一动作 | 进入 `P1` 批次 |
| 备注 | 第一版已支持卷级和里程碑级编辑，正文上下文当前默认消费“当前卷 + 前一卷” |

### 目标

把世界状态从“被动摘要”提升到“结构化 delta”，让卷与卷之间的世界变化能持续进入生成。

### 目标文件

- `app/src/types/domain.ts`
- `app/src/stores/world-state-store.ts`
- `app/src/lib/structure-memory-client.ts`
- `app/src/components/StructureWorkspace.tsx` 或临时落点组件
- `server/src/services/generation-sqlite.ts`
- `server/src/services/structure-memory-store.ts`
- `server/src/routes/structure-memory.ts`
- `server/src/services/generation-context.ts`

### 子任务拆解

| 子任务ID | 子任务 | 产出 | 状态 | 依赖 | 备注 |
|------|------|------|------|------|------|
| `P0-4.1` | 类型与字段定义 | `WorldStateEntry` 接口落地 | 已完成 | `S0-1` | 包含卷级和可选里程碑级 |
| `P0-4.2` | 服务端表与索引 | `world_state_entries` 表落库 | 已完成 | `S0-2` | 已补唯一范围索引 |
| `P0-4.3` | 服务端 CRUD | 支持按卷、按里程碑读取 | 已完成 | `S0-3` | 已落 `structure-memory-store.ts` |
| `P0-4.4` | 合并规则实现 | 固化 `milestone > volume` 的结构化覆盖口径 | 已完成 | `P0-4.3` | 第一版已实现 volume/milestone 合并；当前不声明 chapter 级 `WorldStateEntry`，正文入口默认仍以卷级世界状态为主 |
| `P0-4.5` | 前端 store | 支持卷级编辑和里程碑级编辑 | 已完成 | `S0-5` | 已补 `world-state-store.ts` |
| `P0-4.6` | 最小编辑 UI | 可编辑公开事件、秘密事件、势力变化、制度变化、风险 | 已完成 | `P0-4.5` | 已落在 `OutlineView` 内嵌面板 |
| `P0-4.7` | 正文消费 | 生成时注入“当前卷 + 前一卷”两层 delta | 已完成 | `P0-4.4` | 已接 `generation-context.ts` |
| `P0-4.8` | 调试可见性 | 上下文预览可看见世界状态块来源 | 已完成 | `P0-4.7` | 当前可通过上下文预览看到新增 section |

### 验收口径

- 卷级世界状态可维护并持久化
- 正文生成时能消费最近两层 delta
- 合并规则有明确实现，不靠临时拼接
- 现有 `WorldStateSummary` 仍可作为补充兜底，不直接冲突

### 实时记录

| 日期 | 动作 | 结果 | 后续 |
|------|------|------|------|
| 2026-04-15 | 建立执行计划 | 待开始 | 依赖 `S0` |
| 2026-04-15 | 打通 `WorldStateEntry` 服务端权威态、最小 UI 与 delta 注入 | `P0-4` 第一版完成 | 进入 `P1` |
| 2026-04-15 | 实现“当前卷 + 前一卷”世界状态叠加消费 | 世界变化开始稳定进入正文上下文 | 转做 `QuestionPool / AntagonistAgenda / POVPermission / ResourceContinuity` |
| 2026-04-15 | 将 `resolved` 伏笔与 `WorldState` 面板联动 | 已支持“已回收但同卷世界状态未体现”的强提醒 | 转做剩余反派 / 线程联动 |

---

## P1-1 QuestionPool

### 进度字段

| 字段 | 内容 |
|------|------|
| 状态 | 已完成 |
| 负责人 |  |
| 开始时间 | 2026-04-15 |
| 完成时间 | 2026-04-15 |
| 最后更新 | 2026-04-15 |
| 阻塞项 | 无 |
| 下一动作 | 转入 `P2` 或继续打磨 P1 细节 |
| 备注 | 主要补“悬念规划层”，不替代伏笔，已接入卷纲 prompt |

### 子任务拆解

| 子任务ID | 子任务 | 产出 | 状态 | 依赖 | 备注 |
|------|------|------|------|------|------|
| `P1-1.1` | 类型与字段定义 | `QuestionPool` 接口落地 | 已完成 | `S0-1` | 与 `ThreadLedger`、`ForeshadowPlan` 建关联 |
| `P1-1.2` | 服务端表与索引 | `question_pools` 表落库 | 已完成 | `S0-2` | 已补常用索引 |
| `P1-1.3` | CRUD 与查询 | 按问题状态、揭晓窗口查询 | 已完成 | `S0-3` | 已落独立 store/service |
| `P1-1.4` | 前端 store 与最小 UI | 可维护未解问题、线索、假答案、揭晓窗口 | 已完成 | `S0-5` | 已补 `QuestionPoolPanel` |
| `P1-1.5` | 卷纲消费 | 生成卷纲时提醒本卷该推进哪些悬念 | 已完成 | `P1-1.3` | 已通过 `questionPoolBundle` 注入 |
| `P1-1.6` | 超期告警 | `open` 且超过揭晓窗口未推进时提示 | 已完成 | `P1-1.5` | 已补轻告警 |

### 验收口径

- 可维护项目级未解问题池
- 卷纲生成可消费问题窗口提醒
- 与 `ThreadLedger`、`ForeshadowPlan` 的关联字段可用

### 实时记录

| 日期 | 动作 | 结果 | 后续 |
|------|------|------|------|
| 2026-04-15 | 建立执行计划 | 待开始 | 等 P0 稳定 |
| 2026-04-15 | 打通 `QuestionPool` 服务端、前端面板和卷纲提示注入 | `P1-1` 第一版完成 | 继续推进 `P1-2` |
| 2026-04-15 | 将 `QuestionPool` 告警升级为按当前卷序判断 | 已支持“已到窗口 / 超窗 N 卷”联动提醒 | 转做剩余跨任务联动 |

---

## P1-2 AntagonistAgenda

### 进度字段

| 字段 | 内容 |
|------|------|
| 状态 | 已完成 |
| 负责人 |  |
| 开始时间 | 2026-04-15 |
| 完成时间 | 2026-04-15 |
| 最后更新 | 2026-04-16 |
| 阻塞项 | 无 |
| 下一动作 | 无独立主线动作；继续观察世界状态触发提醒的噪声和人工复核负担 |
| 备注 | 重点是让反派主动推进，不是人物卡重复录入；世界状态联动当前只做提醒 / 建议复核，不自动改写议程数据 |

### 子任务拆解

| 子任务ID | 子任务 | 产出 | 状态 | 依赖 | 备注 |
|------|------|------|------|------|------|
| `P1-2.1` | 类型与字段定义 | `AntagonistAgenda` 接口落地 | 已完成 | `S0-1` | 关联 `LoreEntity` |
| `P1-2.2` | 服务端表与索引 | `antagonist_agendas` 表落库 | 已完成 | `S0-2` | 已补常用索引 |
| `P1-2.3` | CRUD 与查询 | 支持按角色、按状态读取 | 已完成 | `S0-3` | 已保留 `ifProtagonistDoesNothing` |
| `P1-2.4` | 前端 store 与最小 UI | 可编辑目标、动作、触发条件、出手窗口 | 已完成 | `S0-5` | 已补 `AntagonistAgendaPanel` |
| `P1-2.5` | 正文消费 | 符合触发条件时注入反派议程块 | 已完成 | `P1-2.3` | 已接 `generation-context.ts` |
| `P1-2.6` | 联动提醒 | 世界状态变化后给出反派议程复核提醒，建议人工检查 `currentAction / triggerToStrike` 是否需要调整 | 已完成 | `P0-4`, `P1-2.5` | 第一版只做提醒，不自动改写议程数据 |

### 验收口径

- 可为核心反派维护主动议程
- 正文生成中可以看到“主角不动时反派会做什么”
- 不与人物卡字段重复堆叠
- 世界状态变化时可收到“建议复核 / 重新检查”的提醒，但不会自动闭环改写议程

### 实时记录

| 日期 | 动作 | 结果 | 后续 |
|------|------|------|------|
| 2026-04-15 | 建立执行计划 | 待开始 | 等 P0 稳定 |
| 2026-04-15 | 打通 `AntagonistAgenda` 服务端、前端面板和正文上下文消费 | `P1-2` 第一版完成 | 继续推进 `P1-3` |
| 2026-04-15 | 将 `WorldState` 与 `AntagonistAgenda` 面板联动 | 已支持“世界状态触发提醒”，建议复核当前动作和出手触发，不自动改写记录 | 跨任务联动清单进入收口阶段 |
| 2026-04-16 | 回填当前口径 | 明确联动能力属于提醒 / 建议复核，不是自动闭环改写 | 继续观察提醒噪声和人工维护负担 |

---

## P1-3 POVPermission

### 进度字段

| 字段 | 内容 |
|------|------|
| 状态 | 已完成 |
| 负责人 |  |
| 开始时间 | 2026-04-15 |
| 完成时间 | 2026-04-15 |
| 最后更新 | 2026-04-15 |
| 阻塞项 | 无 |
| 下一动作 | 转入 `P2` 或继续打磨 P1 细节 |
| 备注 | 这是信息权限层，不是场景限制层；第一版正文消费优先章节级与卷级 |

### 子任务拆解

| 子任务ID | 子任务 | 产出 | 状态 | 依赖 | 备注 |
|------|------|------|------|------|------|
| `P1-3.1` | 类型与字段定义 | `POVPermission` 接口落地 | 已完成 | `S0-1` | 范围字段支持卷、里程碑、章节 |
| `P1-3.2` | 服务端表与索引 | `pov_permissions` 表落库 | 已完成 | `S0-2` | 已补常用索引 |
| `P1-3.3` | CRUD 与覆盖查询 | 支持按当前章节求出有效权限 | 已完成 | `S0-3` | 第一版由 `generation-context.ts` 完成有效范围选择 |
| `P1-3.4` | 前端 store 与最小 UI | 支持卷级默认、里程碑覆盖、章节覆盖 | 已完成 | `S0-5` | 已补 `PovPermissionPanel` |
| `P1-3.5` | 正文消费 | 注入 `mustHide + canHint + forbiddenReveal` | 已完成 | `P1-3.3` | 已接 `generation-context.ts` |
| `P1-3.6` | 调试可见性 | 能看到当前权限块来自哪一层 | 已完成 | `P1-3.5` | 当前可通过上下文预览看到新增 section |

### 验收口径

- 可按卷/里程碑/章节维护信息权限
- 正文生成能明确看到禁止透露与可暗示信息
- 覆盖优先级清晰且可调试

### 实时记录

| 日期 | 动作 | 结果 | 后续 |
|------|------|------|------|
| 2026-04-15 | 建立执行计划 | 待开始 | 等 P0 稳定 |
| 2026-04-15 | 打通 `POVPermission` 服务端、前端面板和正文信息控制块 | `P1-3` 第一版完成 | 继续推进 `P1-4` |

---

## P1-4 ResourceContinuity(core)

### 进度字段

| 字段 | 内容 |
|------|------|
| 状态 | 已完成 |
| 负责人 |  |
| 开始时间 | 2026-04-15 |
| 完成时间 | 2026-04-15 |
| 最后更新 | 2026-04-15 |
| 阻塞项 | 无 |
| 下一动作 | 转入联动验收，优先检查 `章节生成完成 -> ResourceContinuity 是否被遵守` |
| 备注 | 已具备结构化 CRUD、正文上下文注入与审核联动，是否进入 `P2` 取决于本轮收口结果 |

### 现有基础

当前已存在 [generation-resource-continuity.ts](./server/src/services/generation-resource-continuity.ts) 的启发式连续性检测，可直接复用思路，但不能替代结构化长期约束。

### 子任务拆解

| 子任务ID | 子任务 | 产出 | 状态 | 依赖 | 备注 |
|------|------|------|------|------|------|
| `P1-4.1` | 类型与字段定义 | `ResourceContinuity` 接口落地 | 已完成 | `S0-1` | 先只收核心资源类型 |
| `P1-4.2` | 服务端表与索引 | `resource_continuities` 表落库 | 已完成 | `S0-2` | 已补常用索引 |
| `P1-4.3` | CRUD 与聚合查询 | 可按焦点人物和激活状态查询 | 已完成 | `S0-3` | 已落独立 service/store |
| `P1-4.4` | 前端 store 与最小 UI | 可维护当前状态、表现限制、恢复条件、隐藏代价 | 已完成 | `S0-5` | 已补 `ResourceContinuityPanel` |
| `P1-4.5` | 正文消费 | 只注入与焦点人物强相关的 active 条目 | 已完成 | `P1-4.3` | 已接 `generation-context.ts` |
| `P1-4.6` | 审核联动 | 用结构化数据补强已有连续性问题检测 | 已完成 | `P1-4.5` | 结构化审查器已接入，旧启发式继续保留 |

### 验收口径

- 核心代价连续性可以结构化维护
- 正文生成能消费焦点人物的 active 代价约束
- 现有启发式检测得到增强而不是被废弃

### 实时记录

| 日期 | 动作 | 结果 | 后续 |
|------|------|------|------|
| 2026-04-15 | 建立执行计划 | 待开始 | 等 P0 稳定 |
| 2026-04-15 | 打通 `ResourceContinuity(core)` 服务端、前端面板和正文约束块 | `P1-4` 第一版完成 | P1 全部完成，后续转 `P2` |
| 2026-04-15 | 启动 `P1-4` 收口验收 | 已确认存在 CRUD、上下文注入、审核联动三条链路 | 回填跨任务联动检查与后续最小动作 |

---

## P2-1 ResourceContinuity(expanded)

### 进度字段

| 字段 | 内容 |
|------|------|
| 状态 | 已完成 |
| 负责人 |  |
| 开始时间 | 2026-04-15 |
| 完成时间 | 2026-04-15 |
| 最后更新 | 2026-04-16 |
| 阻塞项 | 无 |
| 下一动作 | 维持运行态观察，优先关注预算区间、样章注入表现与审核噪声；必要时回写跨任务联动清单 |
| 备注 | 当前按代码收口验收完成；按“不主动本地构建/运行”规范，样章实测、预算观察和误报 / 漏报验证继续随主线滚动，不回写为当前阻塞 |

### 子任务拆解

| 子任务ID | 子任务 | 产出 | 状态 | 依赖 | 备注 |
|------|------|------|------|------|------|
| `P2-1.1` | 扩资源类型枚举 | 物资、人情、信用、证据链等扩展可用 | 已完成 | `P1-4` | 已补预设类型列表，仍保留自定义类型口 |
| `P2-1.2` | 优化查询与筛选 | 支持按资源类型、人物、风险级别过滤 | 已完成 | `P1-4` | 已补 route / client / UI 筛选链路 |
| `P2-1.3` | 扩充正文消费策略 | 在高压结构章中动态扩张注入 | 已完成 | `P1-4` | 已按章节压力动态放宽条目数并补最近消耗/恢复条件 |
| `P2-1.4` | 审核联动扩展 | 将证据链、信用、人情等纳入连续性检查 | 已完成 | `P2-1.3` | 已补类型化约束与冲突模式 |

### 验收口径

- 代码收口：扩展资源类型可维护、可消费、可调试
- 代码收口：不破坏 `P1-4` 的核心约束消费逻辑
- 运行态待验证：样章中的动态扩张不会明显挤压其他上下文预算
- 运行态待验证：扩展审核规则的误报 / 漏报仍需继续观察

### 实时记录

| 日期 | 动作 | 结果 | 后续 |
|------|------|------|------|
| 2026-04-15 | 建立执行计划 | 待开始 | 等核心版稳定 |
| 2026-04-15 | 启动 `P2-1` 第一版实现 | 已落扩展类型、筛选、动态注入与审核补强 | 下一步做生成链路实测与收口 |
| 2026-04-15 | 完成 `P2-1` 收口 | 代码口径与文档口径均已切到完成 | 后续转跨任务联动补强 |
| 2026-04-16 | 回填验收分层口径 | 明确 `P2-1` 的“已完成”指向代码收口；样章、预算和噪声观察继续跟随主线 | 继续在预算与限流观察清单中跟踪 |

---

## 跨任务联动检查清单

| 联动项 | 检查时机 | 状态 | 备注 |
|------|------|------|------|
| `Foreshadow resolved -> WorldState 是否要更新` | `P0-3`, `P0-4` 都落地后 | 已完成 | `WorldStatePanel` 已支持“同卷世界状态待同步提醒” |
| `Antagonist trigger -> ThreadLedger 是否要推进` | `P1-2` 落地后 | 已完成 | `ThreadLedgerPanel` 已支持“反派触发提醒” |
| `章节生成完成 -> ResourceContinuity 是否被遵守` | `P1-4` 落地后 | 已完成 | 代码收口已完成结构化约束注入、动态扩张与审核联动；运行态观察后续随主线滚动 |
| `卷结束 -> QuestionPool 窗口问题是否推进` | `P1-1` 落地后 | 已完成 | 已按 `currentVolumeOrder` 输出“已到窗口 / 超窗 N 卷”提醒 |
| `WorldState 变化 -> AntagonistAgenda 是否需复核` | `P1-2`, `P0-4` 落地后 | 已完成 | `AntagonistAgendaPanel` 已支持“世界状态触发提醒 / 建议复核”，当前不自动改写 `currentAction` |

## 预算与限流观察清单

该清单对应设计文档中的“结构记忆层增量预算”和“单章记忆层总预算”，不是新的任务 ID，但属于验收后的运行态观察口径。

- 这里的观察项默认不反向覆盖“代码收口已完成”的任务状态；只有出现真实样章失控、明显误报或上下文挤压时，才回写对应任务状态或阻塞项

| 观察项 | 设计口径 | 当前状态 | 备注 |
|------|------|------|------|
| 轻章结构层预算 | `1000-1800 tokens` | 口径已定义 | 后续随主线样章继续观察 |
| 常规章结构层预算 | `1800-3000 tokens` | 口径已定义 | 作为默认注入区间 |
| 高压结构章结构层预算 | `3000-5000 tokens` | 口径已定义 | `P2-1` 的动态扩张主要受这档预算约束 |
| 单章记忆层总预算 | 轻章 `3000-5000` / 常规 `5000-8000` / 高压 `8000-10000 tokens` | 口径已定义 | 需要与人物关系层、检索摘要层合并观察 |

## 后置开发计划（现状与增强）

以下内容原本不属于本轮必须完成项；当前四项均已有第一版实现，保留在此作为“现状记录 + 后续增强入口”，而不是“尚未开始的纯待办”。

### 原建议顺序（已执行完成，保留回看）

1. `D1` 前端 Dexie 镜像全量落地
2. `D2` StructureWorkspace 大工作台
3. `D3` 历史章节全自动回填脚本
4. `D4` 复杂跨系统自动联动守护任务

### D1 前端 Dexie 镜像全量落地

| 字段 | 内容 |
|------|------|
| 状态 | 已完成 |
| 优先级 | 高 |
| 依赖 | 当前 `S0 + P0 + P1 + P2` 第一版 |
| 目标 | 补齐“服务端权威态 + 前端草稿态/镜像态”双层架构，支持未提交改动、本地暂存、冲突回写 |
| 建议产出 | `app/src/lib/db.ts` 补结构记忆镜像表；各结构记忆 store 增加镜像同步与回写策略；补基础冲突提示 |
| 验收口径 | 代码收口：前端可离线编辑草稿；保存后能与服务端版本对齐；草稿态不会直接污染正式生成输入；运行态待验证：长会话下的失败重试、同步提示和冲突提示是否足够稳定 |
| 当前进度 | 已补 Dexie 镜像总表、通用 mirror helper、7 个结构记忆 store 的镜像加载/回写基础链路；create 失败可保留本地草稿并重试上行；主要面板已补最小同步提示 |
| 下一动作 | 转入 `D2` 或在后续版本继续细化记录级同步徽标与冲突提示 |

建议拆解：
- `D1.1` 为各结构记忆系统补 Dexie 表和基础索引
- `D1.2` 定义“本地草稿 / 服务端正式态 / 待同步态”三种标记
- `D1.3` 在现有 store 中补镜像加载、保存回写、失败重试
- `D1.4` 为冲突场景补最小提示，不做复杂 merge 编辑器

当前记录：
- 2026-04-15：已补 `structureMemoryMirrors` Dexie 总表与通用 helper
- 2026-04-15：已接 `ThreadLedger / ForeshadowPlan / WorldState / QuestionPool / AntagonistAgenda / POVPermission / ResourceContinuity` 的镜像加载、服务端失败兜底、更新/删除待同步状态
- 2026-04-15：已补 create 失败时的本地草稿保留、localOnly 重试上行以及主要面板的最小同步提示

### D2 StructureWorkspace 大工作台

| 字段 | 内容 |
|------|------|
| 状态 | 已完成 |
| 优先级 | 高 |
| 依赖 | `D1` 最少完成结构记忆镜像和统一加载口径 |
| 目标 | 把目前分散在 `OutlineView` 的多个面板收敛成统一管理界面，支持跨系统浏览、筛选、联动跳转 |
| 建议产出 | 新建 `app/src/components/StructureWorkspace.tsx`；按系统分区；提供统一筛选、统一告警视图、统一跳转入口 |
| 验收口径 | 代码收口：用户能在一个工作台里看完 `Thread / Foreshadow / WorldState / Question / Antagonist / POV / Resource` 全部结构记忆；运行态待验证：多系统、多记录项目下的筛选、搜索和提醒区是否仍足够清晰 |
| 当前进度 | 已落 `StructureWorkspace` 第一版骨架、统一筛选/汇总区、跨系统搜索、统一提醒区和 `AppShell` 导航入口 |
| 下一动作 | 转入 `D3`，或在后续版本继续打磨更细的跨系统筛选与批量操作 |

建议拆解：
- `D2.1` 统一列表视图和筛选器
- `D2.2` 统一告警区和联动提醒区
- `D2.3` 支持从告警直接跳到对应记录编辑
- `D2.4` 保留 `OutlineView` 内嵌面板作为轻入口，不强行替换

当前记录：
- 2026-04-15：已补 `StructureWorkspace` 第一版骨架，统一收拢 7 个结构记忆面板
- 2026-04-15：已接 `AppShell` 导航入口，可直接进入“结构记忆”工作台
- 2026-04-15：已补跨系统搜索、统一提醒区和从提醒直达对应记录编辑

### D3 历史章节全自动回填脚本

| 字段 | 内容 |
|------|------|
| 状态 | 已完成 |
| 优先级 | 中 |
| 依赖 | `D1` 或至少服务端结构记忆表稳定 |
| 目标 | 对已有老项目按章节摘要、StateChange、Foreshadow、VolumeRecap 做批量回填，降低首次接入成本 |
| 建议产出 | 服务端脚本或维护接口，支持按项目批量生成 `ThreadLedger / QuestionPool / WorldState / ResourceContinuity` 的候选草稿 |
| 验收口径 | 代码收口：老项目可以通过一次回填拿到“可人工校正”的初稿，而不是从空表开始；运行态待验证：至少用一个老项目跑通“预览 -> 确认写入”闭环，核对来源依据与最终落库是否一致 |
| 当前进度 | 已补服务端候选生成与确认写入接口，支持按项目输出 `ThreadLedger / QuestionPool / WorldState / ResourceContinuity` 候选草稿，并在 `StructureWorkspace` 提供预览与批量写入入口 |
| 下一动作 | 后续可继续补更细粒度的“按系统 / 按候选勾选写入”与更多历史回填来源 |

建议拆解：
- `D3.1` 定义各系统可回填的最小字段集
- `D3.2` 按章节索引、卷总结、Foreshadow 状态生成候选草稿
- `D3.3` 输出“候选值 + 来源依据”，避免黑盒回填
- `D3.4` 提供“只预览 / 确认写入”两段式流程

当前记录：
- 2026-04-16：已补 `/api/structure-memory/maintenance/backfill-preview`，支持按项目生成结构记忆候选草稿并返回来源依据
- 2026-04-16：已补 `/api/structure-memory/maintenance/backfill-apply` 与 `StructureWorkspace` 入口，支持“预览 -> 确认写入”两段式回填

### D4 复杂跨系统自动联动守护任务

| 字段 | 内容 |
|------|------|
| 状态 | 已完成 |
| 优先级 | 中 |
| 依赖 | `D1` 稳定；最好 `D2` 已有统一告警入口 |
| 目标 | 把当前面板级提醒升级成更自动的守护规则或后台校验任务，减少人工漏看 |
| 建议产出 | 服务端联动检查器；按项目/章节/卷触发；统一告警输出与去重 |
| 验收口径 | 代码收口：常见的跨系统断档能被自动发现并进入统一提醒区；运行态待验证：噪声、重复告警和阈值是否控制在可用范围内 |
| 当前进度 | 已补服务端守护扫描器，统一输出 `伏笔回收 -> 世界状态`、`反派动作 -> 剧情线`、`世界状态 -> 反派议程复核提醒`、`章节生成 -> 资源连续性` 等联动告警，并接入 `StructureWorkspace` 统一提醒区 |
| 下一动作 | 后续随主线运行态继续调阈值、补更多规则并观察噪声控制效果 |

建议拆解：
- `D4.1` 统一联动规则注册和告警数据结构
- `D4.2` 支持按“章节完成 / 卷完成 / 结构记忆更新”触发检查
- `D4.3` 对重复告警做去重、聚合和降噪
- `D4.4` 先保持“提示为主”，暂不自动改写数据

当前记录：
- 2026-04-16：已补 `/api/structure-memory/maintenance/guard-alerts`，支持按项目统一拉取守护告警结果
- 2026-04-16：已把守护告警接入 `StructureWorkspaceIndex` 统一提醒区，并按目标系统落到对应分区与记录入口

### 可选深化项

以下不属于当前后置主线，但如果要继续精修，可并入后续版本：

- `WorldStateEntry` 的 chapter 级正式记录层
- 预算观测从文档口径升级成可视化统计面板
- 结构记忆提醒与生成 debug 面板的统一汇总视图

## 验收关口

### 验收分层说明

- 代码收口：检查类型、存储、路由、UI、上下文注入、调试可见性是否形成闭环
- 运行态待验证：检查样章、老项目、预算区间、守护告警噪声与人工维护负担是否符合预期；若本轮未主动本地构建 / 运行，应继续在本节和“预算与限流观察清单”里跟踪

### P0 总验收

- 代码收口：`ThreadLedger / ForeshadowPlan / WorldStateEntry` 均已服务端权威化
- 代码收口：正文生成链路可消费三类结构记忆，上下文预览或调试视图能看见注入结果
- 运行态待验证：剧情线、伏笔、世界状态的注入命中率和排序是否符合预期
- 运行态待验证：结构层 token 预算没有明显挤压人物关系层与检索摘要层

### P1 总验收

- 代码收口：`QuestionPool / AntagonistAgenda / POVPermission / ResourceContinuity(core)` 进入可用状态
- 代码收口：至少能覆盖“悬念不断线、反派主动推进、信息不泄露、关键代价不消失”四类核心问题
- 运行态待验证：问题窗口告警、反派议程提醒、资源连续性审查的误报 / 漏报是否可接受

### P2 总验收

- 代码收口：资源连续性扩展字段稳定
- 代码收口：不影响 P0/P1 主链稳定性，扩展类型筛选、动态注入和审核补强均已落地
- 运行态待验证：高压结构章中的动态扩张与审核补强不会造成预算失控或提醒噪声失真

## 测试待办清单 / 后续验证清单

该清单不是新的任务 ID，用于防止文档口径、实现行为和调试视图再次漂移。每次触碰 `generation-context`、`structure-memory` 路由、`StructureWorkspace`、`relationSnapshot`、`OutlineView` 或相关 store 时，至少回看本节对应条目。

### 自动化优先

优先补单测、集成测试、接口契约测试和最小 e2e 冒烟，用来锁住“规则边界明确、行为可稳定复现”的链路。

| 编号 | 验证主题 | 验证目标 | 建议形态 |
|------|------|------|------|
| `T-A1` | `ThreadLedger` 注入口径 | 断言进入 `ChapterBeat` / 正文上下文的剧情线是“未 `resolved` 且 `audienceHeat >= 3`”；`active` 只影响排序优先级，`dormant` 高热线也必须可见，避免回退成“只看 `active`” | 服务端上下文组装测试 + 调试块快照断言 |
| `T-A2` | `WorldStateEntry` 当前合并边界 | 断言当前正式口径仅为 `milestone > volume`；正文默认仍以卷级世界状态为主，不把 `chapter > milestone > volume > recap` 旧口径重新写回实现或测试基线 | 服务端上下文测试 + 类型 / 路由契约断言 |
| `T-A3` | `QuestionPool.expectedRevealWindow` 解析边界 | 覆盖 `第2卷中段承天城`、`第二卷末`、纯地点、空值等输入；当前只稳定识别卷号，其他自然语言语义不得被误判为可靠告警依据 | store / 工具函数单测 |
| `T-A4` | `ForeshadowPlan.relatedQuestionIds` 预留字段 | 断言当前保存链路仍允许空数组，且该字段不会驱动自动联动、自动注入或“已闭环”判断，避免把预留字段误写成现状能力 | 面板保存测试 + 接口契约测试 |
| `T-A5` | `relationSnapshot` 正式生成链路 | 断言生成请求携带前端组装的 `relationSnapshot` 并被服务端消费；避免回退成“服务端直接查询前端 `EntityRelation` 表”的错误口径 | 前后端集成测试 + 请求体快照 |
| `T-A6` | `availableCharacters` 当前边界 | 断言当前仍是人工维护名单，规划与生成链路只读取 / 透传，不自动候选、自动回填或静默覆盖人工修改 | 规划要求收集测试 + prompt payload 测试 |
| `T-A7` | `StructureWorkspace` 工作台骨架 | 断言 7 个结构记忆系统、统一搜索、统一提醒区和跳转入口仍在，避免工作台退化为分散入口或只读壳层 | 最小 e2e 冒烟 |
| `T-A8` | 历史回填接口闭环 | 覆盖 `backfill-preview -> backfill-apply` 两段式流程，校验候选值、来源依据和最终写入结果一致，不出现“预览一套、落库另一套” | 维护接口集成测试 |
| `T-A9` | 守护告警输出与去重 | 断言 `guard-alerts` 至少稳定输出目标系统、目标记录、提醒类型；规则调整时要有去重 / 聚合回归基线，避免提醒区被重复噪声淹没 | 维护接口测试 + 告警快照断言 |

### 手工联调优先

优先用于样章、老项目、跨系统工作台和预算观察；重点检查误报 / 漏报、告警噪声、人工维护负担与调试可见性。

| 编号 | 验证主题 | 验证目标 | 联调建议 |
|------|------|------|------|
| `T-M1` | `ThreadLedger` 样章注入观察 | 选择同时存在 `active` 与 `dormant` 高热线的项目，核对正文调试块 / 上下文预览中的可见性、排序和提示文案是否与当前计划口径一致 | 至少观察 1 个样章生成结果 |
| `T-M2` | `WorldStateEntry` 样章 / 卷级联调 | 用同时含卷级状态与里程碑状态的项目，确认当前正文默认仍以卷级世界状态为主；若后续真的接入里程碑或 chapter 级链路，必须单独补一轮验证而不是沿用旧结论 | 至少观察 1 个有世界状态变更的项目 |
| `T-M3` | `QuestionPool` 窗口告警噪声 | 在真实项目中混合录入“可识别卷号”“自然语言窗口”“空窗口”三类问题，观察提醒是否只在当前能力边界内触发，记录误报 / 漏报样例 | 至少覆盖 1 轮卷纲或章节规划 |
| `T-M4` | `relationSnapshot + availableCharacters` 联动 | 各跑一条“章节规划”和“正文生成”最小链路，确认显式关系来自请求快照，`availableCharacters` 仍是手工名单，不让接手者误以为“系统自动回填已落地” | 适合和调试块一起核对 |
| `T-M5` | `StructureWorkspace` 多系统体验 | 在多系统、多记录项目里检查搜索、筛选、统一提醒区和跳转是否仍足够清晰，避免提醒噪声掩盖真正问题或跨系统定位成本过高 | 至少选 1 个结构记忆记录较多的项目 |
| `T-M6` | 历史回填“预览 -> 确认写入”闭环 | 至少挑 1 个老项目跑通完整回填流程，核对来源依据、最终落库和人工校正成本，避免“理论可回填、实际不可用” | 优先选已有章节积累的老项目 |
| `T-M7` | 守护告警噪声与口径 | 连续观察若干章或至少 1 卷的守护提醒，确认仍是“提醒 / 建议复核”为主，而不是暗含自动改写；同时记录重复告警、阈值过敏和真正漏报 | 适合跟随主线滚动观察 |
| `T-M8` | `ResourceContinuity` 与预算观察 | 在高压结构章、资源密集章里观察动态扩张是否明显挤压其他上下文预算，扩展审核规则是否引入不可接受的误报 / 漏报 | 结果需同步回写“预算与限流观察清单” |

补充约束：

- 若本节条目长期未被消费，至少在每轮文档大修、上下文注入口径调整或维护接口变更后重新过一遍
- 若新增结构记忆系统或新增正式注入链路，应同步在本节补一条“自动化优先”与一条“手工联调优先”待办
- 若某条待办完成并形成稳定回归用例，可在对应任务章节的“验收口径”或“实时记录”里引用，不必删除本节原始风险说明

## 实时更新规则

每次推进建议同步更新以下位置：

1. 总览看板里的 `状态 / 当前进度 / 最后更新 / 阻塞项`
2. 对应任务章节里的 `进度字段`
3. 对应任务章节底部的 `实时记录`
4. 若本轮调整了注入规模或限流口径，同步更新“预算与限流观察清单”

建议最少记录这些信息：

- 今天做了什么
- 当前卡在哪
- 下一步最小动作是什么
- 是否改变了原实施口径

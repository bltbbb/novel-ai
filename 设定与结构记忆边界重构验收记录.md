# 设定与结构记忆边界重构验收记录

对应方案：[设定与结构记忆边界重构方案.md](./设定与结构记忆边界重构方案.md)  
对应执行计划：[设定与结构记忆边界重构执行计划.md](./设定与结构记忆边界重构执行计划.md)

## 验收目的

本记录用于收口这轮“设定与结构记忆边界重构”的最终验收结论，确认以下 5 条执行线已经按当前方案口径落地：

- `P0` 来源优先级与同类信息去重
- `P0-I` 结构记忆注入增强
- `P1` 设定库收窄为稳定事实层，并拆出候选设定
- `P2` 伏笔追踪降级并入结构记忆
- `P3` 关系图谱展示改为展示 / 观察 / 诊断视图

## 验收范围与边界

本记录纳入的范围：

- 服务端上下文组装规则是否按新边界收口
- 前端信息架构、命名和关键工作流是否与方案一致
- 文档相关的关键 smoke 是否能在当前 UI 上走通

本记录不纳入的范围：

- 老项目迁移与兼容策略
- 旧版全量 e2e 的通过率
- 与当前方案无关的历史调试脚本稳定性
- 真实长期项目的连续多章人工创作压测

说明：

- 老前端 e2e 存在明显 selector 与页面流程漂移，本轮不再把“旧全量 e2e 是否通过”作为主验收依据。
- 本轮验收采用“服务端主链路回归 + 当前 UI 定向 smoke + 文档口径核对”的组合口径。

## 验收方式

- 验收日期：`2026-04-17`
- 文档依据：
  - [设定与结构记忆边界重构方案.md](./设定与结构记忆边界重构方案.md)
  - [设定与结构记忆边界重构执行计划.md](./设定与结构记忆边界重构执行计划.md)
- 服务端与工程健康结果：
  - `server npm test`：`57/57` 通过
  - `server npm run build`：通过
  - `app npm run typecheck`：通过
  - `app npm run build`：通过
- 本轮前端定向 smoke：

```powershell
npx playwright test e2e/records-foreshadow.spec.ts e2e/inspiration-entry.spec.ts e2e/graph-template.spec.ts --reporter=line --workers=1
```

结果：

- `3 passed (25.1s)`

对应 smoke 文件：

- [app/e2e/records-foreshadow.spec.ts](./app/e2e/records-foreshadow.spec.ts)
- [app/e2e/inspiration-entry.spec.ts](./app/e2e/inspiration-entry.spec.ts)
- [app/e2e/graph-template.spec.ts](./app/e2e/graph-template.spec.ts)

## 分项验收结果

## P0 来源优先级与去重

验收点：

- `entitySnapshot > generation_entities`
- `foreshadowSnapshot > generation_foreshadows`
- `world_state_delta` 列表维度按“里程碑优先 + 卷级补并 + 去重”收口
- `resource_continuity` 不再与运行态变化并列重复
- `candidate_entities` 只消费 `availableCharacterNames`，不反向并入 `focus_entities`

结论：

- 已通过。
- 当前实现已经按“winner 主块 + supplement 折回 + discard 不输出”收口。
- `candidate_entities` 当前预算上限固定为 `5` 条，并已同步回方案和执行计划。

相关记录：

- [P0-5 bundle回归验证记录.md](./P0-5%20bundle回归验证记录.md)

## P0-I 结构记忆注入增强

验收点：

- `thread_ledger / foreshadow_plan / resource_continuity / antagonist_agenda / pov_permission` 的参与感提升
- `QuestionPool` 仍维持轻量提醒，不升为独立主 section
- 放宽后没有出现明显重复注入回潮

结论：

- 已通过。
- 当前策略保持“强命中优先完整、候选项预算内追加、低优先级摘要压缩”。
- `QuestionPool` 仍符合方案中“提醒层 / 候选层”的定位，没有修偏成正文主块。

相关记录：

- [P0-I5 注入增强回归验证记录.md](./P0-I5%20注入增强回归验证记录.md)

## P1 设定库收窄与候选设定分区

验收点：

- `设定库` 默认维护稳定事实层
- 页面区分 `正式设定 / 候选设定 / 待确认设定`
- 人物完整度按稳定事实层计算，不再依赖 `current_*`
- 动态字段不再作为人物卡主编辑区核心内容

前端 smoke 覆盖：

- 新建设定后默认进入 `正式设定`
- `转为草案` 后进入 `候选设定 / 待确认设定`
- `确认设定` 后回到正式区
- 当前人物卡完整度文案为 `0/8 稳定事实层`

结论：

- 已通过。
- 当前 UI 与方案中“正式设定是真源，候选设定待人工确认”的口径一致。

## P2 伏笔追踪并入结构记忆

验收点：

- 前台不再保留独立一级 `伏笔追踪`
- `结构记忆` 内保留 `伏笔事实 / 伏笔规划` 双模式
- 事实层与规划层职责分离，不再相互混写

前端 smoke 覆盖：

- 左侧导航不再出现 `伏笔追踪`
- `结构记忆` 中可进入 `伏笔事实 / 规划`
- 双 Tab `伏笔事实 / 伏笔规划` 可正常切换

结论：

- 已通过。
- 这条线当前按“入口下线，但能力并入结构记忆统一工作台”的方案执行，没有偏离。

## P3 关系图谱展示口径收口

验收点：

- 一级入口保留，但名称改为 `关系图谱展示`
- 页面明确为“展示 / 观察 / 诊断视图”
- 关系视图中明确区分 `显式关系真源` 与 `运行态关系观察`
- 节点详情区继续作为查看与跳转辅助，不承担正式维护职责

前端 smoke 覆盖：

- 左侧导航入口名称为 `关系图谱展示`
- 页面头部说明文案强调“不是正式关系维护台账”
- `综合图谱 / 关系视图` 切换可用
- 在 `关系视图` 中可看到 `显式关系真源 / 运行态关系观察`
- 点击节点后可进入详情区，并显示 `前往设定库`

结论：

- 已通过。
- 当前页面定位已经和方案定义一致，不再容易被理解成“正式关系真源维护台”。

## 文档补充结果

这轮验收同时补齐了两处之前“实现已定、文档未写死”的边界：

1. `candidate_entities`
   - 只消费 `availableCharacterNames`
   - 已进入 `focus_entities` 的角色不再重复进入候选区
   - 当前弱提示预算上限固定为 `5`
2. `world_state_delta`
   - 列表维度固定为“里程碑 winner 主导 + 卷级补并唯一项 + 去重”

对应文档已同步：

- [设定与结构记忆边界重构方案.md](./设定与结构记忆边界重构方案.md)
- [设定与结构记忆边界重构执行计划.md](./设定与结构记忆边界重构执行计划.md)

## 总体验收结论

本轮“设定与结构记忆边界重构”按当前方案与执行计划口径，可以判定为：

- `通过`

通过依据是：

1. 服务端来源优先级、去重与结构记忆注入增强已经收口
2. `设定库 / 结构记忆 / 关系图谱展示` 的前端信息架构与职责边界已对齐方案
3. 文档相关 3 条前端 smoke 已在当前 UI 上全部通过
4. 原先未写死的关键实现边界已回填到方案与执行计划

## 当前仍保留的后续事项

这轮验收完成后，仍建议继续保留以下后续动作：

1. 若后面继续调整导航或文案，需同步维护这 3 条前端 smoke，避免再次出现“脚本过旧但产品正常”的假红灯。
2. 当真实调试项目进入更长链路创作时，仍建议补一次“真实章节 bundle 导出 + 人工逐 section 审计”。
3. 老全量 e2e 如仍要保留，需要按当前 UI 和页面职责重新拆分，而不应继续直接沿用旧入口脚本。

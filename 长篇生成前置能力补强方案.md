# 长篇生成前置能力补强方案

## 1. 摘要

基于现有架构，`v1` 不重造入口，而是增强现有 `InspirationDialog`，并把“时间轴骨架 + 实体轴 + 伏笔轴 + 手动规划修正”接成一条完整立项链路。

本轮固定决策如下：

- 保留“直接创建项目”和“灵感入口”两个并列入口
- 复用并增强现有 `InspirationDialog`，不新建第二套灵感组件
- 新增服务端 `POST /api/ai/inspiration-blueprint`，仅负责 `transcript -> InspirationBlueprint`
- `requiredEntities / requiredForeshadows` 只先加到 `VolumeOutlineFields` 和 `VolumeMilestoneDraft`
- 第一卷立项只调用一次 `volume-outline`，直接使用返回的 `milestones`
- `Drift Correction` 采用“手动触发 + 建议稿 + 新旧两栏 + 一键覆盖/取消”
- `Dexie` 不升 `schema version`，只做默认值归一化与兼容处理

## 2. 灵感入口增强

### 2.1 总体方向

增强现有 `InspirationDialog`，保留当前“多轮讨论 -> 提炼 blueprint -> 创建项目 + 书纲 + 卷纲”的主流程，但把 blueprint 提炼从前端 prompt 内联迁到服务端接口。

### 2.2 新增接口

- `POST /api/ai/inspiration-blueprint`

职责：

- 输入：聊天 transcript、当前模型配置
- 输出：`InspirationBlueprint`
- 只负责结构化提炼，不负责创建项目，不负责落库

### 2.3 InspirationBlueprint 结构

固定输出：

- `projectTitle`
- `projectDescription`
- `genres`
- `projectStylePrompt`
- `discussionSummary`
- `bookOutlineHint`
- `volumePlans`
- `seedEntities`
- `seedForeshadows`
- `coverage`

其中：

- `seedForeshadows` 结构：
  - `title`
  - `notes`
  - `linkedEntityNames?: string[]`
- `coverage` 结构：
  - `coreHook: boolean`
  - `protagonistDrive: boolean`
  - `worldSlice: boolean`
  - `endgameConflict: boolean`

### 2.4 前端交互规则

- 讨论区继续走多轮对话
- 右侧显示 4 项 checklist：
  - 核心卖点/金手指
  - 主角前期驱动力
  - 初始世界切片
  - 终局冲突/最高敌人
- `v1` 不做每轮实时 AI checklist 判断
- 在点击“生成项目”时调用 `inspiration-blueprint`，用返回的 `coverage` 更新 checklist，并提示“信息偏少但可继续”
- 用户仍可继续生成，不做硬门槛拦截

## 3. 立项包生成与落库顺序

点击“生成项目”后，前端串联调用并逐步持久化；中途失败时保留已完成结果，不回滚整个项目。

固定顺序：

1. 调用 `inspiration-blueprint`
2. 创建项目 + 默认卷
3. 写入 `seedEntities -> Lore`
4. 写入 `seedForeshadows -> Foreshadow`
5. 调用 `book-outline`
6. 保存书纲
7. 调用一次 `volume-outline`
8. 保存第一卷卷纲与其中返回的 `milestones`
9. 对 `requiredEntities` 中缺失的实体补建 `Lore` 占位条目

失败策略：

- 项目创建成功后，不因后续生成失败而删除项目
- 书纲成功、卷纲失败时：保留书纲，卷纲留空，允许用户手动继续生成
- `Lore / Foreshadow` 写入成功后不回滚
- `InspirationDialog` 保持显式进度状态机，沿用现有 `progressText` 风格，但升级为分步骤状态

## 4. 结构化引用

### 4.1 新增字段范围

仅新增到以下结构：

- `VolumeOutlineFields`
  - `requiredEntities?: string[]`
  - `requiredForeshadows?: string[]`
- `VolumeMilestoneDraft`
  - `requiredEntities?: string[]`
  - `requiredForeshadows?: string[]`

明确不做：

- `v1` 不给 `ChapterBeatFields` 加这两个字段
- `v1` 不给书纲层加这两个字段

### 4.2 兼容策略

- 前后端类型同步扩展
- 所有读取、保存、导入导出路径统一归一化为空数组
- 因为不新增 `Dexie` 索引字段，不升 `schema version`
- `archive round-trip` 必须保持兼容旧项目

### 4.3 Prompt 与接口同步

- `volume-outline` prompt 要求模型输出 `requiredEntities / requiredForeshadows`
- `volume-outline` 的归一化逻辑补这两个字段
- `volume-milestones` 如果后续单独调用，也补同样字段，但立项主流程里不使用它

## 5. Lore / Foreshadow 与上下文组装联动

### 5.1 实体联动

- 将当前卷纲与当前里程碑中的 `requiredEntities` 合并进现有 `focusEntityNames`
- 继续复用服务端 `generation_entities` 快照与 `buildEntityBlocks()` 逻辑
- 不直接读取前端 `IndexedDB Lore` 作为运行时上下文来源

### 5.2 伏笔联动

`requiredForeshadows` 必须覆盖两条路径：

- 对 `active foreshadow`
  - 确保这些项在 `working memory` 注入时优先保留，不被截断
- 对 `dormant foreshadow`
  - 在 `dormant recall` 里额外加权或直接强制召回

也就是说，`requiredForeshadows` 要同时传给 `active` 注入路径和 `dormant recall` 路径，而不是只管 `retrieval`

### 5.3 占位条目规则

- 若 `requiredEntities` 对应 `Lore` 不存在，则创建轻量 `Lore` 占位条目
- 占位条目不新增 `schema` 字段，只在 `tags` 中打 `#placeholder`
- `requiredForeshadows` 对应 `Foreshadow` 不存在时，创建最小条目，`notes` 优先用 blueprint 的 `seedForeshadows.notes`

## 6. Drift Correction

### 6.1 新增接口

- `POST /api/ai/volume-plan-reconcile`

职责：

- 输入由前端组装后一次性提交
- 服务端只做规划修正推理，不主动查散落表

### 6.2 前端输入硬上限

- 章节摘要：最多最近 `30` 章，或当前卷已有章节
- `Lore` 摘要：只取 `pinned + requiredEntities` 涉及实体
- `Foreshadow` 摘要：只取 `active + 与当前卷相关的 dormant`
- 避免输入 token 失控

### 6.3 输出结构

- `proposedVolumeOutline`
- `proposedMilestones`
- `changeSummary`
- `riskNotes`

### 6.4 前端交互

- 在 `Outline / Generation` 侧提供“修正当前卷规划”入口
- 生成建议稿后展示“旧规划 / 新规划”两栏
- 操作仅有：
  - 一键覆盖
  - 取消
- `v1` 不做字段级部分接受
- `v1` 不修书纲，只修当前卷

## 7. 测试与验收

### P1 类型与兼容

- 旧项目读取不报错
- 新字段默认归一化为空数组
- 导入导出 `round-trip` 后，新字段不丢失，旧归档仍可导入

### P2 Blueprint 提炼

- 给定 transcript，`inspiration-blueprint` 能稳定返回合法 `JSON`
- 输出包含 `coverage`
- `seedForeshadows` 至少能稳定返回 `title + notes`

### P3 灵感入口落库链路

- 现有 `InspirationDialog` 仍能正常讨论
- 点击生成后能完成：项目 + 书纲 + 第一卷卷纲 + `Lore + Foreshadow`
- 中途失败时已落库部分保留，用户可继续补全
- checklist 会在生成前显示覆盖状态，不足时仅提示不拦截

### P4 上下文消费

- 卷纲/里程碑声明的 `requiredEntities` 能优先进入实体上下文
- `requiredForeshadows` 对 `active` 和 `dormant` 两条路径都生效
- 没有结构字段时，现有 `Lore` 文本命中与 `dormant recall` 仍可兜底

### P5 Drift Correction

- 手动触发后能生成建议稿
- 前端能显示新旧两栏
- 覆盖前原规划不变
- 覆盖后当前卷卷纲与里程碑同时更新

## 8. 实施阶段

| Phase | 内容 | 验收标准 |
| --- | --- | --- |
| P1 | 类型定义、归一化、导入导出兼容 | 旧项目读取正常，新字段默认空数组，archive round-trip 通过 |
| P2 | 服务端 `inspiration-blueprint` 接口 | transcript 能稳定返回完整 `InspirationBlueprint JSON` |
| P3 | 增强现有 `InspirationDialog` 与落库链路 | 从讨论到项目 + 书纲 + 卷纲 + `Lore + Foreshadow` 一条龙可用 |
| P4 | Context 组装消费 `requiredEntities / requiredForeshadows` | 卷纲声明的实体/伏笔能优先注入上下文 |
| P5 | Drift Correction 接口与前端两栏对比 | 手动触发 -> 建议稿 -> 新旧两栏 -> 一键覆盖/取消 |

并行关系：

- `P1` 与 `P2` 可并行
- `P3` 依赖 `P1 + P2`
- `P4` 依赖 `P1`
- `P5` 依赖 `P1`，但可独立推进

## 9. 假设与默认

- 这次明确是重构现有 `InspirationDialog`
- `inspiration-blueprint` 只做 transcript 提炼，不做项目创建
- `requiredEntities / requiredForeshadows` 只先加到卷纲与里程碑
- 立项时第一卷只调用一次 `volume-outline`
- 占位实体使用 `#placeholder` tag 标记
- `Drift Correction v1` 只做当前卷，且只支持“一键覆盖 / 取消”

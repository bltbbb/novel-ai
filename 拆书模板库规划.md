# 拆书模板库规划

## 1. 文档目标

本规划用于明确“拆书功能 + 模板库存储 + 新建项目套用模板”的完整方案，目标是让系统能够分析长篇网文的写法特征，沉淀为可复用的创作模板，并在新项目创建时直接使用这些模板结果。

本次规划只定义产品方案、数据结构、处理流程、页面入口、接口边界与阶段目标，不包含代码实现。

## 1.1 当前进度

截至 2026-04-11，当前实现进度如下。

### 已完成

- 已完成模板库本地数据结构与本地存储
- 已完成拆书模板的数据模型、项目模板快照模型与 `promptBundle`
- 已完成同步版拆书分析接口
- 已完成拆书任务制接口、任务列表、任务进度展示与服务端任务持久化
- 已完成模板库弹层版入口
- 已完成模板库独立页面壳子
- 已完成模板新建、编辑、保存、删除
- 已完成模板复制、模板导入、模板导出
- 已完成 EPUB 导入与正文提取
- 已完成拆书范围选择，当前支持 `全文 / 开篇 / 中段 / 结尾`
- 已完成自定义章节区间拆书
- 已完成子模板拆分，当前支持开篇 / 中段 / 爆点 / 收尾四类子模板
- 已完成模板效果对比
- 已完成新建项目时选择模板库模板
- 已完成已有项目绑定模板、切换模板、解绑模板
- 已完成项目列表和项目内的模板状态展示
- 已完成模板接入书纲、卷纲、里程碑、节拍、正文、风格转译、编辑器续写
- 已完成兼容控制台队列入口的模板文风接入
- 已完成拆书任务恢复
- 已完成基础细粒度统计展示，当前已展示段落数、平均段长、对白段占比和标题片段数
- 已完成基础证据回看，当前已展示采样片段标题与摘录
- 已完成全量轻分析独立模块，当前已独立输出轻分析统计与任务阶段

### 部分完成

无。

### 未完成

无。

## 2. 背景与问题

当前系统的主创作链路是：

1. 填写书纲
2. 填写卷纲
3. 生成里程碑
4. 生成节拍
5. 生成正文
6. 进行风格转译与审查

当前存在的问题：

- 还没有独立的模板库，无法沉淀“拆书后得到的写法模板”
- 现有项目创建入口里的模板偏向“起步项目模板”，不是服务整条创作链路的“写法模板”
- 拆书对象通常是几百万字级的网文，无法把整本书直接塞进单次 prompt
- 即便分析出结果，如果不进入项目链路，也只能形成一次性分析文本，复用价值有限

因此，本次方案需要解决两类问题：

- 如何对超长网文进行可扩展、可控成本的拆解分析
- 如何把分析结果真正接入项目创建与后续生成链路

## 3. 目标

本功能的核心目标：

- 新增一个独立的模板库，用于保存拆书得到的创作模板
- 支持对长篇作品进行离线式拆书分析，而不是一次性大 prompt 分析
- 让新建项目时可以选择模板库中的模板结果
- 让模板结果参与后续的大纲、卷纲、里程碑、节拍、正文与风格转译
- 让模板结果以“抽象写法”形式保存，而不是保存整本原文

## 4. 非目标

第一阶段不纳入以下内容：

- 不做多模板混合生成
- 不做模板市场、模板分享、模板发布
- 不做站外自动抓书
- 不做整本书原文的长期存档与检索中心
- 不做复杂版权管理能力
- 不做高度自动化的“整本精读复刻”

## 5. 核心设计原则

- 模板库与项目解耦，模板是可复用资产，不直接绑定单一项目
- 项目使用模板时采用“模板快照”，避免模板后续变更污染已创建项目
- 拆书结果保存为抽象写法、统计特征与生成约束，不保存大段原文作为模板正文
- 长篇拆书采用“切章、统计、采样、分段分析、聚合”的离线流程
- 大模型负责归纳、抽象、定稿，不负责一次性通读整本书
- 模板必须可直接进入现有创作链路，而不是只停留在展示层

## 6. 核心概念

### 6.1 模板库模板

指拆书后沉淀下来的创作模板，用于指导新项目的整体写法。

### 6.2 拆书任务

指一次对某部作品或某段章节区间进行分析的后台任务，任务完成后产出模板草稿。

### 6.3 项目模板快照

指新建项目时，从模板库复制一份模板内容到项目内部，后续项目使用这份快照，不直接依赖模板库原始记录。

## 7. 用户流程

### 7.1 模板入库流程

1. 用户进入模板库页面
2. 用户发起拆书任务
3. 用户导入作品文本，或选择章节区间
4. 系统完成预处理、统计、采样、分段分析与聚合
5. 系统生成模板草稿
6. 用户人工检查、修改、命名
7. 用户保存到模板库

### 7.2 项目使用流程

1. 用户新建项目
2. 在创建流程中选择“使用模板库模板”
3. 选择一条模板库记录
4. 系统把模板复制为项目模板快照
5. 项目后续书纲、卷纲、里程碑、节拍、正文与风格转译使用该模板快照

### 7.3 后续扩展流程

后续可支持：

- 已有项目切换模板
- 一个项目解绑模板
- 基于模板重新生成某一阶段内容

## 8. 模板库模板内容设计

模板库中的每条模板建议保存以下信息：

- 模板名称
- 来源作品名
- 来源作者名
- 来源备注
- 题材标签
- 适用方向
- 风格总述
- 叙事视角特征
- 信息揭示方式
- 节奏结构特征
- 冲突设计方式
- 爽点设计方式
- 人物塑造方法
- 对话习惯
- 开篇写法
- 中盘推进写法
- 爆点章节写法
- 收尾与钩子写法
- 常用推进套路
- 禁忌写法
- 模板置信度
- 分析版本号
- 分析时间

同时保存一组供生成链路直接使用的 `promptBundle`：

- `bookOutlinePrompt`
- `volumeOutlinePrompt`
- `milestonePrompt`
- `beatPrompt`
- `writingPrompt`
- `stylePrompt`
- `negativePrompt`

## 9. 长篇网文拆书处理方案

### 9.1 总体思路

对几百万字作品，不采用“一次性全文进 prompt”的方式，而采用以下分层流程：

1. 预处理
2. 全量轻分析
3. 代表性采样
4. 分段精读
5. 分层聚合
6. 模板生成

### 9.2 预处理阶段

输入来源建议支持：

- `txt`
- 章节化纯文本
- 后续扩展 `epub`

预处理内容：

- 自动切章
- 自动识别卷
- 清理作者话
- 清理平台提示语
- 清理重复结尾
- 清理广告和异常段落
- 生成章节索引
- 记录章节字数与顺序

产物：

- 标准化章节列表
- 卷列表
- 原始统计索引

### 9.3 全量轻分析阶段

该阶段尽量不依赖大模型，优先使用本地规则与统计方法，目标是低成本获取全书轮廓。

建议统计项：

- 每章字数
- 卷内字数分布
- 段落长度分布
- 句长分布
- 对话占比
- 第一人称/第三人称倾向
- 标题格式与命名习惯
- 开头高频切入方式
- 结尾高频悬念表达
- 高频转折词
- 高频行为词
- 角色称呼方式
- 章节节奏波动曲线

这一层的目的不是生成模板，而是为后续采样和聚合提供基础信号。

### 9.4 代表性采样阶段

由于无法让大模型逐章精读整本书，因此需要从全书中选取代表样本。

推荐采样维度：

- 开篇样本
- 每卷卷首样本
- 每卷卷中样本
- 每卷卷末样本
- 高冲突样本
- 高对白样本
- 高铺垫样本
- 高悬念收尾样本
- 固定间隔抽样样本

采样策略目标：

- 尽量覆盖不同阶段写法
- 避免只学到开篇风格
- 避免整本平均后失真

### 9.5 分段精读阶段

把样本章节拼成若干分析 chunk，每个 chunk 单独交给模型分析。

chunk 设计建议：

- 每个 chunk 由 3 到 5 章组成
- 单个 chunk 不追求完整剧情理解，只分析写法模式
- 输出必须为结构化 JSON

每个 chunk 需要提取的信息：

- 文风特征
- 叙事视角
- 节奏判断
- 冲突制造方式
- 爽点触发方式
- 人物塑造方法
- 对话特点
- 信息揭示方式
- 开头习惯
- 结尾习惯
- 常见套路
- 禁忌写法

### 9.6 聚合阶段

聚合分为两层：

1. 卷级聚合
2. 全书聚合

聚合时要显式区分：

- 稳定特征
- 阶段性特征
- 高频特征
- 偶发特征

这样可以避免把前期开局写法和后期拖长线写法简单平均。

### 9.7 模板生成阶段

最终由聚合结果生成模板草稿，模板草稿重点产出：

- 风格总述
- 结构写法摘要
- 节奏策略摘要
- 爽点与冲突策略
- 角色塑造策略
- 章节开头与结尾策略
- 负面约束
- 可直接用于生成的 promptBundle

模板里不保留整本原文，只保留必要的证据摘要与分析元信息。

## 10. 模板库存储设计

建议新增三类核心数据：

### 10.1 模板库记录

用于保存最终模板。

建议字段：

- `id`
- `name`
- `sourceTitle`
- `sourceAuthor`
- `sourceNotes`
- `tags`
- `summary`
- `narrativeStyle`
- `pacingStyle`
- `conflictStyle`
- `characterStyle`
- `dialogueStyle`
- `openingStyle`
- `midgameStyle`
- `climaxStyle`
- `endingHookStyle`
- `commonPatterns`
- `forbiddenPatterns`
- `promptBundle`
- `confidence`
- `analysisVersion`
- `createdAt`
- `updatedAt`

### 10.2 拆书任务记录

用于保存任务状态与进度。

建议字段：

- `id`
- `sourceTitle`
- `sourceAuthor`
- `importType`
- `status`
- `progressStage`
- `progressPercent`
- `message`
- `selectedRange`
- `chapterCount`
- `wordCount`
- `sampleCount`
- `errorMessage`
- `resultTemplateId`
- `createdAt`
- `updatedAt`

### 10.3 项目模板快照

项目创建时保存到项目内部。

建议字段：

- `projectId`
- `templateLibraryId`
- `templateName`
- `sourceTitle`
- `summary`
- `promptBundle`
- `createdAt`
- `updatedAt`

## 11. 项目接入设计

### 11.1 新建项目

新建项目时增加一项：

- 是否从模板库选择模板

如果用户选择模板：

- 在创建项目时复制模板为项目模板快照
- 模板名称和来源信息写入项目元数据
- 后续链路优先使用项目模板快照中的 promptBundle

### 11.2 项目后续生成链路接入

模板快照建议接入以下节点：

- 书纲生成
- 卷纲生成
- 里程碑生成
- 节拍生成
- 正文生成
- 风格转译

接入原则：

- 项目模板优先
- 全局设置中的文风配置作为兜底
- 用户手工输入的即时提示词优先级高于模板默认提示

### 11.3 模板快照策略

项目创建时采用快照，而不是实时引用模板库记录，原因如下：

- 老项目行为稳定
- 模板库后续编辑不会影响历史项目
- 有利于回溯和调试

## 12. 页面与交互规划

### 12.1 模板库页面

建议新增一级入口“模板库”，包含三个区域：

- 模板列表
- 拆书任务列表
- 模板详情/编辑区

模板列表信息建议展示：

- 模板名
- 来源作品
- 标签
- 最近更新时间
- 模板摘要

模板详情建议展示：

- 风格总述
- 写法拆解
- promptBundle
- 证据摘要
- 分析时间

### 12.2 拆书任务创建弹层

建议支持：

- 输入作品名
- 输入作者名
- 选择导入方式
- 粘贴文本或选择文件
- 选择分析范围
- 启动分析任务

### 12.3 新建项目弹层

在现有创建项目流程中新增：

- 选择模板库模板
- 预览模板摘要
- 显示模板来源作品
- 允许不使用模板

## 13. 接口规划

### 13.1 模板库接口

建议提供：

- 获取模板列表
- 获取模板详情
- 创建模板
- 更新模板
- 删除模板

### 13.2 拆书任务接口

建议提供：

- 创建拆书任务
- 查询任务列表
- 查询任务详情
- 查询任务进度
- 取消任务
- 基于任务结果生成模板草稿

### 13.3 项目绑定接口

如果项目模板快照走后端统一管理，则建议提供：

- 新建项目时绑定模板
- 读取项目模板快照
- 更新项目模板快照
- 解绑项目模板快照

## 14. 拆书任务状态机

建议状态如下：

- `pending`
- `preprocessing`
- `light_analyzing`
- `sampling`
- `chunk_analyzing`
- `aggregating`
- `draft_ready`
- `completed`
- `failed`
- `cancelled`

每个任务需要可展示：

- 当前阶段
- 已处理章节数
- 已抽样数量
- 已完成 chunk 数
- 当前说明文案
- 失败原因

## 15. MVP 范围

第一阶段建议只做以下能力：

- `已完成` 新增模板库页面
- `已完成` 新增拆书任务能力
  当前已支持后台任务创建、轮询、服务端持久化、任务恢复、结果回填、失败重试、预设范围选择与自定义区间拆书
- `已完成` 支持 `txt` 导入
- `已完成` 支持 `epub` 导入
- `已完成` 支持长文本切章与基础清洗
- `已完成` 支持全量轻分析
- `已完成` 支持代表性采样
- `已完成` 支持 chunk 级分析与聚合
- `已完成` 生成一份模板草稿
- `已完成` 用户保存模板到模板库
- `已完成` 新建项目时选择模板库模板
- `已完成` 项目创建后保存模板快照
- `已完成` 模板快照接入书纲、卷纲、里程碑、正文、风格转译

## 16. 第二阶段增强项

后续可扩展：

- 细化范围分析策略
- 支持项目中途切换模板
- 支持模板导出
- 支持模板复制
- 支持更细粒度的统计与证据回看

## 17. 风险与注意事项

### 17.1 成本风险

如果 chunk 过多、采样过密，会导致模型调用成本偏高。

应对方式：

- 先做全量轻分析
- 控制抽样密度
- 优先分析代表性章节
- 支持分阶段任务恢复

### 17.2 失真风险

如果只分析少量章节，容易学偏。

应对方式：

- 保证采样覆盖不同卷和不同阶段
- 在聚合阶段区分稳定特征与阶段性特征

### 17.3 版权风险

拆书结果不应作为原文镜像保存或输出。

应对方式：

- 模板只保存抽象写法
- 不保存大段连续原文
- 页面展示证据时只保留必要摘录

### 17.4 使用效果风险

如果模板只做展示，不接入生成链路，用户感知价值会很低。

应对方式：

- 新建项目时直接套用
- 后续生成节点默认引用模板快照

## 18. 开发阶段建议

### 阶段一：基础骨架

- `已完成` 完成模板库数据结构
- `已完成` 完成模板库页面壳子
- `已完成` 完成新建项目选择模板入口

### 阶段二：拆书任务 MVP

- `已完成` 完成导入、切章、清洗
- `已完成` 完成轻分析
- `已完成` 完成采样
- `已完成` 完成 chunk 分析与聚合
- `已完成` 完成模板草稿生成

### 阶段三：项目接入

- `已完成` 完成模板快照保存
- `已完成` 接入书纲、卷纲、里程碑、正文、风格转译
- `已完成` 补充已有项目切换模板与解绑模板

### 阶段四：增强体验

- `已完成` 增加任务进度与失败恢复
- `已完成` 增加模板编辑与复制
- `已完成` 增加范围分析与子模板能力

## 19. 验收标准

达到以下标准即可认为第一阶段方案落地成功：

- 用户可以在模板库中创建拆书任务
- 系统可以对长篇文本完成非一次性分析
- 系统可以生成并保存一条模板库模板
- 用户新建项目时可以选择模板库中的模板
- 项目创建后保存模板快照
- 模板快照能够实际影响后续生成结果
- 模板内容保存为抽象写法，不依赖全文上下文

## 20. 结论

本方案的核心不是“把一本几百万字网文塞给模型”，而是把拆书能力做成一个可扩展的离线分析系统，再把结果沉淀为模板库资产，并在新建项目时直接套用到整条创作链路中。

最终形成的能力闭环是：

1. 长篇作品导入
2. 离线拆书分析
3. 模板草稿生成
4. 模板保存入库
5. 新建项目套用模板
6. 模板驱动后续创作生成

这条链路一旦落地，模板库就不再只是“分析结果展示区”，而会成为项目创作的长期能力底座。

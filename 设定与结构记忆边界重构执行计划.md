# 设定与结构记忆边界重构执行计划

对应设计文档：[设定与结构记忆边界重构方案.md](./设定与结构记忆边界重构方案.md)

## 文档用途

这份文档不是边界说明，而是执行拆解、状态维护和推进顺序入口。

- 边界定义与产品口径看 `设定与结构记忆边界重构方案.md`
- 本文只负责“怎么改、先改什么、涉及哪些文件、如何验收”
- 当前项目现状为“只有调试项目，没有正式项目”
- 因此本计划默认采用**硬切策略**
- 不再保留“正式项目迁移兼容优先”的保守口径

---

## 状态口径

| 字段 | 说明 |
|------|------|
| 状态 | `未开始 / 进行中 / 已完成 / 阻塞 / 暂缓` |
| 负责人 | 当前实际推进人，可留空 |
| 开始时间 | 首次进入执行的日期 |
| 完成时间 | 达到当前章节验收口径后的日期 |
| 最后更新 | 最近一次更新日期 |
| 阻塞项 | 当前最直接的阻塞信息，没有则写 `无` |
| 下一动作 | 下一步要做的最小闭环动作 |
| 备注 | 记录实现口径、风险和临时决定 |

---

## 执行前提

当前执行按以下前提推进：

- 当前没有正式用户项目，只有调试项目
- 可以接受旧调试数据报废或重建
- 可以直接收窄旧入口和旧字段
- 可以直接调整生成上下文来源优先级
- `generation_*` 明确视为运行态缓存层，不再保留正式真源口径

---

## 范围边界

本次执行纳入范围：

- `P0` 来源优先级与同类信息去重
- `P0-I` 结构记忆注入增强
- `P1` 设定库收窄为稳定事实层
- `P2` 伏笔追踪降级并入结构记忆
- `P3` 关系图谱展示口径与入口收口

当前不纳入范围：

- 老项目迁移脚本
- 正式项目兼容策略
- 数据回填精细迁移工具
- 运行态候选审核流的高级工作流
- QuestionPool 正文主注入策略调整

---

## 总览看板

| 任务ID | 任务 | 优先级 | 状态 | 当前进度 | 负责人 | 最后更新 | 依赖 | 阻塞项 |
|------|------|------|------|------|------|------|------|------|
| `P0` | 来源优先级与去重 | P0 | 进行中 | `P0-6` 已补齐 `thread_ledger / pov_permission` 第二版 canonical 与优先级；下一步进入 `P0-I1` 做结构记忆注入现状审计 | Codex | 2026-04-16 | 无 | 无 |
| `P0-I` | 结构记忆注入增强 | P0 | 已完成 | `P0-I5` 已完成增强前后 bundle 回归，确认参与感上升且噪音可控 | Codex | 2026-04-16 | `P0-5` 最佳，至少 `P0-4` | 无 |
| `P1` | 设定库收窄与候选设定分区 | P1 | 已完成 | `P1-4` 回看完成，前后端口径已对齐“正式设定 / 候选设定”；下一步切到 `P2` | Codex | 2026-04-16 | `P0` 最佳 | 无 |
| `P2` | 伏笔追踪并入结构记忆 | P2 | 已完成 | `P2-1 ~ P2-4` 已完成：独立入口已下线，结构记忆内已改成事实/规划双模式并支持互跳 | Codex | 2026-04-16 | `P0`, `P1` 部分口径最好先定 | 无 |
| `P3` | 关系图谱展示口径调整 | P3 | 已完成 | `P3-1 ~ P3-3` 已完成：入口已改为“关系图谱展示”，页面口径已明确为展示/观察层，显式关系与运行态关系主次已收口 | Codex | 2026-04-17 | 可并行 | 无 |

---

## 建议实施顺序

按当前“硬切”口径，建议顺序为：

1. 先做 `P0`，把服务端真正的上下文冲突收住。
2. 再做 `P0-I`，在去重前提下放宽结构记忆注入，提升正文参与感。
3. 再做 `P1`，收窄设定库，避免继续制造新冲突。
4. 再做 `P2`，把伏笔事实层并入结构记忆。
5. 最后做 `P3`，收口关系图谱展示页面的定位与文案。

---

## 建议提交粒度

建议按“一个闭环一提交”推进：

1. `P0-0` 现状审计与 bundle 导出
2. `P0-1` 冲突键与 canonical 层定义
3. `P0-2` 设定实体 / 伏笔事实来源优先级
4. `P0-3` 世界状态 / 资源连续性结构化去重
5. `P0-4` 主块/补充块渲染
6. `P0-5` 第一版回归验证
7. `P0-OPT` 低优先级信息压缩
8. `P0-6` 第二版扩展到 `thread_ledger / pov_permission`
9. `P0-I1` 结构记忆注入现状审计
10. `P0-I2` 第一版注入增强（4 类）
11. `P0-I3` 第二版注入增强（2 类）
12. `P0-I4` 强制项/候选项分层与低优先级压缩
13. `P0-I5` 注入增强回归验证
14. `P1-1` 完整度算分与候选设定分区
15. `P1-2` 设定库动态字段 UI 收窄
16. `P1-3` Chat/辅助上下文与服务端实体注入口径收口
17. `P2-1` 伏笔追踪入口下线
18. `P2-2` 结构记忆内事实/规划双模式
19. `P2-3` 事实层口径收窄
20. `P2-4` 伏笔事实与规划关联强化
21. `P3` 关系图谱展示口径与命名收口

---

## P0 来源优先级与去重

### 进度字段

| 字段 | 内容 |
|------|------|
| 状态 | 进行中 |
| 负责人 | Codex |
| 开始时间 | 2026-04-16 |
| 完成时间 |  |
| 最后更新 | 2026-04-16 |
| 阻塞项 | 无 |
| 下一动作 | 进入 `P0-I1`，按当前 bundle 样本审计结构记忆 7 块里哪些实际进入正文上下文、哪些仍偏弱 |
| 备注 | 当前重点不是“结构记忆是否接入生成”，而是“同类信息是否重复注入”；`P0-0` 已确认主要重复集中在 `thread_ledger / world_state_delta / short_term_memory / long_term_memory / retrieval_memory` 的交叉复述，`P0-1` 已产出第一版 `entity / foreshadow / world_state / resource` 的 `conflictKey / canonical` 定义，`P0-2` 已落实 `entitySnapshot > generation_entities`、`foreshadowSnapshot > generation_foreshadows` 的服务端组装优先级，`P0-3` 已把 `world_state` 的 `world:{volumeKey}:{dimension}` 结构化去重落到 `world_state_delta`。`P0-4` 已补齐 `entity / foreshadow` 的 winner + supplement 收口，并与已落地的 `world_state / resource` 一起统一回 bundle；`P0-OPT` 第一版已对 `thread_ledger / foreshadow_plan / resource_continuity` 落摘要压缩，`P0-5` 已通过 [P0-5 bundle回归验证记录.md](./P0-5%20bundle回归验证记录.md) 完成四类 bundle 回归验证；`P0-6` 已把 `thread_ledger / pov_permission` 纳入第二版 canonical 与优先级决策，其中剧情线按“同线唯一主块”收口，信息权限按“章节 > 里程碑 > 卷级”分层补并 |

### 目标

在服务端上下文组装里落实“冲突键 + canonical + 来源优先级 + 主块/补充块”机制，避免：

- `设定库` 与 `generation_entities` 重复
- `伏笔追踪` 与 `generation_foreshadows` 重复
- `世界状态 / 资源连续性` 与 `generation_state_changes` 结构化重复

### 分阶段范围

#### 第一版

先覆盖最适合结构化冲突键的 4 类：

- `entity`
- `foreshadow`
- `world_state`
- `resource`

#### 第二版

在第一版稳定后，再扩展到更偏策略块的 2 类：

- `thread_ledger`
- `pov_permission`

第二版不是“不做”，而是放在第一版收稳后继续推进，避免第一刀把 `generation-context.ts` 改得过重。

### 目标文件

- `server/src/services/generation-context.ts`
- `server/src/services/context-dedup.ts`（新增）
- 必要时：`server/src/services/generation.ts`

### 子任务拆解

| 子任务ID | 子任务 | 产出 | 状态 | 依赖 | 备注 |
|------|------|------|------|------|------|
| `P0-0` | 现状审计 | 产出一份 bundle 审计记录（markdown），包含 bundle 全文、逐 section 标注与重复内容对照 | 已完成 | 无 | 已产出 `P0-0 bundle审计记录.md`，主样本为 `demo-project-last-cultivator / demo-chapter-024 / 第24章：归炉前夜`，并补充 `第15章 / 第21章` 对照样本；同时通过 `server/src/scripts/seed-p0-audit-structure-sample.ts` 补齐 `foreshadow_plans / pov_permissions / antagonist_agendas` 的固定审计样本 |
| `P0-1` | 冲突键定义 | 第一版为 `entity / foreshadow / world_state / resource` 定义 `conflictKey` 与 canonical 规则；第二版补 `thread / pov_permission` | 已完成 | `P0-0` | 已产出 `P0-1 冲突键与canonical定义.md`，第一版先收口为结构化可判定规则；`thread / pov_permission` 继续留到第二版 |
| `P0-2` | 设定实体 / 伏笔事实来源优先级 | `设定库 > generation_entities`、`伏笔追踪 > generation_foreshadows` | 已完成 | `P0-1` | 已在服务端组装层落实 `entitySnapshot / foreshadowSnapshot` 优先级，并补 `generation-context.source-priority.test.ts` 回归；同名实体 / 同名伏笔以正式真源为准 |
| `P0-3` | 世界状态 / 资源连续性结构化去重 | `world:{volumeKey}:{dimension}`、`resource:{owner}:{resourceType}` 的 winner/supplement/discard 规则 | 已完成 | `P0-1` | 已落实 `resource` 与 `world_state` 两条线；`world_state_delta` 现按卷内维度 canonical 合并，列表维度口径固定为“里程碑 winner 主导 + 卷级补并唯一项 + 去重”；第一版仍只做结构化语义去重，不做自由文本语义比较 |
| `P0-4` | 渲染回 bundle | winner 进主块，supplement 进补充摘要，discard 不输出 | 已完成 | `P0-2`,`P0-3` | 已补齐 `entity / foreshadow / world_state / resource` 的主块/补充块收口：实体与伏笔的低优先级补充信息不再直接丢弃，而是折叠回 winner；`discard` 继续不输出 |
| `P0-OPT` | 低优先级信息压缩 | 低优先级项由完整块降为一行摘要 | 进行中 | `P0-4` | 第一版已对 `thread_ledger / foreshadow_plan / resource_continuity` 落摘要压缩；这是注入策略优化，不作为 P0 去重主线验收前提 |
| `P0-5` | 回归验证 | 针对实体、伏笔、世界状态、资源连续性做 bundle 回归样例 | 已完成 | `P0-4` | 已产出 `P0-5 bundle回归验证记录.md`；确认四类信息的 winner / supplement / discard 已真实进入 bundle，未再出现并列重复主块 |
| `P0-6` | 第二版扩展 | 将 `thread_ledger / pov_permission` 纳入 canonical 与优先级决策器 | 已完成 | `P0-5` | 已落实 `thread_ledger` 的同线唯一主块与补充折叠，以及 `pov_permission` 的章节 > 里程碑 > 卷级优先级；同视角不再并列重复输出 |

### 验收口径

- 同一 `conflictKey` 在最终 bundle 中只能有一个主事实块
- 低优先级来源不得覆盖高优先级来源核心字段
- 若低优先级来源保留，只能进入补充摘要
- `world_state / resource_continuity` 不得与对应运行态变化并列重复输出
- `world_state_delta` 列表维度按“里程碑优先 + 卷级补并 + 去重”收口，不再并列出现卷级 / 里程碑双主块
- 回归样例中，重复 token 明显下降且关键信息不丢失

---

## P0-I 结构记忆注入增强

### 进度字段

| 字段 | 内容 |
|------|------|
| 状态 | 已完成 |
| 负责人 | Codex |
| 开始时间 | 2026-04-16 |
| 完成时间 | 2026-04-16 |
| 最后更新 | 2026-04-16 |
| 阻塞项 | 无 |
| 下一动作 | 回到 `P1`，继续推进设定库收窄、完整度算分与候选设定分区 |
| 备注 | 目标不是简单增加注入量，而是提高有效注入量；`P0-I1` 已产出 `P0-I1 结构记忆注入现状审计.md`，当前口径已确认是“前台 7 块，正文主注入 6 类，QuestionPool 不在正文主 bundle”。`P0-I2` 第一版已放宽 `thread_ledger / foreshadow_plan / resource_continuity`：剧情线放宽到“低热 active 但必须直命中当前章/伏笔/双角色”的保留条件，核心伏笔规划允许在未挂 active 事实时仍进入 section，资源连续性降低非高压章节阈值并提高预算；`world_state` 继续保持当前卷强保留、里程碑优先覆盖、前一卷残留补充的既有口径。`P0-I3` 已把 `antagonist_agenda / pov_permission` 纳入增强，`P0-I4` 已把“强制保留 / 预算内追加 / 低优先级摘要压缩”策略扩到后续结构记忆条线，并让 `QuestionPool` 以轻量提醒并入 `working_memory`；`P0-I5` 已通过 [P0-I5 注入增强回归验证记录.md](./P0-I5%20注入增强回归验证记录.md) 完成增强前后对比，确认参与感提升且噪音可控 |

### 目标

把当前“结构记忆虽然参与正文生成，但参与感偏弱”的问题，转化为一条独立执行线：

- 先确认实际注入现状
- 再放宽筛选
- 再引入强制项/候选项分层
- 最后在不明显增加噪音的前提下提高结构记忆参与感

### 目标文件

- `server/src/services/generation-context.ts`
- 必要时：`server/src/services/generation.ts`
- 必要时：调试导出相关接口或验证脚本

### 子任务拆解

| 子任务ID | 子任务 | 产出 | 状态 | 依赖 | 备注 |
|------|------|------|------|------|------|
| `P0-I1` | 注入现状审计 | 抽样导出正文生成 bundle，按 section 统计 7 块结构记忆里哪些实际进入上下文 | 已完成 | `P0-5` 最佳 | 已产出 `P0-I1 结构记忆注入现状审计.md`，明确当前是“前台 7 块，正文主注入 6 类”，`QuestionPool` 仍走卷纲/里程碑的 `questionPoolBundle` 链路 |
| `P0-I2` | 第一版注入增强 | 放宽 `thread_ledger / foreshadow_plan / world_state / resource_continuity` 的筛选与上限 | 已完成 | `P0-I1` | 已放宽 `thread_ledger / foreshadow_plan / resource_continuity` 的筛选与预算，并补对应服务端回归用例；`world_state` 当前卷强保留、里程碑优先、前一卷残留补充的现有口径已满足第一版目标，因此本轮未额外改动；决策依据见 `P0-I2 放宽决策说明.md` |
| `P0-I3` | 第二版注入增强 | 将 `antagonist_agenda / pov_permission` 纳入增强，评估 `QuestionPool` 是否以轻量摘要参与 | 已完成 | `P0-I2` | 已放宽 `antagonist_agenda / pov_permission` 的保留上限，并补充低优先级摘要压缩；`QuestionPool` 已作为轻量提醒并入 `working_memory`，但仍未升成独立主 section |
| `P0-I4` | 强制项 / 候选项分层 | 为结构记忆建立“强制保留 / 预算内追加 / 低优先级压缩”策略 | 已完成 | `P0-I2` | 当前已在 `thread_ledger / foreshadow_plan / antagonist_agenda / pov_permission / resource_continuity / QuestionPool` 上落第一版分层：强命中优先完整、候选项预算内追加、后排条目压缩为摘要 |
| `P0-I5` | 注入增强回归验证 | 对增强前后 bundle 做定量与定性比对，确认参与感上升但噪音可控 | 已完成 | `P0-I4` | 已产出 `P0-I5 注入增强回归验证记录.md`；固定样本下 `thread_ledger / foreshadow_plan / resource_continuity / antagonist_agenda / pov_permission / QuestionPool` 的参与度均提升，当前未见明显噪音回潮 |

### 第一版覆盖范围

第一版优先增强的 4 类：

- `thread_ledger`
- `foreshadow_plan`
- `world_state_delta`
- `resource_continuity`

### 第二版覆盖范围

第二版再增强的 2 类：

- `antagonist_agenda`
- `pov_permission`

`QuestionPool` 当前定位仍以：

- 规划提醒
- 搜索与回填候选
- 统一提醒区

为主，后续只探索轻量摘要式参与，不在第一版中作为正文主注入块强推。

`candidate_entities` 当前固定口径为：

- 只消费规划链路提供的 `availableCharacterNames`
- 已进入 `focus_entities` 的角色不再重复进入候选区
- 当前弱提示预算上限先固定为 `5` 条

### 验收口径

- 结构记忆实际注入现状有明确抽样证据
- 增强后，结构记忆在正文 bundle 中的参与感明显提高
- 放宽筛选后，主信息仍以高优先级条目为主
- 低优先级条目以摘要形式出现，不显著拉高噪音
- 增强前后对比中，没有出现大面积重复注入回潮

---

## P1 设定库收窄与候选设定分区

### 进度字段

| 字段 | 内容 |
|------|------|
| 状态 | 已完成 |
| 负责人 | Codex |
| 开始时间 | 2026-04-16 |
| 完成时间 | 2026-04-16 |
| 最后更新 | 2026-04-16 |
| 阻塞项 | 无 |
| 下一动作 | 切到 `P2-1`，开始处理伏笔追踪入口下线与并入结构记忆 |
| 备注 | 当前 `static_* / current_*` 已经在代码里分层，适合直接收窄；其中 `P1-4` 已先行落一刀，前端 `context-assembler / buildWorldStateSummary` 与服务端实体注入已不再把 `current_*` 当主信息注入。本轮前端已把设定库区分为“正式设定 / 候选设定”，并支持把运行态候选一键收为本地 `draft`；`P1-4` 已通过 [P1-4 上下文收口回看记录.md](./P1-4%20上下文收口回看记录.md) 回看确认前后端口径一致 |

### 目标

把 `设定库` 收窄成“稳定事实层”，并把 AI 发现的新设定统一放入“候选设定 / 待确认设定”分区。

其中人物卡的具体改动范围明确为：

- 保留 `static_*` 作为稳定人格内核
- 收窄 `current_*`，不再作为设定库主维护字段
- 完整度从“静态+动态”改为“静态为主”
- 前后端上下文不再把 `current_*` 作为人物卡主信息重复注入

### 目标文件

- `app/src/components/LoreWorkspace.tsx`
- `app/src/lib/lore-entity.ts`
- `app/src/lib/context-assembler.ts`
- `server/src/services/generation-context.ts`
- 必要时：`app/src/types/domain.ts`

### 子任务拆解

| 子任务ID | 子任务 | 产出 | 状态 | 依赖 | 备注 |
|------|------|------|------|------|------|
| `P1-1` | 完整度算分调整 | 人物卡完整度从“静态+动态”改为“静态为主” | 已完成 | `P0` 最佳 | 当前完整度已只按 `static_*` 字段计算，不再把 `current_*` 动态字段计入人物卡完整度 |
| `P1-2` | 动态字段 UI 收窄 | `current_*` 字段移除、只读或历史化 | 已完成 | `P1-1` | 人物卡编辑区已只保留稳定人格内核；`current_*` 仅以“历史动态记录 / 待迁移信息”只读提示展示，不再作为主编辑字段 |
| `P1-3` | 候选设定分区 | 用 `draft` 先承接“候选/待确认”区 | 已完成 | 无 | 第一版未新建表结构；当前前端已区分“正式设定 / 候选设定”，本地 `draft` 与运行态候选均进候选区，运行态候选支持一键收为本地 `draft` |
| `P1-4` | 上下文收口 | 前端 `context-assembler` 与服务端实体注入不再重复提取 `current_*` | 已完成 | `P1-2` | 已产出 `P1-4 上下文收口回看记录.md`；确认前端 `context-assembler / generation-context / buildWorldStateSummary` 与服务端实体注入均不再把 `current_*` 当主信息，`draft` 也不再作为正式设定真源参与正文上下文 |

### 验收口径

- `设定库` 页面默认只维护稳定事实
- AI 发现的新设定不直接进入正式设定，而是先进候选区
- 人物卡完整度不再因为迁走动态字段而失真
- 服务端实体注入不再提取 `current_*` 字段作为主信息，动态状态改由对应结构记忆 section 承载

---

## P2 伏笔追踪并入结构记忆

### 进度字段

| 字段 | 内容 |
|------|------|
| 状态 | 已完成 |
| 负责人 | Codex |
| 开始时间 | 2026-04-16 |
| 完成时间 | 2026-04-16 |
| 最后更新 | 2026-04-16 |
| 阻塞项 | 无 |
| 下一动作 | 切到 `P3-1`，开始处理关系图谱展示口径与命名收口 |
| 备注 | 已把独立 `伏笔追踪` 一级入口下线，事实维护已并入 `ForeshadowPlanPanel` 的“伏笔事实 / 伏笔规划”双模式；事实层继续只保留状态、章节与备注，规划层保留卷次、条件与效果；当前已支持按标题自动匹配事实/规划，并可从规划跳到事实、从事实跳回规划 |

### 目标

把 `伏笔追踪` 从一级入口降级，并并入 `结构记忆`，形成：

- `事实状态`
- `规划安排`

双模式工作流。

### 目标文件

- `app/src/components/AppShell.tsx`
- `app/src/components/ForeshadowWorkspace.tsx`
- `app/src/components/ForeshadowPlanPanel.tsx`
- `app/src/components/StructureWorkspace.tsx`
- `app/src/stores/foreshadow-store.ts`
- `app/src/stores/foreshadow-plan-store.ts`

### 子任务拆解

| 子任务ID | 子任务 | 产出 | 状态 | 依赖 | 备注 |
|------|------|------|------|------|------|
| `P2-1` | 一级入口下线 | 左侧主导航移除独立 `伏笔追踪` | 已完成 | 无 | 独立一级入口已移除，相关跳转统一改到结构记忆的 `foreshadow-plan` section |
| `P2-2` | 事实/规划双模式 | 在 `ForeshadowPlanPanel` 顶部加 Tab：`伏笔事实 / 伏笔规划` | 已完成 | `P2-1` | 事实 Tab 已复用 `ForeshadowWorkspace` 的核心列表+编辑逻辑，规划 Tab 保持现有 Panel |
| `P2-3` | 事实层口径收窄 | `伏笔追踪` 只保留事实状态，不再维护计划性内容 | 已完成 | `P0` 最佳 | 事实层当前只维护标题、摘录、备注、状态与来源/回收章节；规划性字段继续留在 `ForeshadowPlan` |
| `P2-4` | 事实与规划关联强化 | 按 title 自动匹配高亮，支持从规划跳到事实、从事实跳到规划 | 已完成 | `P2-2` | 第一版已按 title 自动匹配“已关联事实 / 未关联事实”，并支持从规划定位到事实、从事实跳回规划；匹配不上时会明确提示先补录对应项 |

### 验收口径

- 前台不再存在独立一级 `伏笔追踪`
- 用户仍能在结构记忆内维护伏笔事实状态和规划
- 事实层与计划层口径明确，不再互相重叠

---

## P3 关系图谱展示口径收口

### 进度字段

| 字段 | 内容 |
|------|------|
| 状态 | 已完成 |
| 负责人 | Codex |
| 开始时间 | 2026-04-17 |
| 完成时间 | 2026-04-17 |
| 最后更新 | 2026-04-17 |
| 阻塞项 | 无 |
| 下一动作 | 回看全线执行状态，确认是否还需要补总体验证或补充文档 |
| 备注 | 当前已确定保留一级入口，但页面定位必须改清楚；本轮已完成导航改名、页面展示层口径提示，以及“显式关系真源 > 运行态关系观察层”的主次收口 |

### 目标

保留一级入口，但将其明确为：

- `关系图谱展示`
- 关系观察与诊断视图
- 不被用户误解为正式关系真源

同时明确关系数据的主次关系：

- `EntityRelation` 继续作为正式关系真源
- `generation_relationships` 降级为运行态观察层
- 图谱页面继续展示两层，但默认以显式关系为主

### 目标文件

- `app/src/components/AppShell.tsx`
- `app/src/components/GraphWorkspace.tsx`
- `server/src/services/generation-context.ts`
- 相关文案文件或帮助文档

### 子任务拆解

| 子任务ID | 子任务 | 产出 | 状态 | 依赖 | 备注 |
|------|------|------|------|------|------|
| `P3-1` | 入口命名调整 | 导航与页面标题改成 `关系图谱展示` | 已完成 | 无 | 导航与页面标题已改成 `关系图谱展示` |
| `P3-2` | 页面口径收口 | 页面内强调“展示/观察，不是正式维护台账” | 已完成 | 无 | 页面头部、详情说明与运行态区块均已强调“展示 / 观察 / 诊断视图”定位 |
| `P3-3` | 关系数据来源优先级口径 | 明确 `EntityRelation > generation_relationships`，图谱展示两层但显式关系为主 | 已完成 | `P0` 最佳 | 页面内已明确“显式关系真源 > 运行态关系观察”，服务端 `generation-context` 的关系块也同步改成“显式关系真源 / 运行态关系观察”口径 |

### 验收口径

- 页面保留一级入口
- 产品口径清楚说明它是展示层
- 用户不会把它误解成正式关系维护页
- 显式关系与运行态关系在口径上主次明确，不再被理解成两个并列真源

---

## 执行后建议验证

本轮口径对齐与定向 smoke 结果已记录在：[设定与结构记忆边界重构验收记录.md](./设定与结构记忆边界重构验收记录.md)

虽然当前没有正式项目，但在硬切完成后，仍建议至少做 3 轮人工验证：

1. `生成上下文验证`
   - 用调试项目第 5 章发起一次正文生成
   - 通过 generation-debug 或服务端调试口径导出完整 bundle
   - 逐 section 检查：
      - 同名实体是否只出现一次
      - 同名伏笔是否只出现一次
      - 世界状态是否只保留当前卷 + 前一卷
      - 世界状态列表维度是否按“里程碑优先 + 卷级补并 + 去重”收口
      - 资源连续性是否没有和运行态状态变化并列重复
      - 候选出场人物是否只消费 `availableCharacters`，且不与焦点人物重复
   - 产出一份 markdown 审计记录，包含 bundle 全文、逐 section 标注与重复内容对照
2. `设定库验证`
   - 候选设定是否默认进入 `draft`/候选区
   - 确认收录后是否才进入正式设定
   - 完整度算分是否与新 UI 一致
3. `伏笔与结构记忆验证`
   - 结构记忆中是否能通过双 Tab 分别查看 `事实状态 / 规划安排`
   - 规划项与事实项是否能通过 title 自动高亮对应
   - 伏笔一级入口下线后，用户仍能完成同样维护闭环

---

## 后续建议补充文档

这份执行计划进入实施后，后续可继续补：

1. `来源优先级与去重测试清单.md`
2. `设定库候选设定交互说明.md`
3. `伏笔事实层/规划层合并交互说明.md`

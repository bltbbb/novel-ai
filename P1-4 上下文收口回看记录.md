# P1-4 上下文收口回看记录

对应执行计划：[设定与结构记忆边界重构执行计划.md](./设定与结构记忆边界重构执行计划.md)

## 回看目的

本记录用于确认 `P1-4` 是否已经达到当前阶段的收口要求：

- 前端上下文组装不再把 `current_*` 当作人物卡主信息
- 服务端实体注入不再把 `current_*` 作为主信息回灌正文
- `draft` / 候选设定不会被当成正式设定真源
- 设定库 UI 已经和“稳定事实层 / 候选设定层”口径一致

## 结论摘要

当前回看结果是：

1. 前端 `context-assembler` 已只消费正式设定，且明确排除 `current_*`
2. 前端 `generation-context` 的世界状态快照已只基于正式设定的稳定字段
3. 服务端 `generation-context` 已对 `draft` 与 `current_*` 做双重收口
4. `LoreWorkspace` 已区分“正式设定 / 候选设定”，并把 `current_*` 降为只读历史提示

结论上，`P1-4` 当前可以判定为完成。

## 回看明细

## 1. 前端 `context-assembler`

文件：[app/src/lib/context-assembler.ts](/H:/myproject/novel-ai/app/src/lib/context-assembler.ts:63)

当前行为：

- `isCurrentFieldKey()` 会识别并排除 `current_*`
- `assembleChatContext()` 只先取 `confirmedEntities`
- 实体命中匹配时，不会拿 `current_*` 去做正文召回
- 实体引用构建时，也不会把 `current_*` 带进参考块

关键定位：

- [isCurrentFieldKey](/H:/myproject/novel-ai/app/src/lib/context-assembler.ts:63)
- [matchEntitiesByContent](/H:/myproject/novel-ai/app/src/lib/context-assembler.ts:80)
- [buildEntityReference](/H:/myproject/novel-ai/app/src/lib/context-assembler.ts:98)
- [confirmedEntities](/H:/myproject/novel-ai/app/src/lib/context-assembler.ts:173)

回看结论：

- 前端聊天上下文已经不再把人物卡动态字段当成主信息消费

## 2. 前端 `generation-context`

文件：[app/src/lib/generation-context.ts](/H:/myproject/novel-ai/app/src/lib/generation-context.ts:71)

当前行为：

- `buildGenerationContextBundle()` 先过滤出 `confirmedEntities`
- `worldState` 使用的是 `buildWorldStateSummary(confirmedEntities)`

关键定位：

- [confirmedEntities](/H:/myproject/novel-ai/app/src/lib/generation-context.ts:71)
- [buildWorldStateSummary 调用](/H:/myproject/novel-ai/app/src/lib/generation-context.ts:94)

回看结论：

- 前端生成预览链路也已经只把正式设定当真源

## 3. 前端 `buildWorldStateSummary`

文件：[app/src/lib/generation-utils.ts](/H:/myproject/novel-ai/app/src/lib/generation-utils.ts:4)

当前行为：

- `isCurrentFieldKey()` 会过滤 `current_*`
- `getStableEntityFieldEntries()` 只保留稳定字段
- `buildWorldStateSummary()` 只会输出稳定字段摘要

关键定位：

- [isCurrentFieldKey](/H:/myproject/novel-ai/app/src/lib/generation-utils.ts:4)
- [getStableEntityFieldEntries](/H:/myproject/novel-ai/app/src/lib/generation-utils.ts:8)
- [buildWorldStateSummary](/H:/myproject/novel-ai/app/src/lib/generation-utils.ts:33)

回看结论：

- 前端“世界状态快照”已经不再从人物卡动态字段抓状态

## 4. 服务端 `generation-context`

文件：[server/src/services/generation-context.ts](/H:/myproject/novel-ai/server/src/services/generation-context.ts:344)

当前行为：

- 服务端实体读取后先过滤 `draft`
- `mergeEntityRowsWithSnapshotPriority()` 不再把 `current_*` 作为补并字段
- `buildEntityBlocks()` 输出时也排除 `current_*`

关键定位：

- [isEntityDynamicFieldKey](/H:/myproject/novel-ai/server/src/services/generation-context.ts:344)
- [mergeEntityRowsWithSnapshotPriority](/H:/myproject/novel-ai/server/src/services/generation-context.ts:387)
- [draft 过滤](/H:/myproject/novel-ai/server/src/services/generation-context.ts:3732)
- [buildEntityBlocks](/H:/myproject/novel-ai/server/src/services/generation-context.ts:2882)

回看结论：

- 服务端正文 bundle 已不再把 `draft` 或 `current_*` 当作正式实体主信息

## 5. 设定库 UI

文件：[app/src/components/LoreWorkspace.tsx](/H:/myproject/novel-ai/app/src/components/LoreWorkspace.tsx:218)

当前行为：

- 正式设定与候选设定已分区
- `draft` 条目进入候选区，不再和正式设定混排
- 运行态候选已进入候选区，并支持一键“收为候选”
- 人物卡编辑区只编辑静态人格字段
- `current_*` 仅以“历史动态记录 / 待迁移信息”只读提示展示

关键定位：

- [confirmedEntities / candidateDraftEntities](/H:/myproject/novel-ai/app/src/components/LoreWorkspace.tsx:218)
- [正式/候选总览文案](/H:/myproject/novel-ai/app/src/components/LoreWorkspace.tsx:517)
- [正式设定区标题](/H:/myproject/novel-ai/app/src/components/LoreWorkspace.tsx:567)
- [历史动态记录 / 待迁移信息](/H:/myproject/novel-ai/app/src/components/LoreWorkspace.tsx:778)
- [候选设定区](/H:/myproject/novel-ai/app/src/components/LoreWorkspace.tsx:1036)
- [运行态候选收为候选](/H:/myproject/novel-ai/app/src/components/LoreWorkspace.tsx:386)

回看结论：

- UI 口径已经基本和“正式设定 = 稳定事实层，候选设定 = 待确认层”对齐

## 6. 人物卡完整度

文件：[app/src/lib/lore-entity.ts](/H:/myproject/novel-ai/app/src/lib/lore-entity.ts:27)

当前行为：

- `CHARACTER_CARD_FIELD_TOTAL` 只统计静态字段定义
- `getCharacterCardFilledCount()` 只数静态字段
- `getCharacterCardCompleteness()` 的分母分子都已经不含动态字段

关键定位：

- [CHARACTER_CARD_FIELD_TOTAL](/H:/myproject/novel-ai/app/src/lib/lore-entity.ts:27)
- [getCharacterCardFilledCount](/H:/myproject/novel-ai/app/src/lib/lore-entity.ts:119)
- [getCharacterCardCompleteness](/H:/myproject/novel-ai/app/src/lib/lore-entity.ts:125)

回看结论：

- 完整度不会再因为收窄 `current_*` 而“看起来被砍坏”

## 当前仍保留的边界

虽然 `P1-4` 可收口，但还有两个边界说明需要保留：

1. 运行态层仍然可以出现 `current_*`
   - 这不是 bug
   - 因为运行态缓存层本来就允许承接动态信息
   - `P1` 的目标是“不要再把它当正式设定真源”，不是“禁止运行态层存在动态字段”

2. 候选设定当前仍是第一版前端承接
   - 本轮没有新建独立表结构
   - 仍以本地 `draft` + 运行态候选区承接
   - 但对当前阶段目标已经够用

## 验收口径核对

对照执行计划里的 `P1` 验收口径：

1. `设定库` 页面默认只维护稳定事实
   - 已满足
2. AI 发现的新设定不直接进入正式设定，而是先进候选区
   - 已满足（候选区承接本地 `draft` 与运行态候选）
3. 人物卡完整度不再因为迁走动态字段而失真
   - 已满足
4. 服务端实体注入不再提取 `current_*` 字段作为主信息
   - 已满足

## 回看结论

`P1-4` 当前可判定为完成。

如果只看当前阶段目标，`P1` 整体也已经具备收口条件。下一步可以切回后续主线：

- `P2` 伏笔追踪并入结构记忆
或
- `P3` 关系图谱展示口径收口
